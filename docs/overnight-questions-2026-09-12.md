# Overnight questions — 2026-09-12

## Reviewer

1. **ANSWERED 2026-09-12: it was Isabella. §0ar's line closed.** ~~The 12:08 iPhone-Safari sign-in for `j.barnes@`~~ (2026-09-11, IP 90.216.38.36, the same address as your five other rows).** Both of your messages carried the `[me / not me]` placeholder. §0ar's last line records it as unattributed; say which and I close it.
2. **DONE 2026-09-12 (answer: rename).** ~~Design-final PNG filenames with keystroke slips.~~ Renamed with `git mv` to `…-selection-2/3.png` (12-13) and `…-selection-2/3/4.png` (SS-01); the flow ID is now in every name. `ath-adult-12-13 final/` holds `ATH-ADULT-12-313 …` and `…My da2ta…`; `STAFF-SS-01 final/` holds `…Dashboard 2and…`, `…and7 phone…`, `…Da3shboard…`. They are different crops, not duplicates (md5 checked), so all were committed as the flow's PNG exports under their current names — renaming your files is not mine to do. Say if you want them renamed; a `git mv` each.
3. **Awaiting your check.** `2.png` to `6.png` in `STAFF-SS-01 final/` were committed as SS-01's own exports on the strength of the folder they sit in. If any is a scrap, say which and it comes out.
4. **ANSWERED 2026-09-12** — region stays Ireland (closed, decided against); Supabase Pro gated on the pilot signing; §0ad filed as build with the `rpeClosesAt` rule; mechanism and DPA/legal are yours. ~~Pilot-readiness list, group 1 ownership.~~ Three of the eight need only you (Supabase Pro, the `mechanism` sentence to medics, DPA/legal) and one needs your cutoff decision (§0ad). Nothing on the builder's side moves on those until you say.

5. **DECIDED 2026-09-12: leave the 228 files in history — no rewrite, no force-push; a force-push on a branch two live sessions work from risks more than a smaller repo is worth. Revisit only if both sessions are idle and the size becomes a real problem.** ~~My error, corrected in-tree, your call on history.~~ Commit `c32af7f` staged the two design folders by directory to pick up the PNG renames, which also added 228 nested files — the other flows' boards, `_ds/` (the Claude Design bundle that is never committed), uploads, scraps, exports — about 42 MB. Caught on the commit's own status output and reverted in `b1d0ce8` + the follow-up: the tree is back to the per-flow set (20 files across the two folders). The files stay in history on `athlete-spec-builder` (three commits). I have not rewritten or force-pushed anything — the builder merges from this branch. If you want the 42 MB out of history, say so and I will do it the morning you are watching, after the builder has synced.

6. **DECIDED 2026-09-12 (Isabella): bottom bar with a "More" sheet; §0af's top bar superseded. Recorded on the to-do list, the decisions log and the STAFF-SHELL brief.** ~~STAFF-SS-01, decision D1 (from the builder's record, for you).~~ The approved dashboard board puts a bottom bar and a "More" sheet on the phone shell; §0af decided a compact top bar with a menu. The builder built nothing and recommends holding §0af. Which stands? Nothing on the staff phone shell moves until you say.
7. **§0aa, direction.** Not built overnight because the list carried no decision. Builder question 3 recommends `resolveGymSetConflict`; it is on the pilot list as group 1 ("decision, then build").
8. **§0at, new, group 1.** Gym tonnage is stored only by the correction RPC (0045's own comment), so 41 of 45 real sessions on scratch have no total, My data's history reads "Not logged" beside a detail that sums 4762 kg, and staff volume means average the corrected four. Fix direction is the builder's call unless you prefer one: derive at read time (removes the class) or maintain on every set log + backfill.
9. **Migrations 0103, 0104, 0105 are on scratch and not on production** (read-only, both, this morning). None touches data in a way that needs a pre-check beyond what their headers say; they wait for your deploy instruction like everything else.

## Builder

1. **§0ah — `sessions.duration_min` stays nullable; should it become `NOT NULL`?** The decision left this open ("decide whether to add a NOT NULL with a backfill or leave the column as is and guard at the boundary"). Built overnight: the boundary — `NewSessionForm` and `SessionEditForm` refuse an empty or out-of-range duration, `createSession` and `updateSession` refuse a null or non-positive one before any I/O. Scratch holds 0 null durations; production is unread. **Recommendation:** leave the column nullable for now and add `NOT NULL` in a later migration only after a read-only count on production shows zero nulls (or a backfill value you choose — 60, the form's default, is the honest candidate). A migration that fails on production data is worse than a boundary that already holds.
2. **§0ah — the week-template path can still create a null-duration session.** `WeekTemplateBuilder` allows "no planned duration" per template session, and `applyWeekTemplate` inserts `duration_min` straight from the template, so applying such a template creates the exact session the form now refuses. Not touched overnight: it is a second product surface with its own copy. **Recommendation:** require a duration in the template builder too (same 5–240 rule, default 60) and have `applyWeekTemplate` refuse a null; own commit once you say.
3. **§0aa — the offline gym-set discard: no fix direction is recorded, so it was not built.** The entry describes the defect (OutboxFlusher's gym branch dequeues any duplicate-key error as "sent" with no lookup, so a set queued offline whose slot was since filled with a different value is dropped without telling the athlete) and the precedent (the wellness, RPE and nutrition branches call `resolve*Conflict`, compare the stored row with the queued one, and surface a real conflict on `/today`), but it carries no DECIDED line. **Recommendation:** give the gym branch the same shape — `resolveGymSetConflict` reads the live set for the slot (`gym_set_logs_current` by session log and set index), treats an identical row as the athlete's own replay (dequeue as sent) and a different one as a conflict (keep the queued item, surface it on `/today` with the two values so the athlete chooses), and the three-branch guard in `test-mutation-retry.ts` / `test-silent-saves.ts` gains a fourth. Own commit once you say.
4. **§0ai — the schedule grid's draft card can still stage a session with no group.** `SelectedSessionPanel`'s "Add to day" needs only a title; a group-less draft now fails at publish with "Not published: {title}: A session needs at least one group." (the `createSession` boundary), which is honest but late. The decision named the create-session form, so the grid was left alone. **Recommendation:** disable "Add to day" until a group is chosen, with the same caption the form now carries; own commit once you say.
5. **ANSWERED 2026-09-12 (yes) — BUILT `67cf9aa`: the athlete report reads the same classifier and the same session read as the squad report.** ~~§0ad — the athlete report's own compliance figure does not apply the RPE cutoff yet.~~ The compliance report now counts an RPE only if the original submission was before `rpeClosesAt` (built, `rpeSubmittedInTime` / `complianceRpe.ts`). `athleteReport.ts`'s per-athlete compliance percentage still counts any `training_entries_current` row on the date, so for an athlete with a late rating the two staff screens show different numbers for the same period. The dashboard's "RPE, yesterday" track cannot disagree (it only ever looks inside the window). Not touched: the instruction named the compliance report. **Recommendation:** apply the same classifier in `athleteReport.ts` (read originals with `session_id, submitted_at`, the sessions' windows, key by session) — one commit, and MET-012's note in `docs/metrics.md` already says it is pending.
6. **ANSWERED 2026-09-12 (queuedAt, believed when earlier than arrival and within 24 hours; tests both ways) — BUILT `0c63b4c`, migration 0105 on scratch.** ~~§0ad — a rating queued offline and flushed after the cutoff now counts as a miss.~~ `submitted_at` is the database's `now()` at insert; the outbox carries `queuedAt` (the athlete's device time when they rated) but does not send it. The RPE screen refuses a rating after the close, so a late-flushed queue item is the one honest way an in-time rating can carry a late `submitted_at`. **Recommendation:** have the outbox flush pass `submitted_at: queuedAt` on insert for training entries (the column is client-writable on insert; `entry_date` is already trusted from the same device), so the report judges when the athlete rated, not when their phone reconnected. Own commit once you say — it trusts device time for a compliance-relevant timestamp, which is your call.
7. **STAFF-SS-01 — the in-page group filter bar on a phone.** The board's notes say "Group filter bar and chips unchanged. On a phone only the active chip appears, at 44px." Built: the shell's title bar shows the active group as a chip (`af09c17`), and the page's own chip row is untouched (every chip still shows, wrapping). If only the active chip were shown, nothing on the page would change the group. **Recommendation:** leave the chip row as it is on a phone — it wraps to two rows on the dashboard at 375 and every chip is a 44px target — and treat the board's line as describing the title bar's chip, which is what was built. Say if you meant a picker.
