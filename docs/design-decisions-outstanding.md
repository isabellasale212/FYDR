# Design decisions outstanding — every unbuilt item across `docs/designs`

**Living list, opened 2026-09-12.** One row per item that a design board proposed and the
build did not do. The source of each row is the flow's A/B/C/D record in
`docs/overnight-records-2026-09-12.md`; this sheet is the decision view of the same items.
When a new design folder lands, its record is written, its A items (and B items with a
nearest existing token) are built, and its C and D items are appended here in the same
format. When an item is built or declined, its row is struck through with the commit or
the decision, not deleted.

**Cost:** small = under an hour · medium = an afternoon · large = a day or more, or its own
brief. **⚠ migration** = needs a database migration. **⚠ permissions** = touches who may
read or write something (CLAUDE.md §5: the test comes before the rule).

**Reading the groups.** (a) is safe to say yes to as a batch. (b) is where a board disagrees
with something already decided and recorded — each needs an explicit "reverse" or "keep".
(c) needs data or a query we do not have today; the cost is mostly there. (d) cannot be
expressed in the 171 tokens; each is a proposed new token (or a substitute), per §0.01.

---

## Decisions — 2026-09-12 (Isabella), recorded by the reviewer

Read each row below against its group; a row not named here is **approved and queued**
("everything else in groups (a) to (d)").

**Struck — already live:** STAFF-SS-01 C5 (the last-admin guard at the database is migration
0101, on production since 2026-09-11; 0102 covers the self-grant).

**Declined:** ATH-ADULT-08 D1 (the answers' words stay "Yes / Roughly / No") · ATH-ADULT-12
D2 (the active segment stays the accent-filled pill) · ATH-ADULT-12 C4 (deltas stay "on last
week") · ATH-ADULT-12 B6 (hero eyebrows stay `--muted`) · ATH-ADULT-12 D5 (readiness stays
0–100) · **the 3px bar pattern** wherever it appears (ATH-ADULT-12 B5's `--border-accent-w`
row bar and the same bar in STAFF-SS-01 B1 — the rest of B1's substitutions stand) ·
ATH-ADULT-10 C2 (no confirmation before finishing early) · **motion tokens** (ATH-ADULT-09 B3
and any other `--ease-*` / duration proposal — the "no motion tokens" rule stands) · **the
second nutrition question** (ATH-ADULT-08/12 D2/D6) · STAFF-SS-01 C4 ("Send a reminder").

**Gold — kept on navigation, reversed inside the gym logger:** ATH-ADULT-12 D4 / STAFF-SS-01
D2 declined (the gym glyph keeps its gold on the athlete tab bar and the staff sidebar);
ATH-ADULT-09 D4 **approved** (inside the logger the logged keys, the progress fill and the
"recommended" line move to the accent family).

**Approved and queued — everything else in (a) to (d)**, including the gym logger rebuild
(ATH-ADULT-09 C1) **with two new tokens, `--hit-lg: 56px` and `--hit-md: 52px`** (09 B2 —
a §0.01 system decision: the builder dates them beside their values in `tokens.css` and
records them in the Decisions Log; the other (d) rows that name a token are approved on the
same footing, each dated at the value). STAFF-SS-02-05 C9 is approved with a wider rule than
its row: **the coach does not see body mass at all — the section as well as the three
buttons** (Q27 of the data-architecture briefing, closed 2026-09-12).

**Later the same day (evening):** PATTERN-S4 D1 (sessions live on create, no publish/discard) — **DECLINED**: publish stays; "nothing changes on their phone until you publish" is the schedule's core promise and dropping it discards the offline-publish protection just built (§0al). Every S4 row that depends on D1 falls with it. And a new rule outside the sheet: the injured-athlete proposal gate applies to every role except the medic, at the database (to-do §0bb).

**Two decisions from the same message, outside the sheet:** the coach keeps the permission
to create an injury record, and the `/injuries` board shows "+ Injury" to the coach as well
(the coach's form is the non-clinical one, PATTERN-S3 C9; clinical fields stay medic-only)
— filed as a defect on to-do §0av. And Q27 above.

---

## (a) Cheap and uncontroversial

| Flow | ID | What it changes | Why it isn't built | Recommendation · cost |
|---|---|---|---|---|
| ~~ATH-ADULT-04~~ | ~~C4~~ | ~~The check-in page would say *who* corrected an entry.~~ **Built with C1** — visibility.md withholds nothing about it and My data already names the person; "Corrected by {name} on {date}" · | | |
| ~~ATH-ADULT-06~~ | ~~C1~~ | ~~A subhead under the RPE title.~~ **Kept as built** (decided 2026-09-12) · | | |
| ~~ATH-ADULT-09~~ | ~~B4~~ | ~~The set keys go from 42px to 44px tall.~~ **Built** · | | |
| ~~ATH-ADULT-09~~ | ~~C5~~ | ~~Wake Lock and a buzz on log.~~ **Built** (`lib/wakeLock.ts`; on open, on visibility, released on leave; 10 ms buzz where present) · | | |
| ~~to-do §0az~~ | ~~—~~ | ~~The group reorder arrows: a silent no-op for the medic, the S&C and the nutritionist.~~ **Built** 2026-09-12 (added here as uncovered by the sheet: the to-do decision names the BlockedButton treatment; `moveGroup` checks its rows) · | | |
| ~~ATH-ADULT-10~~ | ~~C1~~ | ~~"Finish early" moves to the header.~~ **Built** with 09 C1 (dashed, neutral, 44px beside the title while sets remain; "Finish session" is the footer's primary once every set is logged) · | | |
| ~~ATH-ADULT-13~~ | ~~C4~~ | ~~"4 of 6 shown".~~ **Declined** · | | |
| ~~ATH-ADULT-13~~ | ~~C5~~ | ~~No tab bar on the session detail.~~ **Declined** — the bar stays · | | |
| ATH-ADULT-13 | D1 | The §0v "What you reported" card is replaced by a per-row "Corrected · was 100 kg × 8" marker. | The card was built 2026-09-10 with its reasoning (no "by" line); the row marker needs the 3px bar the system lacks (12 B5) and the row-tap correction (13 C2). | **Keep the card until 13 C2/C3 land**, then fold · small now |
| ~~STAFF-SS-01~~ | ~~A1~~ | ~~Doubtful and Ruled out as tone-family cards.~~ **Built** (the availability line's fill and border on the two readiness rows) · | | |
| ~~STAFF-SS-01~~ | ~~A2~~ | ~~Summary cards say which state they are in.~~ **Built** ("Closed · opens a list" / "Open · showing the list", ▸/▾, a --surf2 well closed, the surface with an accent ring open) · | | |
| ~~STAFF-SS-01~~ | ~~A4~~ | ~~Missing check-ins by run length, "Not submitted".~~ **Built** (`lib/missingRuns.ts`; the Wellness in list, longest run first, last entry date) · | | |
| ~~STAFF-SS-01~~ | ~~D3~~ | ~~"Ready for {matchday}" only within 14 days.~~ **Built** (`FIXTURE_RANGE_DAYS = 14`; "Squad readiness · No fixture in the next 14 days" beyond it). The board's "the week card leads and the matchday card is absent" is a structure change, with C2's remaining steps · | | |
| ~~STAFF-SS-01~~ | ~~Q7~~ | ~~Only the active chip on a phone.~~ **Left as is** (decided 2026-09-12) · | | |
| ~~STAFF-SS-02-05~~ | ~~C4~~ | ~~Who will read what, before a status change lands.~~ **Built** (`AvailabilityAudience` on both forms; the coaches by role — no per-athlete coach exists in the data) · | | |
| ~~STAFF-SS-02-05~~ | ~~C8~~ | ~~Empty panels state the requirement.~~ **Built** (body weight, nutrition plan, flags; the injury panel from A4) · | | |
| ~~STAFF-SS-02-05~~ | ~~D2~~ | ~~One rule for blocked controls.~~ **Built** (`BlockedButton`: aria-disabled, the reason on tap/focus, never a title — the builder's chips, the weigh-in trio, the bio Edit, the correction button, the leaderboard family chips; 06-design-system.md §7.0) · | | |
| ~~PATTERN-S3~~ | ~~C4~~ | ~~Who sees what, by name.~~ **Built** with STAFF-SS-02-05 C4 · | | |

## (b) Reverses something we decided

| Flow | ID | What it changes | Why it isn't built | Recommendation · cost |
|---|---|---|---|---|
| ~~ATH-ADULT-08~~ | ~~D1~~ | ~~"Yes, most days / Some days / No".~~ **Declined** — Yes / Roughly / No stay · | | |
| ~~ATH-ADULT-09~~ | ~~D4~~ | ~~The logger drops the gold.~~ **Reversed and built** (fill, keys — logged / current / not reached —, the live head, the deviation line "prescribed 100 kg · +2.5" in --muted; the gold stays on navigation) · | | |
| ~~ATH-ADULT-12~~ | ~~D1~~ | ~~Five segments.~~ **Reversed and built** (Wellness · Gym · Sessions · Nutrition · Tests; Leaderboards keeps its footer row) · | | |
| ~~ATH-ADULT-12~~ | ~~B3~~ | ~~Labels at 11px.~~ **Built** with D1 (`--fs-11`) · | | |
| ~~ATH-ADULT-12~~ | ~~C9~~ | ~~Wraps at Larger Text.~~ **Built** with D1 (basis auto, flex-wrap) · | | |
| ~~ATH-ADULT-12~~ | ~~D2~~ | ~~The active segment as a white card.~~ **Declined** — the accent-filled pill stays · | | |
| ~~ATH-ADULT-12~~ | ~~D3~~ | ~~Deltas never coloured.~~ **Reversed and built** (`.rd-delta` and `.hist-delta` in --muted, the figure --text bold, ↑ ↓) · | | |
| ~~ATH-ADULT-12~~ | ~~C4~~ | ~~Deltas vs the 28-day average.~~ **Declined** 2026-09-12 — the comparison stays "on last week" · | | |
| ~~ATH-ADULT-12 / STAFF-SS-01~~ | ~~D4 / D2~~ | ~~The gym glyph loses its gold on navigation.~~ **Declined** 2026-09-12 — the gold stays on the athlete tab bar and the staff sidebar · | | |
| ~~ATH-ADULT-12~~ | ~~D5~~ | ~~Readiness "of 5".~~ **Declined** — 0–100 stays · | | |
| ~~ATH-ADULT-12~~ | ~~D7~~ | ~~Accent and neutrals only on the athlete's chart.~~ **Built** as `accentOnly`, an athlete-only prop (the line and band were already the accent; the out-of-range markers now are too on My data; staff pages unchanged) · | | |
| ~~ATH-ADULT-12~~ | ~~B6~~ | ~~Eyebrows in --faint.~~ **Declined** · | | |
| ~~ATH-ADULT / catalogue~~ | ~~self-export~~ | ~~An athlete exports their own athlete report (the catalogue's "confirm", and the SAR route it would double as).~~ | **Isabella, 2026-09-13 (`docs/reports-catalogue-source.md`): NOT BUILT.** Athletes export no report; they ask a coach or sport scientist out of band. No request button, no queue, no new entity. **The one affordance that existed — Me › "Export my data" and its CSV route `/me/export` (the Article 20 export) — was removed the same day** with `lib/queries/myDataExport.ts`; `12-me.md`, `20-route-map.md` and `09-security` §Article 20 record it. A portability request goes through the subject-access process. | Done (removed) |
| ~~catalogue~~ | ~~squad weekly send~~ | ~~The squad weekly report goes out automatically on a Monday morning to a distribution list.~~ | **Isabella, 2026-09-13: NOT BUILT.** On demand only, at any point in the week; no scheduler, no outbound email, no distribution list. **Nothing existed to remove**: the three cron jobs on the database are thresholds, compliance expectations and the retention preview, and no code sends any report anywhere. `21-squad-weekly-report.md` says so; `08-notifications.md`'s weekly-digest rows are the superseded catalogue. | Done (nothing to remove) |
| STAFF-SS-01 | — | (built) the bottom bar + More sheet, superseding §0af's compact top bar | decided and built `af09c17` | — |
| PATTERN-S3 | D2 | A one-time "you were told" card at the top of the athlete's Today, displacing the availability line's emphasis until the status screen is opened. | **Decided 2026-09-12: reverse — the card takes the emphasis while unread.** Waits on C1 (the read flag, ⚠ migration), which is "not now". | With C1, when C1 is scheduled · — |
| PATTERN-S4 | D1 | Sessions are live the moment they are created, moved or removed — no pending queue, no Publish, no Discard. | Reverses the schedule's held-until-publish model (07-schedule.md §6: one session published alone puts a half-updated week on phones; §0al's offline hold and the ghost/Restore were built on it today). Most of the S4 board (the grid-click popover writing on Add, a drag writing at once, the three-timeframe confirmation, "N sessions live") depends on this. | **Decide once.** If reversed: the publish machinery, the sessionStorage hold, the ghost/Restore and the banner go; each write is already its own audited act (0104) · large |
| ~~PATTERN-S3~~ | ~~D1~~ | ~~The coach sees the protocol stage.~~ **Enforced** (`lib/restrictions.ts` drops protocol / stage / diagnosis entries from the restriction line at every query read, for every viewer; the seed no longer writes one — SS-02-05 C7 done with it) · | | |

## (c) Needs new data or a query

| Flow | ID | What it changes | Why it isn't built | Recommendation · cost |
|---|---|---|---|---|
| ~~ATH-ADULT-04~~ | ~~C1~~ | ~~The check-in page shows a "Corrected" pill and the correction when staff corrected the day.~~ **Built** (the day's revision chain; pill + "Corrected by {name} on {date}"; "You sent … at" is the athlete's own time) · | | |
| ~~ATH-ADULT-08~~ | ~~C1~~ | ~~A check-in can be corrected once; a second attempt sees a "spent" card.~~ **Built** — migration 0107, the chain read, the spent state, My data's Corrected mark · | | |
| ~~ATH-ADULT-08~~ | ~~C2~~ | ~~"Correction saved" on this page.~~ **Built** · | | |
| ~~ATH-ADULT-08~~ | ~~C3~~ | ~~The caption "You can correct this once after you submit."~~ **Built** · | | |
| ATH-ADULT-08 / 12 | D2 / D6 | A second weekly question, "How well did you fuel around training?" (1–5), on the check-in and in My data's history. | A data change: a column, the registry, CLAUDE.md rule 8's wording ("one question"). No frame draws it. **⚠ migration** | **Decide the product question first**; if yes, a migration + registry entry + form + history in one brief · large |
| ~~ATH-ADULT-09~~ | ~~C3~~ | ~~The weight stepper's increment comes from the exercise.~~ **Built** — migration 0108 `exercises.weight_step_kg`, set on the library's create form; existing exercises keep 2.5 (no edit form for a library row exists — recommend: add the step to the exercise detail's edit when that screen is built) · | | |
| ~~ATH-ADULT-09~~ | ~~C4~~ | ~~Queued sets send on `online` and from the logger itself.~~ **Built** — `lib/gymOutboxFlush.ts` shared by the logger and Today's flusher; "· 2 waiting to send" on the progress row · | | |
| ~~ATH-ADULT-09~~ | ~~C6~~ | ~~The session-complete summary.~~ **Built** — MET-040 / MET-041 registered; `fetchPersonalBestsBefore`; `lib/gymSummary.ts` · | | |
| ~~ATH-ADULT-10~~ | ~~C3~~ | ~~The early-finish summary.~~ **Built** with 09 C6 (a dashed card, not a 3px bar) · | | |
| ~~ATH-ADULT-11~~ | ~~C2~~ | ~~"Set 1 corrected · was 100 kg × 8" on the logger.~~ **Built** — the page reads the session's revision chains; the strip sits under the exercise's keys · | | |
| ATH-ADULT-12 | C2 | Sessions and Nutrition become hero-card tabs: RPE eleven-bar chart with this week solid, "n = 3 rated sessions this week"; nutrition weeks as question/answer rows. | Needs a Monday-week "RPE this week" and a 28-day RPE mean in `metrics.md`; depends on D1. | Own brief after D1 · large |
| ATH-ADULT-12 | C3 | One plain-English line per hero ("Readiness steady over 28 days, averaging 67." / "Two CMJ results is not enough to show a trend."). | Needs a rule for "steady" and a minimum n, in the registry. | State the rule in `metrics.md` first · medium |
| ~~ATH-ADULT-12~~ | ~~C5~~ | ~~The gym hero becomes "Back squat best 102.5 kg, up 5 kg this block".~~ **Built 2026-09-13** (`lib/gymHero.ts`, `fetchBestSetsInPeriod`): "102.5 kg · Back squat best · × 5 · Sat 5 Sept · up 5 kg on your best before Mon 17 Aug" — the period's window as the comparison (blocks carry no dates the athlete sees), said in the line; the lift with the most working sets in the period; down said as down, an equal best as equal, no earlier best "the first Back squat logged". The zero headline is words ("Nothing logged · in this period"); the four-week Sessions card keeps its own stated span beneath. | | |
| ~~ATH-ADULT-12~~ | ~~C6~~ | ~~An empty period says when the last entry was and offers "Show this season".~~ **Built** on four tabs, to PATTERN-S6's grammar (`fetchMyLatestRecord`, `lib/myDataEmpty.ts`) · | | |
| ~~ATH-ADULT-12~~ | ~~C7~~ | ~~Tests lists the tests assigned to the athlete; one with no result reads "Not logged".~~ **Built** as the club's live test definitions (there is no per-athlete assignment in the schema — `test_definitions` is org-wide; a narrower assignment would be a migration, recommend not unless a club asks) · | | |
| ~~ATH-ADULT-13~~ | ~~C1~~ | ~~The session detail's eyebrow "Gym · Lower A · complete".~~ **Built** · | | |
| ~~ATH-ADULT-13~~ | ~~C2~~ | ~~Tapping a set row on the session detail opens the logger's correction; the per-row "Correct" buttons go.~~ **Built 2026-09-13** as recommended: each row links to `/gym/[sessionId]?log=[logId]&correct=[setId]`; the logger opens a past log by id (read, never created — a log not on record is a 404) with that set's correction open beneath the summary; the detail's inline form and its second `revise_gym_set_log` path are gone. | | |
| ~~ATH-ADULT-13~~ | ~~C3~~ | ~~A "Corrected" pill on the gym history row; the dated sentence on the detail.~~ **Built** — the flag off the live sets' `revision_of` in the list read; no 3px bar · | | |
| ~~STAFF-SS-01~~ | ~~A3 / C3~~ | ~~The attention panel counts athletes; the Flags tab badge shows the same number.~~ **Built** — `athleteTotal` on the attention read, `fetchOpenFlagAthleteCount` for the badge · | | |
| ~~STAFF-SS-01~~ | ~~C2~~ | ~~The dashboard rebuild as a series.~~ **All steps built:** A1, A2, A4, the thresholds line (2026-09-12); the role versions, the matchday lead card and the week strip yielding on a heavy morning — five or more athletes needing attention, never while the strip is the lead (`weekStripYields`, `lib/dashboardLead.ts`) — 2026-09-13. D6–D8 record where the board and the matrix or the registry part. | | |
| STAFF-SS-01 | C4 | "Send a reminder" on a missing check-in. | Declined 2026-09-12 because nothing in the product sends anything. **Re-opened 2026-09-13** (`docs/platform-decision.md`, S9: "it returns now that a sender is required" — staff notifications are actions a person takes, not a clock). Waits on the S9 send path (service worker, VAPID keys, a subscription table). | With S9 · small once a sender exists |
| ~~STAFF-SS-01~~ | ~~C5~~ | ~~The last-admin guard enforced at the database.~~ **Struck 2026-09-12 (Isabella): already live** — migration 0101, 0102 covers the self-grant; 0109 (§0bd, 2026-09-13) extends it to the account's status. | | |
| ~~STAFF-SS-01~~ | ~~D6~~ | ~~The nutritionist's lead card "keeps the three availability counts with no names".~~ **Built as the board says, 2026-09-13.** First built as "absent" from `docs/access-matrix.md` §4.2, which withheld MET-013 from the nutritionist — a paragraph migration 0074 (decided 2026-09-06: the nutritionist reads the censored injury and availability view, never a diagnosis) had superseded; the matrix's §2, §4.1 and §4.2 are corrected from the live policies in the same commit. The counts draw; the named lists and the Available tile do not (the board's choice, not a permission). | | |
| STAFF-SS-01 | D7 | The nutritionist's attention card "keeps body-mass rows and drops load"; the S&C's is "load and weigh-ins only". | No threshold raises a flag about body mass (`metric_definitions` seeds `wellness.body_mass_kg` ineligible) or in the nutrition domain, so a weigh-in never reaches either card; the nutritionist's reads "No open flags right now" until one exists. Built 2026-09-13 on the flag domains that exist (S&C: GPS, session RPE, gym, testing; nutritionist: nutrition). | **Decide whether body mass becomes a flaggable metric** (a threshold on the day-to-day change, say) — a registry entry and a threshold row, then both cards fill by themselves · medium |
| STAFF-SS-01 | D8 | The lead card has no Named ring, no split bar, no week-load bar: three counts and two named lists. | Built so 2026-09-13. MET-014 (Named) is no longer drawn anywhere — Full + Doubtful is the named count and the three numbers say it; MET-015 (Week load so far), the flags affecting selection and the sessions left are kept as the card's tail line rather than dropped, because retiring a registry surface is a decision, not a layout step. | **Retire MET-014's registry entry** (its only surface was the ring) and decide whether the tail line stays or MET-015 moves to Analytics · small |
| ~~STAFF-SS-01~~ | ~~D4~~ | ~~Only the medic sees an unavailability *reason*; non-clinical reasons (Academic) show without the "Medical" label.~~ **Read and built 2026-09-13 with the lead card.** The reason CATEGORY is readable by every staff role at the database (access-matrix §4.1: "status, restrictions, and reason category"), so withholding it from every role but the medic on the dashboard is a display rule; the clinical reason (the diagnosis) is medic-only under RLS (`clinical_medical_only`, 0012) and the page calls that read for the medic alone. A non-clinical reason renders to the medic without the Medical eyebrow. | | |
| ~~STAFF-SS-02-05~~ | ~~C1 / B1~~ | ~~Every profile opens with one emphasised status header.~~ **Built 2026-09-13** (`lib/profileHeader.ts`, `.pp-hero`): the wash family for the board's tint (B1), the sub line "Hooker · #2 · Forwards, Rehab", the restriction line with the coach-visible note and the expected return, the owner line "Set by medical staff · Ruth Callaghan · Wed 5 Aug", the bio row (the coach's without Weight), the plan line and its link as the card's last row; the separate plan bar is gone. Built differently: Hand stays in the bio row and Group lives in the sub line only (the board's Group cell would repeat it); the wellness dial stays on the name row (the board did not draw it and did not remove it). | | |
| ~~STAFF-SS-02-05~~ | ~~C2 / B4~~ | ~~The squad list on a phone as 60px rows.~~ **Built** — one markup, a phone grid below 768px with explicit table roles · | | |
| STAFF-SS-02-05 | C3 / B2 | The phone profile gets a sticky 52px jump bar ("Flags · panel 1 of 10") and an "All panels" sheet listing every panel with a value each. | A new component; SS-01's sheet is the shape; the panel order is C4's. | After C4 · medium |
| STAFF-SS-02-05 | C4 / B3 | Panels ordered by role from one library; a panel a role cannot see is absent — no heading, lock or count. | **The inventory is done** (2026-09-13, the record): the per-role absences are the Availability panel (AVAILABILITY_EDIT), Body weight (BODY_MASS_VIEW), SAR (sport scientist) and the medic's clinical block — all already absent, not locked; every other panel is common, differing in which controls are live or blocked. What remains is the ORDER per role, which the board's frames give only for the sport scientist and "differences" — the other four orders are a decision. **⚠ permissions** | **Decide the four orders** (recommend: the sport scientist's order for everyone, Body weight raised above Flags for the S&C and the nutritionist), then build with C3 · medium |
| ~~STAFF-SS-02-05~~ | ~~C5~~ | ~~Read-only panels end with an owner line.~~ **Built** on the Injury card (injury-linked availability, `set_by`) and the Nutrition plan panel (the rule's author) via `ReadOnlyOwner`. **Not yet:** the programme block — `programme_assignments` carries no author; add `assigned_by` when the S5 series lands (⚠ migration, small) · | | |
| ~~STAFF-SS-02-05~~ | ~~C7~~ | ~~Restriction text never names a protocol.~~ **Built** with PATTERN-S3 D1 (the read-side filter and the seed) · | | |
| ~~STAFF-SS-02-05~~ | ~~C9~~ | ~~Body mass hidden from the coach.~~ **Built** as a flat rule, not a setting (decided 2026-09-12: the coach does not see body mass at all — the section as well as the buttons): `BODY_MASS_VIEW` in access.ts; the profile card, the nutrition page's card and the export columns. **Remaining halves, recommend when the club asks:** (1) `wellness_entries.body_mass_kg` is still readable by the coach at the database (the athlete's own check-in field) — an RLS column gate would need a view or a split table (⚠ migration); (2) the athlete nutrition page still shows the coach the per-kg rates beside the daily totals ("30.5 kcal/kg" and "3,200 kcal"), from which a mass is one division away — if the rule is meant strictly, hide the per-kg rates for the coach (small) · | | |
| PATTERN-S3 | C1 | The athlete is told once about a status change: an emphasised card on Today that stays until the status screen has been opened. | Needs a per-athlete read flag against the availability row. **⚠ migration** | With C2; the flag first · medium |
| PATTERN-S3 | C2 | An athlete status screen answering three questions (can I train today / what can I do / when am I back), the restriction list as rows, the stage ladder, the academy slot. | A new athlete route; nothing like it exists (`/me` shows the line). | Own brief · large |
| PATTERN-S3 | C3 | Stages numbered with state words; advancing moves one stage and asks for a rewritten restriction line and a criteria-reviewed confirmation; any other stage needs a reason. | The protocol is a bare counter in a string today, not data. **⚠ migration** | Schema for stages first · large |
| ~~PATTERN-S3~~ | ~~C5~~ | ~~The absence form.~~ **Built** on `SetAvailabilityFormCoach` (reason chips first, Modified / Unavailable, Available withheld, the well, "Mark available again" to end one). Permissions unchanged — the database's (coach + sport scientist non-injury, 0068; the medic unconditionally). **Not built, with a recommendation:** the board's From / Until dates — the read model has no scheduled interval (`effective_to` null = open), so an Until date would hide the absence from every current read; recommend a `scheduled_until` column read by `fetchCurrentAvailability` when the S3 C7 history lands (⚠ migration); the "Already recorded this season" list is C7's · | | |
| PATTERN-S3 | C6 | Rehab programme proposals with three states (Proposed / Approved / Returned) and a required return reason, in one list the S&C and the medic both see. | Only "proposed" exists. **⚠ migration** | Schema first · large |
| ~~PATTERN-S3~~ | ~~C7~~ | ~~Availability history.~~ **Built** at `/squad/[athleteId]/availability` with a CSV — no migration: the table is already the ledger (close-then-insert, one open per athlete), "what changed" is computed against the row before (`lib/availabilityHistory.ts`). Injury events on the board's rows are the injury record's (with C8) · | | |
| PATTERN-S3 | C8 | Body site and side are not coach-visible: a coach reads the status word, the restriction line and the expected return, nothing else. | Rendered to the coach today; the board puts it behind a club setting defaulting to off. **⚠ permissions ⚠ migration** | Decide the default; the render rule is small, RLS on the column is the real change · medium |
| PATTERN-S3 | C9 | The injury form split by permission (clinical fields in their own column, save works without them) and a pitch-side four-field form at 44px. | A restructure of `NewInjuryForm` / `InjuryMedicalForm`. | **Build** after C8 decides what the coach's form carries · medium |
| ~~PATTERN-S4~~ | ~~C2 / B1 / B3~~ | ~~Clicking an empty grid slot opens a 320px popover … and "Add session" writes it.~~ **Fell with D1** (declined 2026-09-12: publish stays). The grid's own draft card (a held draft, published from the banner) is the shape that stands. | | |
| ~~PATTERN-S4~~ | ~~C3~~ | ~~A drag writes the move immediately; typed edits are held until Save changes.~~ **Fell with D1**: nothing writes until Publish. | | |
| PATTERN-S4 | C4 | A session in the past, or already rated, opens read-only with the reason. | Needs the rule for editing a rated session (does the rating follow, detach or revise?). | Decide the rule first · small after |
| ~~PATTERN-S4~~ | ~~C5~~ | ~~Expected attendees are counted as distinct athletes across the selected groups, with the denominator.~~ **Built 2026-09-13** (`lib/scheduleExpected.ts`, `fetchSquadSize`): "Publishes to Backs + Forwards · 18 of 30 athletes are expected" on the panel's preview footer — the workspace already resolved a session's athletes as one Set across its groups, so the number was distinct; it needed saying with its denominator. **Left open, one decision:** whether an athlete marked unavailable is still expected — today they are (the count is membership). Recommend: stays expected and shows as absent; the RPE expectation follows the session, not the status · — | | |
| ~~PATTERN-S4~~ | ~~C6 / B4~~ | ~~The phone: day-first with the dashboard's week strip and 44px rows — no grid at 375.~~ **Built 2026-09-13** (`SchedulePhoneDay`, `lib/schedulePhoneDay.ts`): the strip at 74px a tile (day, date, MD, minutes; today selected), the day heading with its 44px + (the new-session page — forms stay pages), 44px rows linking to each session and fixture, "Nothing on Thursday.", the Next line; the grid, its panels and Read/Edit not drawn below 768px. B4: the shell's own surface, hairline and accent for the board's bar tokens. The week nav, banner and template toolbar stay above it (S6 C5's "Week templates disabled offline" is a separate row). | | |
| PATTERN-S4 | C7 | Applying a template states where each offset lands and what happens to what is already there. | `/schedule/planner/apply` restructure; merge-or-replace is undecided (Q5). | Decide merge/replace, then build · medium |
| ~~PATTERN-S4~~ | ~~C8~~ | ~~A group with no session reads a dash and "5 athletes · nothing scheduled", not 0m.~~ **Built 2026-09-13.** The panel already listed every group with members; the words were the gap. | | |
| ~~PATTERN-S4~~ | ~~C9 / C10~~ | ~~The destructive confirmation answers three timeframes; every count states its denominator ("6 sessions live in the athlete app").~~ **Fell with D1** for the timeframes (a removal is held until Publish, so "now / this week / later" has no meaning). The denominator half is built where it applies: "18 of 30 athletes are expected" (C5), "5 athletes · nothing scheduled" (C8). | | |
| ~~PATTERN-S5~~ | ~~C1~~ | ~~A logged set keeps the prescription it was logged against — reps, load and step snapshotted at logging, so editing a block never rewrites what a past set is compared to.~~ | **Built 2026-09-13** (migration 0111 on scratch): `gym_set_logs.prescribed_reps / prescribed_load_kg / prescribed_step_kg`, written by the logger with the prescription as resolved for that athlete at logging; the _current view carries them; a correction copies the original's and ignores its payload's. Read from the row by the correction's reference line, My data's session detail ("Prescribed" column, "Not recorded" for pre-0111), and both CSV exports. Proven on scratch: the programme moved 12 → 20, the logged set still read 12. | Done |
| PATTERN-S5 | C2 / C5 / C6 | Every prescription write states its effective date ("Save from Sat 12 Sept"; "38 sets already logged keep the prescription they were logged against"); the mid-block confirmation screen; the primary carries the number or date it commits. | With C1. | After C1 · medium |
| PATTERN-S5 | C3 / C10 | The per-athlete adjustment screen: one card with controls, parent rows in `--muted`, a required note, remove-override as a tertiary; the phone version stacked. | The override note is not required server side (Q5). **⚠ migration** (a check) | Build with the check · medium |
| ~~PATTERN-S5~~ | ~~C4~~ | ~~An assignment's headline is the distinct athlete count, with its arithmetic.~~ **Built 2026-09-13** (`lib/assignmentCount.ts`): "15 of 30 athletes" over "15 in Forwards + 1 named − 1 counted twice" on the programme's Assigned card; the same squad denominator as S4 C5. | | |
| ~~PATTERN-S5~~ | ~~C7~~ | ~~A nutrition plan authored as rules per kilogram with the worked example, "not set" as a dashed frame, the squad mean with its n, "Coach-set guidance, not a clinical prescription".~~ | Authored side: already on the running screen (2026-09-13). Resolved side, **Isabella's ruling 2026-09-13: neither spec — show the club default, labelled as the club default**, so the number is present and its provenance is on the face of it. **Built the same day**: a provenance line under every resolved target (the athlete's own card, the staff athlete nutrition page, the profile card) — "The club default target, the same for everyone on it." / "Your group's target." / "Set for you.", with "Not scaled to your weight — no weigh-in on record." when there is none; the board (`PATTERN-S5-final/notes.md`) and `06-athlete-nutrition.md` §States updated to the code plus the label. | Done |
| PATTERN-S5 | C8 | "each side" beside a unilateral exercise's name. | The exercise record has no unilateral flag. **⚠ migration** | Schema first · small |
| PATTERN-S5 | C9 | Weeks run down, sessions across; a week not reached reads "not reached", never 0 of 24. | The block view does not exist in this shape. | With C2 · medium |
| ~~PATTERN-S6~~ | ~~C1~~ | ~~The "Waiting to send" queue screen (oldest first; what, its denominator, the time saved; "4 entries · 5 writes"; no per-item retry; "Nothing is waiting" with the last send time) and "See what is waiting" under Today's count.~~ | **Built 2026-09-13**: `/today/waiting` (`docs/athlete/screens/19-waiting-to-send.md`), rows on the app's own `.hist-row`, header "4 entries · 5 writes", the empty with the last send (`fydr-outbox-last-sent`, written by the flusher); "See what is waiting" under Today's count; Today now counts entries the way the queue does (a gym session is one entry). A gym set is queued with its session's name, total sets and date so the row can be named offline. | Done |
| PATTERN-S6 | C2 | Today's gym row reads "6 of 12 sets · 2 waiting to send". | Today has no gym row to put it on: the to-do list carries wellness, RPE and the nutrition check-in, and an unfinished gym session is "a real, separate query this pass doesn't" make (the page's own note). The waiting count exists (`queuedGymSets`, client-side). **⚠ decision:** does Today list today's gym session (open or not) as a row? | Decide the row, then small · small after |
| PATTERN-S6 | C3 | Session expiry mid-form: answers held on the device, "Sign in and send", back to the same sheet with every answer set. | Nothing preserves a part-filled form across re-authentication; the JWT is 30 min with refresh. **⚠ decision** (where the hold lives — localStorage per form, as the gym drafts do). | Decide, then build · medium–large |
| PATTERN-S6 | C4 | Staff typed writes queue ("Saved on this phone · sends when you have signal. Nobody else can see this until you have signal."), the staff queue time-critical first with "When it sends", raising a flag offline. | Closed 2026-09-12 as post-pilot; **in scope from 2026-09-13** (`docs/platform-decision.md`, `docs/decisions/scope.md`: both apps must have a robust offline outbox). **Not built yet, by instruction**: the clinical-conflict rule inside it — a coach's held Unavailable arriving after a medic has set a different value — is undecided (`architecture/to-do.md` §4). Built on the S11 shared outbox (IndexedDB, service worker), not the per-screen localStorage queues; the S11 inventory says what exists. **⚠ decision** (the conflict rule) | Decide the clinical-conflict rule, then build on the S11 foundation · large |
| PATTERN-S6 | C5 | A drag on the grid disabled offline with the footer reason; + Session / + Fixture queue; Week templates disabled. | With C4 — **in scope from 2026-09-13**, not built yet. | With C4 · medium |
| ~~PATTERN-S6~~ | ~~C6~~ | ~~A failed live write on the schedule undoes itself: the block returns, a dashed ghost reads "Did not save", the notice names both times and the athletes.~~ **Built 2026-09-13** (`lib/scheduleFailedWrite.ts`): in the held model the failed write is a publish the server refused for one session — the change stays held, the block is drawn where the athletes still have it, the attempted position is a "Did not save" ghost (the removal ghost's dashed treatment, the words say which — D2 settled as recommended), the banner reads "Gym A did not save — … The athletes still have Mon 7 Sept · 07:00; you tried 07:15 · 30 athletes affected." with Try again. A dropped connection keeps §0al's one line and ghosts nothing. | | |
| ~~PATTERN-S6~~ | ~~C7~~ | ~~Permission denied: "This is not available to you…", the signed-in identity and role, a reference code, Back to dashboard.~~ | The copy built 2026-09-13 (`/denied`). **The log built the same day on Isabella's ruling** (reversing the deferral: under a v1 that gets sold, a denial a user cannot describe is a support call that cannot be resolved): migration 0112 `log_access_denial` writes one `access.denied` row to the club's audit log as the person refused (gate, path, roles) and returns "D-<id in base 36>"; every gate calls it before redirecting; the screen shows "Reference D-1TLB. Quote it to your club's administrator — it is in Settings › Audit log."; the audit screen shows and finds the row by the code. | Done |
| ~~PATTERN-S6~~ | ~~C8~~ | ~~Staff empty states to the one grammar (what, why, what would fill it, the most recent on record, "Nothing is missing", one action that widens the window).~~ | **Built 2026-09-13, complete** (`lib/staffEmpty.ts`, `lib/dashboardEmpty.ts`): the athlete, training, squad weekly and testing reports, and the dashboard's three (the day timeline with the nearest session day on record and one action to it; a session card's all-clear over its expected count; the flags panel's all-clear over the scope's size). Review fix on the testing report: an empty scope is told apart from a test never run, and every "Show the whole squad" action clears the shared cookie (§0ak). | Done |
| ~~PATTERN-S6~~ | ~~C9~~ | ~~One `role="status"` and one `role="alert"` region per surface, written on transition only.~~ | Audited 2026-09-13 (the record, "C9 audit"): 118 live-region attributes in 90 files, 108 conditional; at runtime 34 of 37 surfaces carry no region on load — the rule already holds in effect because a conditional region mounts once per transition. The one violation (OutboxFlusher's alert per conflict) fixed the same day. **Isabella's ruling 2026-09-13: not now — deferred on its merits, not on scope.** Revisit only if screen-reader testing shows a mounted alert being missed, then per shell. | Deferred on merits · — |
| PATTERN-S6 | C10 | Retry schedule, queue limits, and a "too late to send" state. | No cap, no expiry, no backoff; a check-in past its three-week write window is refused by policy for ever and counted as waiting for ever. **⚠ decision** | Recommend: a policy refusal (42501 / WITH CHECK) marks the item "could not be sent — the week has closed", shown once like a conflict, with Discard · small–medium |
| PATTERN-S6 | D1 | `--pill-offline` / `--on-offline`. | A new token; **in scope with C4 from 2026-09-13**. If C4 is built the offline tone maps to an existing wash first; a new token only if none reads right. | With C4 · — |
| ~~PATTERN-S6~~ | ~~D2~~ | ~~`--line-dashed-failed`.~~ **Settled with C6 (2026-09-13):** the existing dashed ghost and the words "Did not save"; no new token. | | |
| ~~PATTERN-S6~~ | ~~D3~~ | ~~`QueueRow` component.~~ | Built with C1 2026-09-13 as the app's own `.hist-row` (date / detail / value) inside `components/WaitingQueue` — no new component shape; the staff variant's consequence line waits with C4. | Done |
| ~~PATTERN-S7~~ | ~~C1~~ | ~~The report shell for all six reports: title, the catalogue's definition sentence, period and group, the figure with denominator and exclusions, one chart, table, exports top right, print.~~ | **Built 2026-09-13, complete.** The header was already the shared template; the definition sentence (reconciled to Isabella's catalogue — training and match carry none, raised on the sheet) and the one emphasised figure card (`components/ReportFigure`) on all six reports, on screen and in the PDFs; `TableShell` (`components/TableShell`) around every report's main table — title, the sort order in words where the table is a ranking, the count with its denominator, a body that scrolls sideways on a phone. One chart at most per report is already the case. | Done |
| PATTERN-S7 | catalogue · training | The catalogue's confirmed training sentence — "Session load is RPE multiplied by session minutes, summed over the period…" — on `/reports/training`. | **Raised 2026-09-13**: the built `/reports/training` is the per-session GPS board (MET-017/018/019/021, the MET-026 dials; premium). It shows no RPE × minutes load and no load by week, so the sentence would define numbers the page does not carry. The RPE-load report the catalogue describes exists as an Analytics board, not as a report. The GPS board carries no definition card meanwhile; the builder's drafted GPS sentence is withdrawn. **⚠ decision** | **Recommend**: keep the GPS board as the training report with its own (GPS) sentence for Isabella to write, and add the catalogue's RPE-load report as a seventh report built on the Analytics load query — or rename the GPS board "Session GPS" and let the catalogue's training report take the name · medium either way |
| PATTERN-S7 | catalogue · match (Q4) | The match sentence, on hold pending "does the app record who played and how many minutes?" | **Answered from the code 2026-09-13: no.** The app records attendance at a session (`session_attendance`: full / modified / absent / excused with a non-clinical reason, a coach's write from the timetable) and the week's published team allocation (`team_allocations`, the selection list). No column anywhere holds minutes played, starters, bench or substitutions; `fixtures.result` is free text. GPS `duration_s` per athlete is a proxy for time on the pitch only where a unit was worn (premium). **Under v1 this is a gap to close, not a fallback to word** (`docs/design-programme-corrections-2026-09-13.md`: "a product sold to rugby clubs that cannot say who played is not complete"). Needs: a place to record who played and minutes (⚠ migration — per athlete per fixture: started / came on / minutes; a coach's write on the fixture), then the match report reads it. | Isabella writes the sentence and rules on the capture (a coach's post-match sheet on the fixture) · medium |
| PATTERN-S7 | catalogue · testing (Q3) | The confirmed testing sentence's last clause: "an athlete who has never been assigned a test does not appear for it." | **Answered from the code 2026-09-13: tests are not assigned.** `test_definitions` is what the club measures, org-wide; `test_results` is one athlete's result on one date; there is no assignment entity and no group or athlete scoping on a definition, so "never assigned" cannot be told apart from "no result". On the built page the by-athlete grid shows every athlete in scope for every test (a missing result in words) and the by-test ranking lists only athletes with a result and says how many have none. The sentence is on the page as confirmed; the clause has no fact behind it. | Either reword the clause ("an athlete with no result for a test is shown as such, never as zero") or add test assignment (a migration: tests per group or athlete, and the report scoped by it) · small / medium |
| PATTERN-S7 | catalogue · squad weekly window (Q2) | The confirmed sentence "The week Monday to Sunday, club local time" against the built window. | **Found 2026-09-13**: the built squad weekly window was a trailing seven days ending today (`from = today − 6`), so the sentence was true only on a Sunday. **Fixed in the commit after the reconciliation**: the window is the calendar week Monday to Sunday in the club's timezone, the current week running Monday to today, the pager moving a week at a time; `?to=` links still resolve to their week. The shared Monday boundary itself exists — `mondayOf` is the one rule for the schedule grid, the dashboard strip, the timetable, the nutrition check-in (`week_start`, checked at the database) and My data; RPE has no week of its own (a rating is per session per day). The dashboard's strip draws Monday to Saturday. | Done (the window); the six-day strip is the dashboard board's own choice · — |
| PATTERN-S7 | catalogue · load (Q1) | "Is training load stored or derived, and where does 'load 2,910' on a week template come from?" | **Answered from the code 2026-09-13: derived, twice.** Actual session load is MET-007, RPE × duration from the athlete's rating (`training_entries`), computed in application code and summed into acute/chronic. A week template's "load 2,910" is planned load: Σ `duration_min × planned_rpe` over the template's sessions, computed on render (`weekTotalLoad`, `lib/queries/weekTemplates.ts`), with a day holding an unscored session flagged. `sessions.planned_load` exists as a column (0003) and is written and read by nothing but the 0104 audit trigger — a dead column. | Nothing to build; drop or populate `planned_load` when the schedule board is next touched · — |
| ~~PATTERN-S7~~ | ~~C2~~ | ~~Every figure with its denominator and an exclusions sentence ("Nobody is excluded"); missing values as words; worst first.~~ **Built across all six reports, 2026-09-13** (`lib/reportFigures.ts`): compliance, injuries, squad weekly, training, testing and the athlete report ("11% · 4 of 35 submitted · last 28 days"; "Building baseline" on a load tile under its own 21-day floor; "No result", "No comparison", "None assigned", "Nothing submitted"). No dash stands in for a value on any of the six. | | |
| PATTERN-S7 | C3 | The export dialog names the file before it is written; the audit row gains the row count; a medical export carries "Contains medical information. Handle under the club's data policy." | Exports are logged today (`report.<type>.export`) but without the row count; no dialog. Needs `--w-dialog` (D3). | Build after C1 · medium |
| PATTERN-S7 | C4 | One renderer for print and PDF. | Seven `@react-pdf` handlers and a base.css `@media print` block — two renderers. **⚠ decision** (which survives; recommend the PDF, with Print opening it) | Decide, then build · medium |
| ~~PATTERN-S7~~ | ~~C5~~ | ~~The group filter reaches the export routes as the page reads it (the cookie), so a bare export URL is scoped the way the screen was.~~ **Already true — confirmed and pinned 2026-09-13.** All five report export routes go through `resolveGroupFilter` (URL, then the sticky cookie); the exports hub posts the page's resolved scope. The row's "reads `?groups=` only" was stale. Guarded in `test-pattern-s7.ts`. | | |
| PATTERN-S7 | C6 | Analytics as four panels: definition line with n, zero-based bars with the axis in words, day/week grain summed or meaned by measure, the tap-persistent readout, "Not submitted" stubs, the club threshold as a named dated zone, suppression with one action, no export. | The built analytics is four boards by day with no weekly grain; the readout, the zone and the grain rules are new. | Its own brief · large |
| ~~PATTERN-S7~~ | ~~C7~~ | ~~The compliance report says its cutoff in words.~~ **Built 2026-09-13**: the two sentences on the Summary card — the RPE cutoff from `rpeClosesAt`'s rule, and that wellness has none. | | |
| ~~PATTERN-S7~~ | ~~C8~~ | ~~Suppression below five athletes with data on every report and panel — one shared rule.~~ **Built 2026-09-13** (`lib/smallSample.ts`): the positional band's and the profile shading's floors now read it; the training report's heat ramp, the testing report's median and quartiles, the compliance squad mean and the analytics builder's population mean ask it, each with the one notice ("Shading is off — 3 athletes have data, fewer than 5. The individual numbers are unchanged."). An ACWR's 21-of-28 and a rolling band's 10 observations are one athlete's floors and stay their own. | | |
| ~~PATTERN-S7~~ | ~~C9~~ | ~~The period control explains why the narrow choice is usually wrong; navigation both ways.~~ **Built 2026-09-13 on the compliance report** (`lib/periodNav.ts`) — the one report with a day anchor: "‹ Previous 7 days" / "Next 7 days ›" move `?to=` by the window's length (never past today; a season, a year and "all" are not walked), and under the control "Over 7 days one missed morning moves an athlete's rate by 14 points — read four weeks for the habit, 7 days for this week." The other reports resolve from today and have no anchor to walk — with C1's shell if wanted. | | |
| ~~PATTERN-S7~~ | ~~C10 / C11~~ | ~~The role note on the report; exports first under the title on a phone; the training report's phone reading is the PDF.~~ | **Built 2026-09-13.** C10: `lib/reportRoleNote.ts` from `access.ts`'s own sets — one line under the definition on injuries (medic / everyone else), compliance (the nutritionist) and the athlete report (three readings); null where a report reads the same for every role. C11: below 768px the shared header puts the eyebrow-and-exports row directly under the title by CSS order on `.rhead` (desktop untouched); the training board adds a phone-only line, "This board is laid out for a desktop. On a phone, read it as the PDF — Export PDF is above." | Done |
| ~~PATTERN-S7~~ | ~~D1~~ | ~~Readiness plotted "out of 5" on the wellness panel.~~ **Declined** — the same question as ATH-ADULT-12 D5, declined 2026-09-12 (MET-001 is 0–100 in both apps). | | |
| PATTERN-S7 | D2 | `/analytics/build` — keep or remove. | Linked from `/analytics`, sport scientist only, "read off nothing on this board". **⚠ decision** | Recommend keep until C6 replaces the boards · — |
| PATTERN-S7 | D3 | `--print-paper: #ffffff`, `--print-ink: #12161c` (the only theme-ignoring tokens), `--w-dialog: 640px`. | Approved by the board; a §0.01 token decision to date when C3/C4 are built. | With C3/C4 · — |
| PATTERN-S11 | inventory | Installability and offline — the board from `docs/platform-decision.md`. | **Step 1 inventory done 2026-09-13** (the record, "PATTERN-S11"): the manifest holds name, standalone, two colours and two icons (an `any` SVG, a 180px PNG) — no 192/512 PNG, no maskable, no splash, no iOS install teaching; **no service worker at all** (no app shell, no background sync, no push possible); four athlete queues in `localStorage` (`fydr-outbox-wellness/-training/-nutrition/-gym-set`, `lib/outbox.ts`), flushed only by `OutboxFlusher` on `/today` and on `online`, every flush, no backoff, no cap; staff writes not queued (the schedule's held week is `sessionStorage`); visible queue state and last-send time exist (S6 C1). Not designed. | Board next · the inventory names three open questions |
| PATTERN-S11 | "send now" | The platform decision asks for a manual "send now"; the S6 board deliberately has none ("the send is not the athlete's job"; the queue screen reads "there is nothing to press"). | Two rulings contradict. The platform decision is the later one and asks for it explicitly. **⚠ decision** | Recommend: a "Send now" on the queue screen only, never per item, the S6 words otherwise kept · small once the S11 foundation exists |
| PATTERN-S12 | inventory | Premium — the board from `docs/platform-decision.md`. | **Step 1 inventory done 2026-09-13** (the record, "PATTERN-S12"): the tier is one column (`core`/`performance`, no UI flips it, billing out of scope); premium is Analytics (destination), the analytics bar chart, the training report (both boards) and its exports, the athlete report's GPS panel, GPS vendor import and its exports, GPS leaderboards — every gate `isPremium` in the app; **at the database no policy reads the tier**, only `compute_leaderboard` (0094) refuses a gps board on `core` (and the dormant healthkit consent rule); `gps_records` reads are deliberately not tier-gated (SAR), so a Basic staff token can read GPS rows via PostgREST. Downgrade — **ruled the same day** (`docs/decisions/premium-downgrade.md`: kept and hidden under D-20, restored on upgrade, under the normal retention clock): the code already keeps and hides GPS data, refuses gps leaderboards and removes analytics; the one-place sentence, the retention run's coverage of `gps_records`, and the audit row on a tier flip are the gaps. The training-report card on the hub is a destination treated as a region under D-20. Not designed. | Board next · five open questions in the record |
| PATTERN-S8 | C1 | The setup checklist a new club lands on: add athletes, make groups, set thresholds, invite staff, assign roles — each with a count and a done / outstanding state; a list of remaining defaults, not a gate. | A new screen and a "still default" rule for thresholds the rows do not carry (D8). | Decide D8, then build · medium |
| ~~PATTERN-S8~~ | ~~C2~~ | ~~The settings hub in four groups on one 900px screen; cards with counts on a phone, the long lists one level down; Log out a real button.~~ | **Built 2026-09-13**: `/settings` is four cards — Club, People, Data, You — of destination rows with counts ("4 groups", "5 active", "37 active accounts", "0 open", "0 files", "1,988 in 90 days", "Two-factor required"); measured 900px tall at 1440 × 900 as the sport scientist (was 3,772), one column of stacked cards at 375 with 70–92px rows. A row a role cannot open states its reason from `access.ts`'s own sets instead of linking. The Plan card and Integrations moved to `/settings/club` (with the club details form), profile, avatar, password and two-factor to `/settings/profile` (the route-map's name), `#password` anchoring the two-factor notice. Log out stays the A1 button, at the foot of the You card. `src/lib/settingsHub.ts`, guard `test:settings-hub`. | Done |
| ~~PATTERN-S8~~ | ~~C3~~ | ~~The users list with search by name or email and filters by role and status; no sideways table at 375.~~ | **Built 2026-09-13**: search by name or email (was there) plus one role chip row and one status chip row, all 44px; the count line reads back with its denominator and the filters in words ("5 of 37 accounts · coaches"); an empty result is one sentence with a Clear filters button; `?q=&role=&status=` land the list filtered. The row is `.um-row` — name, role toggles, status — and stacks into a card below 768px: measured at 375 with 37 accounts, no sideways scroll on the page or any card. Athlete accounts stay in the list (invitations and linking live here); the board's staff-only table is not followed, "Athlete" is a chip instead. `src/lib/userFilters.ts`, guard `test:user-filters`. | Done |
| ~~PATTERN-S8~~ | ~~C4~~ | ~~A role change previews what it grants and what it removes before the button.~~ | **Built 2026-09-13**: `src/lib/roleGrants.ts` — 24 capabilities, each one of `access.ts`'s exported sets with a sentence (the guard fails if a set is added without one); `roleChangePreview(held, pending)` diffs the union before and after ("Adding Medic: gains 5 things and removes nothing; 14 things unchanged."), lists Grants and Removes, and states the rules it trips — D-25 nutritionist-plus-injury-role, nutritionist alone, athlete only, no role, removing Medic ("Clinical records they wrote stay") — with the effect line (next page load; audit log with your name). On `/settings/users/[id]` the chips move a pending set; the preview sits in the Roles card with Save roles and Cancel; nothing writes before Save. "What this user can see" now reads the same catalogue (the old four-role summary is gone). The list's inline toggles are gone: rows state roles as facts with a Change roles link to `#roles`. Refused chips are BlockedButtons (reason on tap), not `title`s. Run-verified on scratch: S&C added to Peter Ackland then removed, two `user_roles.changed` audit rows, roles back to coach. Guard `test:role-grants`. | Done |
| PATTERN-S8 | C5 | Groups: a warning when a group in use by a session or programme is renamed or archived, with what happens. | A read of sessions and assignments by group; today nothing warns (Step 1 Q4). | **Build** · small |
| PATTERN-S8 | C6 | Thresholds in plain English with the owner and date the dashboard quotes; a 28-day preview of how many athletes a rule would have flagged, writing nothing. | The preview is a compute over 28 days of entries per metric — a read, not the evaluator. | **Build** · medium |
| PATTERN-S8 | C7 | The audit log's filters as a sheet on a phone (the button reads back the count it will show), a person filter, the header carrying the active filter count. | The person filter (actor) exists; the sheet is SS-01's More sheet pattern; the count needs a count query per filter state. | **Build** · medium |
| PATTERN-S8 | C8 | The export file reads its own filters back; the audit row carries the row count. | With PATTERN-S7 C3 (the export dialog). | With S7 C3 · small |
| PATTERN-S8 | C9 | Retention states its consequence before the button: rows, athletes, which are current. | The preview route exists; the sentence and its placement in the same card as the button are the change; whether the preview is logged is D4. | **Build** · small |
| PATTERN-S8 | C10 | Subject access: the staff side and the athlete side on one pattern, reusing the athlete report where it fits. | Both exist (`/settings/subject-access`, `/me/export`); the pattern is the board's. | After S7 C1's shell · medium |
| PATTERN-S8 | C11 | An import holds what it cannot match, named on screen; matching once teaches the vendor's spelling. | The batch's error list exists; a "held" state and a remembered mapping are new. **⚠ migration** (the mapping) | Schema first · medium |
| PATTERN-S8 | C12 | Tables become cards below 900px: audit, retention, subject access. | The three overflow a 375 viewport today (433px, 491px). | **Build** · medium |
| PATTERN-S8 | C13 | The athlete's notifications screen with "Mute everything" as the one switch at the top, no confirmation, and the sentence that nothing stops being recorded. | The form has "Mute everything else" as a button below the list. | **Build** · small |
| ~~PATTERN-S8~~ | ~~D1~~ | ~~Apple Health is listed as a Premium feature.~~ | **Struck 2026-09-13 — Apple Health is removed from the product** (`docs/platform-decision.md`): there is no native iOS app and none is planned, and it was the only capability that required one. Not deferred, not parked. The staff Settings row, the Premium-list line, `lib/queries/healthkit.ts` and the consent toggle are deleted; the `device_metrics` table and the `healthkit_sync` consent purpose stay in the database, dormant. | Done (removed) |
| PATTERN-S8 | D2 | Catapult drawn as "Connected · Sync now". | It is a CSV file drop with no credential; the hub's control now reads "Import files" (A7). | Keep the truthful words; the board's card is wrong on this · — |
| PATTERN-S8 | D3 | Session and schedule changes written to the audit log. | §0al's "next audit-trigger batch"; the log says so meanwhile (A5). | With that batch · — |
| PATTERN-S8 | D4 | Whether a retention preview is itself logged, and its row list retained. | Not observed; decide with C9. | Recommend: log the preview as its own action, retain nothing · — |
| PATTERN-S8 | D5 | Revoking an invitation kills the link at once. | Read 2026-09-13 (the record): there is no revoke control; the invite is a single-use Supabase `invite` token nothing invalidates early; deactivating the account does not stop the link signing the person in, but the access-token hook (0010) then issues a token with no org and no roles. **Isabella, 2026-09-13: do not touch — a security decision on auth, hers to rule on.** | Isabella rules · — |
| PATTERN-S8 | D6 | "Export links expire after seven days". | Exports are direct downloads here (no stored file, no signed URL); the sentence has nothing to describe. | Do not add it · — |
| PATTERN-S8 | D7 | Role change timing stated as "next page load". | Answered: 0010 bumps `claims_version` on a role change and 0109 on a status change, so the change lands on the NEXT REQUEST. | Say "next request" · — |
| PATTERN-S8 | D8 | The checklist's "thresholds still default" step. | The rows carry a value and a creator, not an is-default marker; "created by the club defaults" (no creator) is the nearest fact. | Use "no creator" as default; decide if that is enough · — |
| ~~PATTERN-S8~~ | ~~D9~~ | ~~A saved report reverts to the whole squad when its group is archived, and says so.~~ | **Built 2026-09-13 (Isabella's ruling, with the §0ak cookie fix)**: `resolveGroupFilter` checks every requested id, URL or cookie, against the live groups once per request and drops the rest, so every scope on every screen is a live group's; the staff shell rewrites the cookie without the dropped ids and says so once ("Filter updated: Leadership was archived. Showing Whole squad."); the scope label never reads "1 unknown group". No saved-report entity exists — the cookie and links were the "saved report". | Done |

## (d) Needs a new token

| Flow | ID | What it changes | Why it isn't built | Recommendation · cost |
|---|---|---|---|---|
| ~~ATH-ADULT-09~~ | ~~B1~~ | ~~The logger's numbers at 48px.~~ **Built** with C1 on the existing `--fs-48` step (no `--t-num-hero` token: the value exists; the summary's totals read the same step) · | | |
| ~~ATH-ADULT-09~~ | ~~B2~~ | ~~`--hit-lg` 56px and `--hit-md` 52px.~~ **Built** — approved 2026-09-12 with C1; both in `tokens.css` dated with Isabella's name, read by `.gl-primary` and `.gl-step` only; 06-design-system.md §11.1 · | | |
| ATH-ADULT-09 | B3 | The set chip fills over 300ms on `--ease-ring`. | A motion token; declined in 03 and protected ("no motion tokens"). | **Stays declined** · none |
| ~~ATH-ADULT-09~~ | ~~C1~~ | ~~The set-by-set rebuild.~~ **Built** (commit after `1bf38b9`): one exercise at a time, the two numbers at 48px between the steppers, the one primary labelled with the write, the chips as state display with no disabled control, the deviation as information, what is next beneath. The closed-session finding is closed with it: not-reached and current chips are not controls, and no logging primary is offered on a closed session. B3 (the chip-fill motion) stays declined — the fill is instant · | | |
| ATH-ADULT-10 | C2 | A confirmation before finishing early ("Keep logging" primary; "Finish early" secondary with "4 sets short"; what is saved and where it goes). | A dialog is a component shape the system does not have (03 declined the discard dialog for the same reason). | If a dialog pattern is approved once, build it here first · medium |
| ~~ATH-ADULT-11~~ | ~~C1~~ | ~~The footer swaps to Save correction / Cancel.~~ **Built** with 09 C1 (the correction opens from a logged chip or the strip's "Correct it"; the two numbers edit it) · | | |
| ATH-ADULT-12 | B1 | Charts at `--chart-h: 84px` with a `--chart-stroke: 2.5px` line — the first plotted-series tokens in the athlete app. | Two new tokens; the readiness SVG is viewBox-scaled and the gym bars a fixed 72px today. | **Add both** when the Sessions chart (C2) is built, one commit, `06-design-system.md` updated with it · small (the tokens) |
| ~~ATH-ADULT-12~~ | ~~B2~~ | ~~Prior values in every chart in `--blue-200`.~~ **Built 2026-09-13 where a bar chart exists** — the gym weeks: prior completed weeks in `--wash-accent-strong`, the latest in the accent, a partial week its own mix. The readiness chart on My data is a line with dots (the board drew bars — 12 C2); its mapping goes with C2. | | |
| ~~ATH-ADULT-12~~ | ~~B4~~ | ~~The empty-state "Show this season" button at `--hit-lg` 56px.~~ **Built with 12 C6 at 44px** (`.empty-period` action, min-height 44px). | | |
| ATH-ADULT-12 | B5 | Corrected markers in `--pill-accent` / a 3px `--border-accent-w` bar. | No such names; the existing neutral pill (§0v) is in use; a 3px row bar is a pattern the system lacks. | Keep the neutral pill; the bar with 13 C3 if a row-marker pattern is approved · small |
| ATH-ADULT-12 | C8 | The period control on the title line as a menu (accent with a chevron; the current option carries the wash and a tick). | `PeriodSelector` is a shared staff `<select>` (ten pages); a custom menu is a new component shape. | Keep the select; if the menu is wanted it is an athlete-only component, own brief · medium |
| ~~STAFF-SS-01~~ | ~~B1~~ | ~~The dashboard's emphasised card in `--blue-100` + `--blue-200`; the 3px `--border-accent-w` bar on dense rows; `--pill-accent` + `--accent` actions.~~ **Done:** the lead card carries the wash family (2026-09-13, with C2); the 3px bar was declined 2026-09-12; the actions are the existing ghost/primary controls. | | |

---

**Not on this sheet, because they were built:** every A item of 04, 06, 08, 09 (A3, A4), 10
(A1), 12 (A1–A4), 13 (A1–A5), the STAFF-SS-01 shell (C1, A5, B1 for the shell), and
STAFF-SS-02-05's A1–A6 with PATTERN-S3's A1–A3, PATTERN-S4's A1–A3 and A5–A8, and
PATTERN-S5's A1 and A7 (2026-09-12) — see the records and the handovers in the commit log.
