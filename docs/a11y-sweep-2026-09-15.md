# Accessibility sweep — step 1, the defect list (15 September 2026)

**Report only. Nothing in `src/` changed for this document.** The prompt
(`docs/queue-pending.md`, "SECOND OF THE TWO CLOSING PASSES") says: produce
the full defect list before changing anything, group by class then by page,
and stop. This is that list, measured at commit `b62b824` on the scratch
project after the design-system conformance pass and the 14 September batch
were committed — the settled product the order note asks for. The baseline of
15 September (`docs/a11y-sweep-baseline-2026-09-15.md`, three roles, stopped
by Isabella) is superseded by this as the list the fix pass works from; its
Class 3 hand review and its answer on the guards are carried forward here,
re-checked against the code as it stands.

**Five of the baseline's rulings have since been given (decision batch 14
September 2026, #8–#12) and are applied here rather than re-asked:**

- **#8 — no tap-target minimum on desktop.** 44px is a touch rule and applies
  at phone widths. So Class 2 counts a control as a defect only where it
  measures under 44px **at 390**; a control short at 1440 alone is listed
  under its own heading as "desktop only — not a defect by the ruling", so
  the numbers can be read either way but only one way counts.
- **#9 — the 45% fade on faded unavailable controls stays**, recorded as
  deliberate (`--o-disabled`). A contrast failure whose only cause is that
  fade is listed under its own heading and not counted.
- **#10 `--lb-row-selected` in dark, #11 the placeholder rule, #12 amber small
  text** — fixed in `42ad4ff`. They should not appear below; where a
  measurement still shows one, that is a finding.

**What "measured" means here, in one paragraph.** Headless Chrome over CDP
(`scripts/a11y-sweep.mjs`), signed in through the app's own `/auth/confirm`
route as one single-role account at a time, every route in the lists below at
1440 and at 390 (mobile emulation), and in each page the theme switched the
way `ThemeToggle` does so light and dark were read from the same render.
Contrast is the computed `color` with the whole opacity chain multiplied in,
composited src-over onto the background walked up the ancestor chain (every
`background-color` with its alpha — a wash over a surface is measured as the
colour it paints), against 4.5:1, or 3:1 where the text is genuinely large
(≥ 24px, or ≥ 18.66px at weight ≥ 700; the size and weight are printed wherever
3:1 is applied). A tap target is the border box of every interactive element —
`aria-disabled` included, natively `disabled` listed separately and not
counted — widened by a wrapping `<label>` for a radio or checkbox and by an
absolutely positioned `::before`/`::after`. The colour-only class is a hand
review of every small painted element without text of its own, plus the
markup that drives it. `scripts/a11y-sweep-report.mjs` folds the
measurements into the entries below and names, for every failing text colour,
every text-role token's ratio on that element's composited ground in both
themes, so a proposed swap is one token for another that passes in both — or
the entry says none does and stops there.


## The numbers

- **Eleven single-role accounts** (checked in `user_roles` on scratch before signing in), plus the anonymous pages: at Ashcombe (Performance) — Peter Ackland, coach; Ruth Callaghan, medic; Owen Hartnell, S&C; Sana Mirza, nutritionist; Jane Pemberton, sport scientist; Dan Okonkwo, athlete (adult); Kai Mercer and Niall Rafferty, athletes (16); James Barnes, athlete (declined data consent). At Marlow Vale (Basic) — the sport scientist account, for the plan-gated surfaces and the denied screen. The staff list is 76 routes, the athlete list 24, the Basic list 17, the anonymous list 6 — every route at 1440 and 390, light and dark. Which screens each role actually reached, redirects included, is the table below.
- **Class 1, contrast: 46 distinct measured failures, 38 counted as defects.** Not counted: two that are the Next.js default 404 page rendered black on the dark ground (Other findings, 1), and six whose only cause is the 45%-class fade on a faded unavailable control (ruling #9 of 14 September). Every counted entry names a token swap that passes in both themes, or says none does.
- **Class 2, tap targets: 51 distinct controls measured under 44px; 25 are defects under ruling #8 (short at 390); 23 are short at 1440 only and not defects by that ruling; 3 are natively disabled and exempt.** Listed in three groups so the ruling can be read the other way if it ever changes.
- **Class 3, colour as the only carrier: 5 defects, all on the staff app, all carried from the baseline and re-verified in the code at this commit; the athlete app, reviewed for the first time, has none.**
- **Other findings: 7**, none of the three classes, none changed.

## Roles and the screens each reached

The route lists are `scripts/a11y-routes.sh`'s (scratchpad) — every staff screen with real ids from the Ashcombe fixture, every athlete screen, the Basic-plan set, the anonymous pages. A redirect is recorded as what landed (the denied screen, the settings hub, the plan page), and the landing page was measured as that role's surface.

- **anonymous** — 6 routes requested; 6 rendered as asked.
- **athlete_adult** — 24 routes requested; 22 rendered as asked; 2 redirected: `/consent/declined` → `/today`, `/consent/guardian` → `/consent/decide`.
- **athlete_declined** — 24 routes requested; 23 rendered as asked; 1 redirected: `/consent/guardian` → `/consent/decide`.
- **athlete_minor_kai** — 24 routes requested; 21 rendered as asked; 3 redirected: `/consent/decide` → `/today`, `/consent/declined` → `/today`, `/consent/guardian` → `/today`.
- **athlete_minor_niall** — 24 routes requested; 21 rendered as asked; 3 redirected: `/consent/decide` → `/today`, `/consent/declined` → `/today`, `/consent/guardian` → `/today`.
- **basic_sport_scientist** — 17 routes requested; 14 rendered as asked; 3 redirected: `/reports/gps` → `/denied?r=D-28PD`, `/analytics` → `/denied?r=D-28PJ`, `/settings/imports` → `/denied?r=D-28PL`.
- **coach** — 76 routes requested; 57 rendered as asked; 19 redirected: `/squad/roster` → `/squad`, `/squad/new` → `/squad`, `/compliance` → `/reports/compliance`, `/nutrition/new` → `/nutrition`, `/programmes/new` → `/programmes`, `/programmes/proposals` → `/denied?r=D-28LT`, `/reports/training` → `/reports/gps`, `/analytics` → `/denied?r=D-28N0`, `/settings/audit` → `/settings`, `/settings/imports` → `/settings`, `/settings/plan` → `/denied?r=D-28N9`, `/settings/retention` → `/settings`, `/settings/setup` → `/settings`, `/settings/subject-access` → `/denied?r=D-28ND`, `/settings/subject-access/[id]/review` → `/denied?r=D-28NG`, `/settings/users` → `/settings`, `/settings/users/bulk-invite` → `/settings`, `/settings/users/[id]` → `/settings`, `/platform/sign-in-probes` → `/denied?r=D-28NL`.
- **medic** — 76 routes requested; 52 rendered as asked; 24 redirected: `/squad/roster` → `/squad`, `/squad/new` → `/squad`, `/schedule/new` → `/schedule`, `/schedule/planner` → `/schedule`, `/schedule/planner/new` → `/schedule`, `/schedule/planner/apply` → `/schedule`, `/schedule/fixtures/new` → `/schedule`, `/schedule/fixtures/[id]/participation` → `/denied?r=D-28NV`, `/compliance` → `/reports/compliance`, `/leaderboards/new` → `/leaderboards/manage`, `/nutrition/new` → `/nutrition`, `/reports/training` → `/reports/gps`, `/analytics` → `/denied?r=D-28P1`, `/settings/audit` → `/settings`, `/settings/groups/new` → `/settings/groups`, `/settings/imports` → `/settings`, `/settings/plan` → `/denied?r=D-28P3`, `/settings/retention` → `/settings`, `/settings/setup` → `/settings`, `/settings/thresholds/new` → `/settings/thresholds`, `/settings/users` → `/settings`, `/settings/users/bulk-invite` → `/settings`, `/settings/users/[id]` → `/settings`, `/platform/sign-in-probes` → `/denied?r=D-28P8`.
- **nutritionist** — 76 routes requested; 36 rendered as asked; 40 redirected: `/squad/roster` → `/squad`, `/squad/new` → `/squad`, `/schedule/new` → `/schedule`, `/schedule/planner` → `/schedule`, `/schedule/planner/new` → `/schedule`, `/schedule/planner/apply` → `/schedule`, `/schedule/fixtures/new` → `/schedule`, `/schedule/fixtures/[id]/participation` → `/denied?r=D-28LD`, `/compliance` → `/reports/compliance`, `/injuries` → `/denied?r=D-28LI`, `/injuries/new` → `/denied?r=D-28LK`, `/injuries/[id]` → `/denied?r=D-28LN`, `/injuries/rehab-groups` → `/denied?r=D-28LP`, `/injuries/team-allocation` → `/denied?r=D-28LR`, `/leaderboards/new` → `/leaderboards/manage`, `/programmes/new` → `/programmes`, `/programmes/proposals` → `/denied?r=D-28LV`, `/reports/athlete` → `/denied?r=D-28LZ`, `/reports/athlete/[id]` → `/denied?r=D-28M3`, `/reports/gps` → `/denied?r=D-28MB`, `/reports/match` → `/denied?r=D-28MJ`, `/reports/squad` → `/denied?r=D-28MN`, `/reports/testing` → `/denied?r=D-28MQ`, `/reports/training` → `/denied?r=D-28MV`, `/reports/training-load` → `/denied?r=D-28MY`, `/analytics` → `/denied?r=D-28N2`, `/settings/audit` → `/settings`, `/settings/exports` → `/denied?r=D-28N4`, `/settings/groups/new` → `/settings/groups`, `/settings/imports` → `/settings`, `/settings/plan` → `/denied?r=D-28NB`, `/settings/retention` → `/settings`, `/settings/setup` → `/settings`, `/settings/subject-access` → `/denied?r=D-28NH`, `/settings/subject-access/[id]/review` → `/denied?r=D-28NJ`, `/settings/thresholds/new` → `/settings/thresholds`, `/settings/users` → `/settings`, `/settings/users/bulk-invite` → `/settings`, `/settings/users/[id]` → `/settings`, `/platform/sign-in-probes` → `/denied?r=D-28NO`.
- **sc** — 76 routes requested; 51 rendered as asked; 25 redirected: `/squad/roster` → `/squad`, `/squad/new` → `/squad`, `/schedule/new` → `/schedule`, `/schedule/planner` → `/schedule`, `/schedule/planner/new` → `/schedule`, `/schedule/planner/apply` → `/schedule`, `/schedule/fixtures/new` → `/schedule`, `/schedule/fixtures/[id]/participation` → `/denied?r=D-28PN`, `/compliance` → `/reports/compliance`, `/nutrition/new` → `/nutrition`, `/reports/training` → `/reports/gps`, `/analytics` → `/denied?r=D-28Q9`, `/settings/audit` → `/settings`, `/settings/groups/new` → `/settings/groups`, `/settings/imports` → `/settings`, `/settings/plan` → `/denied?r=D-28QB`, `/settings/retention` → `/settings`, `/settings/setup` → `/settings`, `/settings/subject-access` → `/denied?r=D-28QD`, `/settings/subject-access/[id]/review` → `/denied?r=D-28QF`, `/settings/thresholds/new` → `/settings/thresholds`, `/settings/users` → `/settings`, `/settings/users/bulk-invite` → `/settings`, `/settings/users/[id]` → `/settings`, `/platform/sign-in-probes` → `/denied?r=D-28QH`.
- **sport_scientist** — 76 routes requested; 71 rendered as asked; 5 redirected: `/squad/roster` → `/squad`, `/compliance` → `/reports/compliance`, `/programmes/proposals` → `/denied?r=D-28NZ`, `/reports/training` → `/reports/gps`, `/settings/subject-access/[id]/review` → `/denied?r=D-28P5`.

## By page

The prompt asks for the list grouped by class, then by page. The entries below are one per selector (an element that fails on thirty pages is one defect with one fix, not thirty), so this index is the page view of the same entries: for every page any role reached, the counted Class 1 entries and the Class 2 entries short at 390.

| Page | Class 1, counted | Class 2, at 390 |
|---|---|---|
| `/analytics` | C11 | — |
| `/consent/decide` | C22 | — |
| `/consent/declined` | C22 | — |
| `/consent/staff` | C40 | — |
| `/dashboard` | C10, C13, C23, C24, C46 | — |
| `/flags` | C10, C11 | T11 |
| `/gym/[id]` | C21 | — |
| `/leaderboards` | C9, C11, C25, C26, C27, C28, C32, C42 | T16 |
| `/leaderboards/[id]` | C11, C44 | — |
| `/leaderboards/manage` | C11 | — |
| `/leaderboards/new` | C11 | — |
| `/login` | C33 | — |
| `/login/reset` | — | T1 |
| `/login/reset/confirm` | — | T1 |
| `/me` | — | T39 |
| `/me/data-consent` | C22 | — |
| `/me/notifications` | C38, C39 | T2 |
| `/me/privacy` | — | T2 |
| `/me/status` | — | T2 |
| `/my-data` | — | T18, T25 |
| `/my-data/boards` | — | T3 |
| `/my-data/boards/[id]` | — | T4 |
| `/nutrition` | C10, C11 | T14 |
| `/nutrition/new` | C10, C11 | — |
| `/programme` | — | T38 |
| `/programmes` | C10, C11 | — |
| `/programmes/[id]` | C10, C11 | — |
| `/programmes/[id]/athlete/[id]` | C10, C11 | — |
| `/programmes/exercises` | C10, C11 | — |
| `/programmes/new` | C10, C11 | — |
| `/programmes/proposals` | C10, C11 | T8 |
| `/reports` | C11, C16, C17 | — |
| `/reports/athlete` | C11, C35 | — |
| `/reports/athlete/[id]` | C11, C36 | — |
| `/reports/compliance` | C11, C34 | — |
| `/reports/gps` | C11, C45 | T19, T35, T43 |
| `/reports/injuries` | C11, C34, C41 | — |
| `/reports/match` | C11, C34 | T8 |
| `/reports/squad` | C11, C34 | T9, T21, T22, T36 |
| `/reports/testing` | C11, C34 | T17 |
| `/reports/training-load` | C11, C34 | T8 |
| `/schedule` | C10 | — |
| `/schedule/[id]` | C10 | — |
| `/schedule/fixtures/[id]` | C10 | — |
| `/schedule/fixtures/[id]/participation` | C10 | — |
| `/schedule/fixtures/new` | C10 | — |
| `/schedule/new` | C10 | — |
| `/schedule/planner` | C10 | — |
| `/schedule/planner/[id]` | C10 | T15 |
| `/schedule/planner/apply` | C10 | — |
| `/schedule/planner/new` | C10 | — |
| `/settings` | C11, C29, C30, C31 | — |
| `/settings/audit` | C11, C36 | — |
| `/settings/club` | C11 | T49 |
| `/settings/exports` | C11 | — |
| `/settings/groups` | C11 | T44 |
| `/settings/groups/[id]` | C11 | — |
| `/settings/groups/new` | C11 | — |
| `/settings/imports` | C11 | T44 |
| `/settings/notifications` | C11 | — |
| `/settings/plan` | C11 | — |
| `/settings/profile` | C11 | — |
| `/settings/retention` | C11 | — |
| `/settings/setup` | C11 | — |
| `/settings/subject-access` | C11, C37 | T44 |
| `/settings/subject-access/[id]/review` | C11 | — |
| `/settings/thresholds` | C11 | — |
| `/settings/thresholds/new` | C11 | — |
| `/settings/users` | C11 | T10 |
| `/settings/users/[id]` | C11 | — |
| `/settings/users/bulk-invite` | C11 | — |
| `/squad` | C10 | — |
| `/squad/[id]` | C3, C10, C14, C15, C18, C19, C20, C43 | — |
| `/squad/[id]/availability` | C10 | — |
| `/squad/[id]/gym` | C10 | — |
| `/squad/[id]/nutrition` | C10 | — |
| `/squad/[id]/wellness` | C10 | — |
| `/squad/new` | C10 | — |
| `/testing` | — | T38 |
| `/testing/[id]` | — | T6 |
| `/today/waiting` | — | T2 |

## Why the existing guards did not catch this

The baseline's answer stands, re-checked against the guards as they are
tonight. Every guard in `prebuild` that touches this ground is a
**source-level** check — it reads `base.css`, `tokens.css` or a `.tsx` file
and asserts a rule or an attribute is present — and none of them renders a
page. That is the gap in one sentence; the four below are where it lands, and
the two things that changed since the baseline are marked.

**`test:a11y-floor` (`scripts/test-a11y-floor.ts`) measures neither contrast
nor size.** Its own header names what it pins: "Two floors a screen reader
depends on, checked at the source" — a rendered error is announced
(`role="alert"` / `aria-live`), and every screen has a heading. Both are
markup facts a grep can answer. Nothing in it reads a colour or a box, so it
could not have caught any defect in this report, and it was never claimed
to. Its name promises more than its scope, which is why the prompt names it.

**`test:nav-hit-floor` (`scripts/test-nav-hit-floor.ts`) pins two controls in
one shell.** It asserts that `.phone-body .back-btn::after` extends the
athlete Back button to 44px and that `.tap-floor` pads the hidden-leaderboards
gate link — the two controls three reviews had measured short — and states
its own limit: "Scoped to the athlete shell. The staff app's `.back-btn` is
the same class and still renders 29px … Widening it is a separate decision."
It reads the stylesheet for those two selectors and nothing else, so every
other control on every other surface is outside it by construction. (The
staff Back button at 29px is now, by ruling #8, not a defect at 1440; at 390
it is in Class 2 below if it measures short there.)

**`check:contrast` (`scripts/check-contrast.ts`) measures the stylesheet, on
two flat grounds, with an exemption list.** It derives the set of tokens
`base.css` paints text with and checks each hex against `--bg` and `--surf`
(`GROUNDS = ['bg', 'surf']`), and says so: a token "whose surface is a computed
tint is reported as unresolved rather than failed". Three consequences, each
visible in Class 1:

1. *Composited grounds are never measured.* A token that clears 4.5 on both
   flat grounds can fail on a wash over them — the prompt's own sentence,
   "a tint over a surface is not the token's nominal value", is exactly the
   case the guard declares out of scope. Class 1 lists every such ground by
   name ("`--wash-accent` over `--bg`").
2. *`--surf2` is not a ground it knows.* Dark `--surf2` is the fill of every
   closed settings row, every KPI tile, every leaderboard chip; a text token
   that passes on `--surf` can fail on it.
3. *`KNOWN_BELOW_AA` is down to one entry, and it is the one that fails
   most.* **Changed since the baseline:** `warn-text` (light) left the list
   in `42ad4ff` because every small amber text moved onto `--warn-pill-text`
   and the guard no longer sees the token painted; the placeholder rule the
   baseline said did not exist now does (`input::placeholder,
   textarea::placeholder { color: var(--faint-on-tint) }`, `base.css:102`),
   so the browser's own grey no longer stands in. **Not changed:** `accent`
   (dark, 2.87 on `--bg`) is still exempt with the note that "the 5 text uses
   want `--accent-text`. Fixing the call sites is the right change" — and the
   call sites are still not fixed. Every dark measurement of `--accent` as a
   text colour in Class 1 is that entry. An exemption list is a promise to
   fix later with no date on it.

**`test:staff-phone-shell` §5 pins the phone floor as a class list.** The 44px
floor for staff controls below 768px (`base.css` "THE 44px FLOOR FOR STAFF
CONTROLS BELOW 768") is a list of selectors, and the guard asserts each listed
selector has `min-height: 44px`. Anything not on the list is not floored and
not tested — every Class 2 entry that fails at 390 below is a control the list
does not name, or names by the wrong box (a padded cell whose target is the
`<a>` inside it; a `.btn-ghost` rule with `min-height` that does nothing on an
`<a class="btn-ghost">`, which is `display: inline`). A guard that checks
"the rule is there" cannot check "the rule reaches the element", which is the
difference between the stylesheet and the render.

**What would close the gap.** Not a longer class list or a longer exemption
list — both are the shape that failed. The measuring tool in
`scripts/a11y-sweep.mjs` reads the render; the missing piece is a fixture run
of it in `prebuild` (or nightly) against a seeded page set as each role,
asserting zero rows in Class 1 and Class 2 above the agreed floors (4.5:1
both themes; 44px at 390, per ruling #8), with `KNOWN_BELOW_AA` retired once
its one entry is fixed. That is a proposal for a ruling, not something this
pass builds.


## Class 1 — contrast

Grouped by the element that carries the colour, worst ratio first; each entry lists every surface and role it was measured on and both themes. "Counted" is a defect; the two other labels say why an entry is on record but not in the count. The proposed fix is one token for another that passes on that element's own composited ground in BOTH themes, from the reporter's table of every text-role token; where the fix says a ruling is needed, the failing factor is not the text token.

**Read with these in mind:**

- **C3, the flag card's value** — the flag's tone colour (`--warn`, `--accent`) used as ink on the flag's wash: 1.48:1 in light. The worst counted pair in the product, and the one the baseline flagged as C7 before the tone swap moved everything else. `--warn-pill-text` is the family's own ink and passes both themes.
- **C10 and C11, `--accent` as text in dark** — the staff phone tab bar's active tab on every staff page at 390 (2.87:1). This is the one remaining `KNOWN_BELOW_AA` entry, whose own note names the fix (`--accent-text`) that has not been made.
- **C13–C15, C17–C20, C23–C32, C41, C43–C45 — `--faint` and `--faint-on-tint` on a wash or on dark `--surf2`.** The most repeated shape: a token that clears 4.5 on both flat grounds and fails on the composited one (3.7–4.5): the profile's flag-card captions on `--wash-warn`, the dashboard tiles' labels and feet, the leaderboard wall's headers and units, the settings hub's locked rows (C29–C31, `aria-disabled`, in scope — the row's own `--faint` ink, not a fade), the injury report's unit column, the profile's benchmark meta, the leaderboard gap column. `--muted` passes on every one of them in both themes and is the smallest move; `--text` passes everywhere.
- **C16, C42, C46 — three other inks on composited grounds**: the report hub's source pill (`--accent2-pill-text` on its tint, 3.9 light; `--warn-pill-text` on the testing pill, 3.39 dark), the leaderboard wall's `#1` marker (`--lb-rank1` on `--border`, 4.14), and the dashboard week strip's "1 flag" (`--bad-text` on the washed column, 4.46 dark). Each has a passing swap named in its entry.
- **C33–C37 — `--faint` on bare `--bg` in light, 3.98:1.** The system-A `--faint` (`#667287`) on the deeper light ground is below AA on its own, so any `--faint` text directly on the page ground fails: the launch eyebrow, the report eyebrows, the athlete picker's legend, "Page 1 of 54", the subject-access table's header. `--muted` is the swap.
- **C38–C40 — `--muted` on `--wash-accent` over the athlete `--phone-bg`, 4.09:1 in light** — the notification screen's sub lines and the consent screen's explanatory lines. Only `--text` passes; `--muted-on-tint` (`#4f596c`) is the context ink built for exactly this ground and should be measured before the swap is chosen — the reporter's candidate list did not name it because it lists text-role tokens by name and `--muted-on-tint` resolves to `--muted` in dark.
- **C21 and C22 are the athlete app's only counted contrast entries**: the gym logger's not-reached set keys in dark (`--faint-on-tint` on `--surf2`, 3.89:1) and the LEGAL-* placeholder refs on the consent screens (10px, 4.21 light / 3.89 dark).

### C1. `h1.next-error-h1`

**Not counted here — the Next.js default 404, black on the dark ground. Other findings, 1.**

- dark: **1.23:1** (floor 3, large text) — text rgb(0, 0, 0) on --phone-bg (1.23:1). Size 24px, weight 500. Sample: "404".
- surfaces: /my-data/gym/[id] (6), /gym/[id] (4); roles: athlete_declined, athlete_minor_kai, athlete_minor_niall; widths: 1440/390.
- where: (no rule located by class; inherited from an ancestor outside the recorded path)
- proposed fix: swap to `--text` (light 14.37, dark 14.55) or `--muted` (light 5.08, dark 8.1) or `--faint` (light 3.9, dark 5.53) (currently not a token).

### C2. `div > h2`

**Not counted here — the Next.js default 404, black on the dark ground. Other findings, 1.**

- dark: **1.23:1** (floor 4.5) — text rgb(0, 0, 0) on --phone-bg (1.23:1). Size 14px, weight 400. Sample: "This page could not be found.".
- surfaces: /my-data/gym/[id] (6), /gym/[id] (4); roles: athlete_declined, athlete_minor_kai, athlete_minor_niall; widths: 1440/390.
- where: (no rule located by class; inherited from an ancestor outside the recorded path)
- proposed fix: swap to `--text` (light 14.37, dark 14.55) or `--muted` (light 5.08, dark 8.1) (currently not a token).

### C3. `span.pp-flag-value`

**Counted.**

- light: **1.48:1** (floor 4.5) — text --warn on --wash-warn over --bg (1.48:1). Size 14px, weight 500. Sample: "78.0 kg".
- dark: **2.49:1** (floor 4.5) — text --accent on --wash-warn over --bg (2.49:1). Size 14px, weight 500. Sample: "0 of 7 days".
- surfaces: /squad/[id] (38); roles: medic, nutritionist, sc, sport_scientist, coach; widths: 1440/390.
- where: src/components/PlayerProfileFlags/PlayerProfileFlags.tsx:194 (class `pp-flag-value`; no base.css rule sets color on it); the measured token is set inline — see src/components/PlayerProfileFlags/PlayerProfileFlags.tsx
- proposed fix: swap to `--warn-pill-text` (light 6.31, dark 6.18) or `--text` (light 13.64, dark 10.3) or `--muted` (light 4.82, dark 5.73) (currently `--warn`).

### C4. `span.btn-ghost[aria-disabled]` — aria-disabled, in scope

**Not counted — a faded unavailable control (ruling #9, 14 Sept: the fade stays, recorded as deliberate). Listed so the measurement is on record.**

- light: **1.74:1** (floor 4.5) — text --muted at opacity 0.4 on --bg (1.74:1). Size 13px, weight 600. Sample: "Next week ›".
- dark: **2.27:1** (floor 4.5) — text --muted-on-tint = --muted at opacity 0.4 on --bg (2.27:1). Size 13px, weight 600. Sample: "Next week ›".
- surfaces: /nutrition (32); roles: coach, medic, nutritionist, sc, sport_scientist; widths: 1440/390.
- where: src/styles/base.css:800 `.btn-ghost { color: var(--muted) }`; src/styles/base.css:814 `.btn-ghost:hover { color: var(--text) }`; src/styles/base.css:3444 `.subm .btn-ghost[aria-disabled='true']:hover { color: var(--muted) }`; the opacity: src/components/AddAthleteForm/AddAthleteForm.tsx:264 (class `btn-ghost`; no base.css rule sets opacity on it)
- proposed fix: the text token is not the failing factor — the element is faded to 0.4 by an opacity rule, and no text token clears the floor through it (best: --text at light 2.47, dark 3.18); report and stop — a ruling on the fade is needed (currently `--muted`).

### C5. `button.btn-ghost-pill[aria-disabled]` — aria-disabled, in scope

**Not counted — a faded unavailable control (ruling #9, 14 Sept: the fade stays, recorded as deliberate). Listed so the measurement is on record.**

- light: **1.83:1** (floor 4.5) — text --muted-on-tint at opacity 0.45 on --wash-accent over --bg (1.83:1). Size 13px, weight 600. Sample: "Edit".
- dark: **2.33:1** (floor 4.5) — text --muted-on-tint = --muted at opacity 0.45 on --wash-accent over --bg (2.33:1). Size 13px, weight 600. Sample: "Edit".
- surfaces: /squad/[id] (8); roles: nutritionist, sc; widths: 1440/390.
- where: src/styles/base.css:4407 `.btn-ghost-pill { color: var(--muted) }`; src/styles/base.css:4423 `.btn-ghost-pill:hover { color: var(--text) }`; src/styles/base.css:4427 `.btn-ghost-pill.accent { color: var(--accent-text) }`; the opacity: src/styles/base.css:4432 `.btn-ghost-pill:disabled, .btn-ghost-pill[aria-disabled='true'] { opacity: var(--o-disabled) }`
- proposed fix: the text token is not the failing factor — the element is faded to 0.45 by an opacity rule, and no text token clears the floor through it (best: --text at light 2.69, dark 3.34); report and stop — a ruling on the fade is needed (currently `--muted-on-tint`).

### C6. `span.week-nav > span[aria-disabled]` — aria-disabled, in scope

**Not counted — a faded unavailable control (ruling #9, 14 Sept: the fade stays, recorded as deliberate). Listed so the measurement is on record.**

- light: **1.88:1** (floor 4.5) — text --faint at opacity 0.5 on --surf2 = --pending-fill (1.88:1). Size 13px, weight 400. Sample: "›".
- dark: **2.04:1** (floor 4.5) — text --faint-on-tint = --faint at opacity 0.5 on --surf2 = --border = --pending-fill (2.04:1). Size 13px, weight 400. Sample: "›".
- surfaces: /reports/squad (16); roles: coach, medic, sc, sport_scientist; widths: 1440/390.
- where: src/styles/base.css:12309 `.week-nav a, .week-nav span { color: var(--muted) }`; src/styles/base.css:12313 `.week-nav a:hover { color: var(--text) }`; src/styles/base.css:12326 `.week-nav span[data-disabled='true'] { color: var(--faint) }`; the opacity: src/styles/base.css:12327 `.week-nav span[data-disabled='true'] { opacity: 0.5 }`
- proposed fix: the text token is not the failing factor — the element is faded to 0.5 by an opacity rule, and no text token clears the floor through it (best: --text at light 3.3, dark 3.82); report and stop — a ruling on the fade is needed (currently `--faint`).

### C7. `button.squad-chip[aria-disabled]` — aria-disabled, in scope

**Not counted — a faded unavailable control (ruling #9, 14 Sept: the fade stays, recorded as deliberate). Listed so the measurement is on record.**

- light: **2:1** (floor 4.5) — text --muted at opacity 0.45 on --on-accent = --knob-fill = --toast-text (2:1). Size 12px, weight 600. Sample: "Body mass", "Readiness score".
- dark: **2.59:1** (floor 4.5) — text --muted-on-tint = --muted at opacity 0.45 on --surf = --elev (2.59:1). Size 12px, weight 600. Sample: "Body mass", "Readiness score".
- surfaces: /leaderboards/new (48); roles: coach, sc, sport_scientist; widths: 1440/390.
- where: src/styles/base.css:856 `.squad-chip { color: var(--muted) }`; src/styles/base.css:867 `.squad-chip:hover { color: var(--text) }`; src/styles/base.css:873 `.squad-chip[aria-pressed='true'], .squad-chip[aria-selected='true'], .squad-chip[aria-current='page'] { color: var(--on-accent) }`; the opacity: src/styles/base.css:887 `.squad-chip:disabled, .squad-chip[aria-disabled='true'] { opacity: var(--o-disabled) }`
- proposed fix: the text token is not the failing factor — the element is faded to 0.45 by an opacity rule, and no text token clears the floor through it (best: --text at light 2.94, dark 3.76); report and stop — a ruling on the fade is needed (currently `--muted`).

### C8. `p.tiny`

**Not counted — a faded unavailable control (ruling #9, 14 Sept: the fade stays, recorded as deliberate). Listed so the measurement is on record.**

- light: **2:1** (floor 4.5) — text --muted at opacity 0.45 on --on-accent = --knob-fill = --toast-text (2:1). Size 13px, weight 400. Sample: "RPE × minutes, per athlete, over the period.", "Not open to your role.".
- dark: **2.59:1** (floor 4.5) — text --muted-on-tint = --muted at opacity 0.45 on --surf = --elev (2.59:1). Size 13px, weight 400. Sample: "RPE × minutes, per athlete, over the period.", "Not open to your role.".
- surfaces: /reports (48); roles: nutritionist; widths: 1440/390.
- where: src/styles/base.css:751 `.tiny { color: var(--muted) }`; src/styles/base.css:10239 `.flag-notice .tiny, .flag-notice-attribution { color: var(--muted) }`; src/styles/base.css:11950 `.prog-item .tiny { color: var(--muted-on-tint) }`; the opacity: src/components/AddAthleteForm/AddAthleteForm.tsx:108 (class `tiny`; no base.css rule sets opacity on it)
- proposed fix: the text token is not the failing factor — the element is faded to 0.45 by an opacity rule, and no text token clears the floor through it (best: --text at light 2.94, dark 3.76); report and stop — a ruling on the fade is needed (currently `--muted`).

### C9. `span.lbw-chip-count`

**Counted.**

- light: **2.64:1** (floor 4.5) — text --muted at opacity 0.65 on --bg (2.64:1). Size 11px, weight 600. Sample: "2", "1".
- dark: **3.68:1** (floor 4.5) — text --muted-on-tint = --muted at opacity 0.65 on --bg (3.68:1). Size 11px, weight 600. Sample: "2", "1".
- surfaces: /leaderboards (80); roles: coach, medic, nutritionist, sc, sport_scientist; widths: 1440/390.
- where: src/components/LeaderboardWall/LeaderboardWall.tsx:303 (class `lbw-chip-count`; no base.css rule sets color on it); the opacity: src/styles/base.css:8350 `.lbw-chip-count { opacity: 0.65 }`
- proposed fix: swap to `--text` (light 5.13, dark 5.92) — measured through the 0.65 fade the element carries (currently `--muted`).

### C10. `a.ph-tab`

**Counted.**

- dark: **2.87:1** (floor 4.5) — text --accent on --bg (2.87:1). Size 12px, weight 700. Sample: "Dashboard", "Squad".
- surfaces: /schedule (20), /squad (15), /schedule/[id] (10), /dashboard (6), /squad/[id] (6), /squad/[id]/availability (5), /squad/[id]/gym (5), /squad/[id]/nutrition (5), /squad/[id]/wellness (5), /schedule/planner/[id] (5), /schedule/fixtures/[id] (5), /flags (4), /schedule/new (2), /schedule/planner (2), /schedule/planner/new (2), /schedule/planner/apply (2), /schedule/fixtures/new (2), /schedule/fixtures/[id]/participation (2), /nutrition (1), /nutrition/new (1), /programmes (1), /programmes/new (1), /programmes/exercises (1), /programmes/proposals (1), /programmes/[id] (1), /programmes/[id]/athlete/[id] (1), /squad/new (1); roles: basic_sport_scientist, coach, medic, nutritionist, sc, sport_scientist; widths: 390.
- where: src/styles/base.css:14374 `.ph-tab { color: var(--muted) }`; src/styles/base.css:14382 `.ph-tab[data-active] { color: var(--accent) }`
- proposed fix: swap to `--accent-on-wash` (light 7.05, dark 7.3) or `--accent-text` (light 7.05, dark 6.68) or `--accent-on-tint` (light 7.05, dark 6.68) or `--accent-pill-text` (light 7.05, dark 6.68) (currently `--accent`).

### C11. `button.ph-tab.ph-more`

**Counted.**

- dark: **2.87:1** (floor 4.5) — text --accent on --bg (2.87:1). Size 12px, weight 700. Sample: "More".
- surfaces: /settings (34), /reports/compliance (10), /reports/gps (8), /settings/groups (8), /settings/thresholds (8), /leaderboards/manage (7), /nutrition (7), /reports (6), /leaderboards (6), /settings/club (6), /programmes (6), /reports/athlete/[id] (5), /reports/training-load (5), /reports/match (5), /leaderboards/[id] (5), /reports/injuries (5), /settings/groups/[id] (5), /settings/notifications (5), /settings/profile (5), /programmes/exercises (4), /programmes/[id] (4), /programmes/[id]/athlete/[id] (4), /reports/athlete (4), /reports/squad (4), /reports/testing (4), /settings/exports (4), /leaderboards/new (3), /settings/plan (2), /settings/retention (2), /settings/groups/new (2), /settings/thresholds/new (2), /programmes/new (2), /settings/subject-access (2), /flags (2), /programmes/proposals (1), /settings/subject-access/[id]/review (1), /nutrition/new (1), /analytics (1), /settings/audit (1), /settings/imports (1), /settings/setup (1), /settings/users (1), /settings/users/bulk-invite (1), /settings/users/[id] (1); roles: basic_sport_scientist, coach, medic, nutritionist, sc, sport_scientist; widths: 390.
- where: src/styles/base.css:14374 `.ph-tab { color: var(--muted) }`; src/styles/base.css:14382 `.ph-tab[data-active] { color: var(--accent) }`
- proposed fix: swap to `--accent-on-wash` (light 7.05, dark 7.3) or `--accent-text` (light 7.05, dark 6.68) or `--accent-on-tint` (light 7.05, dark 6.68) or `--accent-pill-text` (light 7.05, dark 6.68) (currently `--accent`).

### C12. `p.card-title`

**Not counted — a faded unavailable control (ruling #9, 14 Sept: the fade stays, recorded as deliberate). Listed so the measurement is on record.**

- light: **2.94:1** (floor 4.5) — text --text = --on-bright-tint at opacity 0.45 on --on-accent = --knob-fill = --toast-text (2.94:1). Size 15px, weight 700. Sample: "Training load", "GPS report".
- dark: **3.76:1** (floor 4.5) — text --heat-ink-1 = --heat-ink-2 = --heat-ink-3 at opacity 0.45 on --surf = --elev (3.76:1). Size 15px, weight 700. Sample: "Training load", "GPS report".
- surfaces: /reports (24); roles: nutritionist; widths: 1440/390.
- where: src/app/(staff)/reports/athlete/[athleteId]/page.tsx:324 (class `card-title`; no base.css rule sets color on it); the opacity: src/components/AddAthleteForm/AddAthleteForm.tsx:105 (class `card-title`; no base.css rule sets opacity on it)
- proposed fix: the text token is not the failing factor — the element is faded to 0.45 by an opacity rule, and no text token clears the floor through it (best: --text at light 2.94, dark 3.76); report and stop — a ruling on the fade is needed (currently `--text`).

### C13. `div.dash-week-col-empty`

**Counted.**

- light: **3.08:1** (floor 4.5) — text --faint on --wash-accent-strong over --bg (3.08:1); rgb(213,213,228) (composited; no single wash over a surface reproduces it) (3.34:1); --wash-accent-soft over --bg (3.57:1). Size 13px, weight 400. Sample: "Nothing scheduled".
- dark: **3.73:1** (floor 4.5) — text --faint-on-tint = --faint on --wash-accent-soft twice over --bg (3.73:1); rgb(48,52,91) (composited; no single wash over a surface reproduces it) (3.85:1); --wash-accent-soft over --bg (4.09:1). Size 13px, weight 400. Sample: "Nothing scheduled".
- surfaces: /dashboard (96); roles: basic_sport_scientist, coach, medic, sport_scientist; widths: 1440/390.
- where: src/styles/base.css:6898 `.dash-week-col-empty { color: var(--faint) }`
- proposed fix: swap to `--text` (light 11.37, dark 9.75) (currently `--faint`).

### C14. `div.v`

**Counted.**

- light: **3.2:1** (floor 4.5) — text --faint on --wash-accent over --bg (3.2:1). Size 15px, weight 400. Sample: "—", "76".
- dark: **3.85:1** (floor 4.5) — text --faint-on-tint = --faint on --wash-accent over --bg (3.85:1). Size 15px, weight 400. Sample: "—", "76".
- surfaces: /squad/[id] (24); roles: basic_sport_scientist, coach, medic, nutritionist, sc, sport_scientist; widths: 1440/390.
- where: src/styles/base.css:2996 `.step .val .v[data-empty] { color: var(--faint) }`; src/styles/base.css:4222 `.target-bar .th .v { color: var(--muted) }`; src/styles/base.css:4297 `.me-row .v { color: var(--accent-text) }`
- proposed fix: swap to `--text` (light 11.75, dark 10.07) (currently `--faint`).

### C15. `div.l`

**Counted.**

- light: **3.2:1** (floor 4.5) — text --faint on --wash-accent over --bg (3.2:1). Size 9/12px, weight 400. Sample: "wellness", "Position".
- dark: **3.85:1** (floor 4.5) — text --faint-on-tint = --faint on --wash-accent over --bg (3.85:1). Size 9/12px, weight 400. Sample: "wellness", "Position".
- surfaces: /squad/[id] (164); roles: basic_sport_scientist, coach, medic, nutritionist, sc, sport_scientist; widths: 1440/390.
- where: src/styles/base.css:4570 `.pp-wellness-mini .l { color: var(--faint) }`; src/styles/base.css:4595 `.pp-detail-cell .l { color: var(--faint) }`
- proposed fix: swap to `--text` (light 11.75, dark 10.07) (currently `--faint`).

### C16. `span.rep-source`

**Counted.**

- light: **3.9:1** (floor 4.5) — text --accent2-pill-text on rgb(168,209,233) (composited; no single wash over a surface reproduces it) (3.9:1). Size 12px, weight 700. Sample: "session RPE", "post-match sheet".
- dark: **3.39:1** (floor 4.5) — text --warn-pill-text = --lb-warn-tint-text = --warn on rgb(101,93,61) (composited; no single wash over a surface reproduces it) (3.39:1). Size 12px, weight 700. Sample: "testing".
- surfaces: /reports (30); roles: basic_sport_scientist, coach, medic, sc, sport_scientist; widths: 1440/390.
- where: src/styles/base.css:10695 `.rep-source { color: var(--rep-pill-text, var(--muted)) }`
- proposed fix: swap to `--text` (light 11.07, dark 5.64) (currently `--accent2-pill-text`).

### C17. `span.rep-exports`

**Counted.**

- light: **3.52:1** (floor 4.5) — text --faint on --wash-accent-soft over --bg (3.52:1); --wash-bad over --bg (3.55:1); rgb(205,224,242) (composited; no single wash over a surface reproduces it) (3.6:1); rgb(210,231,247) (composited; no single wash over a surface reproduces it) (3.83:1); +1 more grounds. Size 12px, weight 400. Sample: "CSV · PDF".
- dark: **3.59:1** (floor 4.5) — text --faint-on-tint = --faint on rgb(55,60,72) (composited; no single wash over a surface reproduces it) (3.59:1); rgb(37,60,96) (composited; no single wash over a surface reproduces it) (3.61:1); rgb(34,57,96) (composited; no single wash over a surface reproduces it) (3.75:1); rgb(55,48,78) (composited; no single wash over a surface reproduces it) (4.02:1); +1 more grounds. Size 12px, weight 400. Sample: "CSV · PDF".
- surfaces: /reports (124); roles: basic_sport_scientist, coach, medic, nutritionist, sc, sport_scientist; widths: 1440/390.
- where: src/styles/base.css:10700 `.rep-exports { color: var(--faint) }`
- proposed fix: swap to `--text` (light 12.97, dark 9.44) or `--muted` (light 4.59, dark 5.26) (currently `--faint`).

### C18. `span.pp-flag-threshold`

**Counted.**

- light: **3.7:1** (floor 4.5) — text --faint on --wash-warn over --bg (3.7:1). Size 12px, weight 400. Sample: "against 4 of 7 days expected", "against 80.0 kg expected".
- dark: **3.91:1** (floor 4.5) — text --faint-on-tint = --faint on --wash-warn over --bg (3.91:1). Size 12px, weight 400. Sample: "against 4 of 7 days expected", "against 80.0 kg expected".
- surfaces: /squad/[id] (76); roles: coach, medic, nutritionist, sc, sport_scientist; widths: 1440/390.
- where: src/styles/base.css:4789 `.pp-flag-threshold { color: var(--faint) }`
- proposed fix: swap to `--text` (light 13.64, dark 10.3) or `--muted` (light 4.82, dark 5.73) (currently `--faint`).

### C19. `p.pp-flag-evidence`

**Counted.**

- light: **3.7:1** (floor 4.5) — text --faint on --wash-warn over --bg (3.7:1). Size 12px, weight 400. Sample: "1 day against a fixed value · flagged Fri 28 Aug", "1 day against a fixed value · flagged Sat 5 Sept".
- dark: **3.91:1** (floor 4.5) — text --faint-on-tint = --faint on --wash-warn over --bg (3.91:1). Size 12px, weight 400. Sample: "1 day against a fixed value · flagged Fri 28 Aug", "1 day against a fixed value · flagged Sat 5 Sept".
- surfaces: /squad/[id] (76); roles: coach, medic, nutritionist, sc, sport_scientist; widths: 1440/390.
- where: src/styles/base.css:4797 `.pp-flag-evidence { color: var(--faint) }`
- proposed fix: swap to `--text` (light 13.64, dark 10.3) or `--muted` (light 4.82, dark 5.73) (currently `--faint`).

### C20. `span.pp-flag-raised`

**Counted.**

- light: **3.7:1** (floor 4.5) — text --faint on --wash-warn over --bg (3.7:1). Size 12px, weight 400. Sample: "raised Sat 29 Aug", "raised Sun 6 Sept".
- dark: **3.91:1** (floor 4.5) — text --faint-on-tint = --faint on --wash-warn over --bg (3.91:1). Size 12px, weight 400. Sample: "raised Sat 29 Aug", "raised Sun 6 Sept".
- surfaces: /squad/[id] (70); roles: coach, medic, nutritionist, sc, sport_scientist; widths: 1440/390.
- where: src/styles/base.css:4809 `.pp-flag-raised { color: var(--faint) }`
- proposed fix: swap to `--text` (light 13.64, dark 10.3) or `--muted` (light 4.82, dark 5.73) (currently `--faint`).

### C21. `span.gym-set-key`

**Counted.**

- dark: **3.89:1** (floor 4.5) — text --faint-on-tint = --faint on --surf2 = --border = --pending-fill (3.89:1). Size 16px, weight 700. Sample: "2", "3".
- surfaces: /gym/[id] (6); roles: athlete_adult; widths: 1440/390.
- where: src/styles/base.css:3855 `.gym-set-key { color: var(--faint-on-tint) }`; src/styles/base.css:3865 `.gym-set-key[data-next] { color: var(--accent-text) }`; src/styles/base.css:3870 `.gym-set-key[data-logged] { color: var(--on-accent) }`
- proposed fix: swap to `--text` (light 15.54, dark 10.23) or `--muted` (light 5.5, dark 5.69) (currently `--faint-on-tint`).

### C22. `span.legal-pending-ref`

**Counted.**

- light: **4.21:1** (floor 4.5) — text --faint on --surf2 = --pending-fill (4.21:1). Size 10px, weight 700. Sample: "LEGAL-3A · solicitor to supply", "LEGAL-3B · solicitor to supply".
- dark: **3.89:1** (floor 4.5) — text --faint-on-tint = --faint on --surf2 = --border = --pending-fill (3.89:1). Size 10px, weight 700. Sample: "LEGAL-3A · solicitor to supply", "LEGAL-3B · solicitor to supply".
- surfaces: /me/data-consent (96), /consent/decide (64), /consent/declined (4); roles: athlete_adult, athlete_declined, athlete_minor_kai, athlete_minor_niall; widths: 1440/390.
- where: src/styles/base.css:14776 `.legal-pending-ref { color: var(--faint) }`
- proposed fix: swap to `--text` (light 15.54, dark 10.23) or `--muted` (light 5.5, dark 5.69) (currently `--faint`).

### C23. `div.dash-stat-label`

**Counted.**

- dark: **3.89:1** (floor 4.5) — text --faint-on-tint = --faint on --surf2 = --border = --pending-fill (3.89:1). Size 11px, weight 600. Sample: "Wellness in", "Available".
- surfaces: /dashboard (18); roles: basic_sport_scientist, coach, medic, sc, sport_scientist; widths: 1440/390.
- where: src/styles/base.css:6625 `.dash-stat-label { color: var(--faint-on-tint) }`; src/styles/base.css:6659 `.dash-stat[data-urgent='true'] .dash-stat-label { color: var(--bad-text) }`; src/styles/base.css:6666 `.dash-stat[data-tone='good'] .dash-stat-label, .dash-stat[data-tone='good'] .dash-stat-value { color: var(--good-pill-text) }`
- proposed fix: swap to `--text` (light 15.54, dark 10.23) or `--muted` (light 5.5, dark 5.69) (currently `--faint-on-tint`).

### C24. `div.dash-stat-foot`

**Counted.**

- dark: **3.89:1** (floor 4.5) — text --faint-on-tint = --faint on --surf2 = --border = --pending-fill (3.89:1). Size 11px, weight 400. Sample: "window closes 09:00", "injury status set by medical, other absences by ".
- surfaces: /dashboard (18); roles: basic_sport_scientist, coach, medic, sc, sport_scientist; widths: 1440/390.
- where: src/styles/base.css:6734 `.dash-stat-foot { color: var(--faint-on-tint) }`
- proposed fix: swap to `--text` (light 15.54, dark 10.23) or `--muted` (light 5.5, dark 5.69) (currently `--faint-on-tint`).

### C25. `div.lbw-sel-thead > div`

**Counted.**

- dark: **3.89:1** (floor 4.5) — text --faint-on-tint = --faint on --surf2 = --border = --pending-fill (3.89:1). Size 10px, weight 600. Sample: "Test".
- surfaces: /leaderboards (10); roles: coach, medic, nutritionist, sc, sport_scientist; widths: 1440/390.
- where: src/styles/base.css:8589 `.lbw-sel-thead { color: var(--faint-on-tint) }`
- proposed fix: swap to `--text` (light 15.54, dark 10.23) or `--muted` (light 5.5, dark 5.69) (currently `--faint-on-tint`).

### C26. `div.r`

**Counted.**

- dark: **3.89:1** (floor 4.5) — text --faint-on-tint = --faint on --surf2 = --border = --pending-fill (3.89:1). Size 10px, weight 600. Sample: "Result", "In group".
- surfaces: /leaderboards (30); roles: coach, medic, nutritionist, sc, sport_scientist; widths: 1440/390.
- where: src/app/(staff)/leaderboards/[leaderboardId]/page.tsx:1 (class `r`; no base.css rule sets color on it); the measured token is set inline — see src/app/(staff)/dashboard/page.tsx, src/app/(staff)/reports/athlete/[athleteId]/page.tsx, src/app/(staff)/reports/athlete/page.tsx
- proposed fix: swap to `--text` (light 15.54, dark 10.23) or `--muted` (light 5.5, dark 5.69) (currently `--faint-on-tint`).

### C27. `p.lbw-leader-board`

**Counted.**

- dark: **3.89:1** (floor 4.5) — text --faint-on-tint = --faint on --surf2 = --border = --pending-fill (3.89:1). Size 11px, weight 600. Sample: "10m sprint", "40m sprint".
- surfaces: /leaderboards (40); roles: coach, medic, nutritionist, sc, sport_scientist; widths: 1440/390.
- where: src/styles/base.css:8661 `.lbw-leader-board { color: var(--faint-on-tint) }`
- proposed fix: swap to `--text` (light 15.54, dark 10.23) or `--muted` (light 5.5, dark 5.69) (currently `--faint-on-tint`).

### C28. `span.lbw-leader-unit`

**Counted.**

- dark: **3.89:1** (floor 4.5) — text --faint-on-tint = --faint on --surf2 = --border = --pending-fill (3.89:1). Size 11px, weight 400. Sample: "Front row", "Second row".
- surfaces: /leaderboards (240); roles: coach, medic, nutritionist, sc, sport_scientist; widths: 1440/390.
- where: src/styles/base.css:8673 `.lbw-leader-unit { color: var(--faint-on-tint) }`
- proposed fix: swap to `--text` (light 15.54, dark 10.23) or `--muted` (light 5.5, dark 5.69) (currently `--faint-on-tint`).

### C29. `span.set-row-label[aria-disabled]` — aria-disabled, in scope

**Counted.**

- light: **4.21:1** (floor 4.5) — text --faint on --surf2 = --pending-fill (4.21:1). Size 14px, weight 600. Sample: "Plan", "Club details".
- dark: **3.89:1** (floor 4.5) — text --faint-on-tint = --faint on --surf2 = --border = --pending-fill (3.89:1). Size 14px, weight 600. Sample: "Plan", "Club details".
- surfaces: /settings (896); roles: coach, medic, nutritionist, sc; widths: 1440/390.
- where: src/app/(staff)/settings/page.tsx:117 (class `set-row-label`; no base.css rule sets color on it); the measured token is set inline — see src/app/(staff)/settings/page.tsx
- proposed fix: swap to `--text` (light 15.54, dark 10.23) or `--muted` (light 5.5, dark 5.69) (currently `--faint`).

### C30. `span.set-row-sub[aria-disabled]` — aria-disabled, in scope

**Counted.**

- light: **4.21:1** (floor 4.5) — text --faint on --surf2 = --pending-fill (4.21:1). Size 12px, weight 400. Sample: "Sport scientist only", "Read by the sport scientist".
- dark: **3.89:1** (floor 4.5) — text --faint-on-tint = --faint on --surf2 = --border = --pending-fill (3.89:1). Size 12px, weight 400. Sample: "Sport scientist only", "Read by the sport scientist".
- surfaces: /settings (896); roles: coach, medic, nutritionist, sc; widths: 1440/390.
- where: src/styles/base.css:5301 `.set-row-sub { color: var(--faint) }`
- proposed fix: swap to `--text` (light 15.54, dark 10.23) or `--muted` (light 5.5, dark 5.69) (currently `--faint`).

### C31. `span.set-row-count[aria-disabled]` — aria-disabled, in scope

**Counted.**

- light: **4.21:1** (floor 4.5) — text --faint on --surf2 = --pending-fill (4.21:1). Size 12px, weight 400. Sample: "—".
- dark: **3.89:1** (floor 4.5) — text --faint-on-tint = --faint on --surf2 = --border = --pending-fill (3.89:1). Size 12px, weight 400. Sample: "—".
- surfaces: /settings (896); roles: coach, medic, nutritionist, sc; widths: 1440/390.
- where: src/styles/base.css:5305 `.set-row-count { color: var(--muted) }`; the measured token is set inline — see src/app/(staff)/settings/page.tsx
- proposed fix: swap to `--text` (light 15.54, dark 10.23) or `--muted` (light 5.5, dark 5.69) (currently `--faint`).

### C32. `p.dash-stat-foot`

**Counted.**

- dark: **3.97:1** (floor 4.5) — text --faint-on-tint = --faint on rgb(34,54,84) (composited; no single wash over a surface reproduces it) (3.97:1). Size 11px, weight 400. Sample: "standards differ for forwards and backs · Fydr p".
- surfaces: /leaderboards (10); roles: coach, medic, nutritionist, sc, sport_scientist; widths: 1440/390.
- where: src/styles/base.css:6734 `.dash-stat-foot { color: var(--faint-on-tint) }`
- proposed fix: swap to `--text` (light —, dark 10.42) or `--muted` (light —, dark 5.8) (currently `--faint-on-tint`).

### C33. `p.eyebrow.launch-narrow-only`

**Counted.**

- light: **3.98:1** (floor 4.5) — text --faint on --bg (3.98:1). Size 11px, weight 600. Sample: "For athletes and club staff".
- surfaces: /login (1); roles: anonymous; widths: 390.
- where: src/styles/base.css:618 `.eyebrow { color: var(--muted) }`; src/styles/base.css:13355 `.launch .launch-head .eyebrow { color: var(--faint) }`
- proposed fix: swap to `--text` (light 14.67, dark 11.84) or `--muted` (light 5.19, dark 6.59) (currently `--faint`).

### C34. `p.rhead-eyebrow`

**Counted.**

- light: **3.98:1** (floor 4.5) — text --faint on --bg (3.98:1). Size 12px, weight 700. Sample: "Reports · Training load", "Reports · Match".
- surfaces: /reports/compliance (20), /reports/training-load (10), /reports/match (10), /reports/injuries (10), /reports/squad (8), /reports/testing (8); roles: basic_sport_scientist, coach, medic, nutritionist, sc, sport_scientist; widths: 1440/390.
- where: src/styles/base.css:13778 `.rhead-eyebrow { color: var(--faint) }`
- proposed fix: swap to `--text` (light 14.67, dark 11.84) or `--muted` (light 5.19, dark 6.59) (currently `--faint`).

### C35. `span.pick-legend > span`

**Counted.**

- light: **3.98:1** (floor 4.5) — text --faint on --bg (3.98:1). Size 12px, weight 400. Sample: "n = 31 athlete s".
- surfaces: /reports/athlete (8); roles: coach, medic, sc, sport_scientist; widths: 1440/390.
- where: src/styles/base.css:12122 `.pick-legend { color: var(--muted) }`; the measured token is set inline — see src/app/(staff)/reports/athlete/page.tsx
- proposed fix: swap to `--text` (light 14.67, dark 11.84) or `--muted` (light 5.19, dark 6.59) (currently `--faint`).

### C36. `span.tiny`

**Counted.**

- light: **3.98:1** (floor 4.5) — text --faint on --bg (3.98:1); --band-2-wash over --surf (4.48:1). Size 13px, weight 400. Sample: "( cm )", "Page 1 of 54".
- surfaces: /reports/athlete/[id] (8), /settings/audit (2); roles: coach, medic, sc, sport_scientist; widths: 1440/390.
- where: src/styles/base.css:751 `.tiny { color: var(--muted) }`; src/styles/base.css:10239 `.flag-notice .tiny, .flag-notice-attribution { color: var(--muted) }`; src/styles/base.css:11950 `.prog-item .tiny { color: var(--muted-on-tint) }`; the measured token is set inline — see src/app/(staff)/dashboard/page.tsx, src/app/(staff)/reports/athlete/[athleteId]/page.tsx, src/app/(staff)/reports/athlete/page.tsx
- proposed fix: swap to `--text` (light 14.67, dark 11.84) or `--muted` (light 5.19, dark 6.59) (currently `--faint`).

### C37. `tr > th`

**Counted.**

- light: **3.98:1** (floor 4.5) — text --faint on --bg (3.98:1). Size 11px, weight 700. Sample: "Athlete", "Requested".
- surfaces: /settings/subject-access (10); roles: medic, sport_scientist; widths: 1440.
- where: (no rule located by class; inherited from an ancestor outside the recorded path)
- proposed fix: swap to `--text` (light 14.67, dark 11.84) or `--muted` (light 5.19, dark 6.59) (currently `--faint`).

### C38. `span.mute-switch-sub`

**Counted.**

- light: **4.09:1** (floor 4.5) — text --muted on --wash-accent over --phone-bg (4.09:1). Size 12px, weight 400. Sample: "One switch for every notification you may turn o".
- surfaces: /me/notifications (8); roles: athlete_adult, athlete_declined, athlete_minor_kai, athlete_minor_niall; widths: 1440/390.
- where: src/styles/base.css:11060 `.mute-switch-sub { color: var(--muted) }`
- proposed fix: swap to `--text` (light 11.61, dark 12.35) (currently `--muted`).

### C39. `p.mute-note`

**Counted.**

- light: **4.09:1** (floor 4.5) — text --muted on --wash-accent over --phone-bg (4.09:1). Size 13px, weight 400. Sample: "Nothing stops being recorded. Your check-ins, ra".
- surfaces: /me/notifications (8); roles: athlete_adult, athlete_declined, athlete_minor_kai, athlete_minor_niall; widths: 1440/390.
- where: src/styles/base.css:751 `.tiny { color: var(--muted) }`; src/styles/base.css:10239 `.flag-notice .tiny, .flag-notice-attribution { color: var(--muted) }`; src/styles/base.css:11950 `.prog-item .tiny { color: var(--muted-on-tint) }`
- proposed fix: swap to `--text` (light 11.61, dark 12.35) (currently `--muted`).

### C40. `p.import-sub`

**Counted.**

- light: **4.09:1** (floor 4.5) — text --muted on --wash-accent over --phone-bg (4.09:1). Size 13px, weight 400. Sample: "Once you submit a morning check-in or a session ", "A day you do not answer stays empty. It is not c".
- surfaces: /consent/staff (16); roles: athlete_adult, athlete_declined, athlete_minor_kai, athlete_minor_niall; widths: 1440/390.
- where: src/styles/base.css:724 `.import-sub { color: var(--muted) }`
- proposed fix: swap to `--text` (light 11.61, dark 12.35) (currently `--muted`).

### C41. `span.inj-unit`

**Counted.**

- dark: **4.11:1** (floor 4.5) — text --faint-on-tint = --faint on rgb(49,50,65) (composited; no single wash over a surface reproduces it) (4.11:1); --wash-bad over --surf (4.46:1). Size 12px, weight 400. Sample: "Scrum-half", "Wing".
- surfaces: /reports/injuries (100); roles: coach, medic, nutritionist, sc, sport_scientist; widths: 1440/390.
- where: src/styles/base.css:11526 `.inj-unit { color: var(--faint) }`
- proposed fix: swap to `--text` (light 15.6, dark 10.8) or `--muted` (light 5.52, dark 6.01) (currently `--faint-on-tint`).

### C42. `span.lbw-value-marker`

**Counted.**

- light: **4.14:1** (floor 4.5) — text --lb-rank1 on --border over --surf-sunken (4.14:1). Size 10px, weight 500. Sample: "#1".
- dark: **4.46:1** (floor 4.5) — text --warn-pill-text = --lb-warn-tint-text = --warn on rgb(82,74,70) (composited; no single wash over a surface reproduces it) (4.46:1). Size 10px, weight 500. Sample: "#4".
- surfaces: /leaderboards (20); roles: coach, medic, nutritionist, sc, sport_scientist; widths: 1440/390.
- where: src/components/LeaderboardWall/LeaderboardWall.tsx:512 (class `lbw-value-marker`; no base.css rule sets color on it)
- proposed fix: swap to `--text` (light 11.16, dark 7.4) (currently `--lb-rank1`).

### C43. `span.pp-bench-meta`

**Counted.**

- light: **4.18:1** (floor 4.5) — text --faint on --wash-bad over --surf (4.18:1); --band-2-wash over --surf (4.48:1); --band-4-wash over --surf (4.49:1). Size 12px, weight 400. Sample: "median 1.7 · best 1.7 · n=22", "median 38.3 · best 70.0 · n=26".
- dark: **4.16:1** (floor 4.5) — text --faint-on-tint = --faint on --wash-good over --surf (4.16:1); --wash-warn over --surf (4.19:1); --wash-bad over --surf (4.46:1). Size 12px, weight 400. Sample: "median 1.7 · best 1.7 · n=22", "median 38.3 · best 70.0 · n=26".
- surfaces: /squad/[id] (100); roles: coach, medic, nutritionist, sc, sport_scientist; widths: 1440/390.
- where: src/styles/base.css:4752 `.pp-bench-meta { color: var(--faint) }`
- proposed fix: swap to `--text` (light 15.45, dark 10.97) or `--muted` (light 5.47, dark 6.11) (currently `--faint`).

### C44. `span.lb-gap`

**Counted.**

- light: **4.48:1** (floor 4.5) — text --faint on rgb(243,246,250) (composited; no single wash over a surface reproduces it) (4.48:1). Size 13px, weight 400. Sample: "—", "−1,194".
- dark: **4.39:1** (floor 4.5) — text --faint-on-tint = --faint on --heat-1 (4.39:1). Size 13px, weight 400. Sample: "—", "−1,194".
- surfaces: /leaderboards/[id] (60); roles: coach, medic, nutritionist, sc, sport_scientist; widths: 1440/390.
- where: src/styles/base.css:11885 `.lb-gap { color: var(--faint) }`
- proposed fix: swap to `--text` (light 16.54, dark 11.55) or `--muted` (light 5.85, dark 6.43) (currently `--faint`).

### C45. `div.tiny`

**Counted.**

- light: **4.41:1** (floor 4.5) — text --faint on --wash-accent-soft over --surf (4.41:1). Size 13px, weight 400. Sample: "Fri 14 Aug · MD-8 ·".
- surfaces: /reports/gps (16); roles: coach, medic, sc, sport_scientist; widths: 1440/390.
- where: src/styles/base.css:751 `.tiny { color: var(--muted) }`; src/styles/base.css:10239 `.flag-notice .tiny, .flag-notice-attribution { color: var(--muted) }`; src/styles/base.css:11950 `.prog-item .tiny { color: var(--muted-on-tint) }`; the measured token is set inline — see src/app/(staff)/dashboard/page.tsx, src/app/(staff)/reports/athlete/[athleteId]/page.tsx, src/app/(staff)/reports/athlete/page.tsx
- proposed fix: swap to `--text` (light 16.26, dark 11.54) or `--muted` (light 5.75, dark 6.42) (currently `--faint`).

### C46. `div.dash-week-col-flag`

**Counted.**

- dark: **4.46:1** (floor 4.5) — text --bad-text = --bad-pill-text on rgb(48,52,91) (composited; no single wash over a surface reproduces it) (4.46:1). Size 12px, weight 600. Sample: "1 flag".
- surfaces: /dashboard (4); roles: medic, sport_scientist; widths: 1440/390.
- where: src/styles/base.css:6907 `.dash-week-col-flag { color: var(--bad-text) }`
- proposed fix: swap to `--bad-on-tint` (light —, dark 6.94) or `--text` (light —, dark 10.16) or `--muted` (light —, dark 5.66) (currently `--bad-text`).


Distinct contrast defects: 46.

### Natively disabled controls, listed and not counted (WCAG 1.4.3 exempts an inactive component)

- `button.squad-chip[disabled]` — light 1.75:1, dark 1.9:1; /me/notifications (12).
- `button.btn-ghost[disabled]` — light 4.21:1, dark 3.89:1; /schedule/planner/[id] (48), /nutrition (24), /testing/[id] (16), /reports/athlete/[id] (10), /settings/thresholds/new (4), /settings/users (2).
- `button.sg-btn-published[disabled]` — light 4.21:1, dark 3.89:1; /schedule (4).
- `button.btn-primary[disabled]` — light 4.21:1, dark 3.89:1; /nutrition (12), /schedule/planner/[id] (4), /settings/users/bulk-invite (2).
- `button.nutr-stepper-btn[disabled]` — light 1.7:1, dark 1.91:1; /nutrition (96).
- `button.reorder-btn[disabled]` — light 1.44:1, dark 1.54:1; /settings/groups (8).

## Class 2 — tap targets under 44px

Every interactive element measured under 44px on either side. **Ruling #8 (14 September) applies: 44px is a touch rule and applies at phone widths**, so the first group — short at 390 — is the defect list; the second group — short at 1440 only — is on record and not a defect; the third is natively disabled and exempt (WCAG 1.4.3 / 2.5.8), listed the way the baseline listed them. The measured sizes are the hit box: the border box, widened by a wrapping `<label>` for a radio or checkbox and by an absolutely positioned `::before`/`::after`.

**Read with these in mind:**

- **T1–T4, T6, T8–T11, T14, T16–T18 — text links at 15–20px.** The staff shell's own floor (`base.css` "THE 44px FLOOR", the padding-and-negative-margin extension §0au built) reaches `.main a.tiny`, `.main .pp-link`, `.main table.tbl .nm`, `.main .eyebrow a` and `.main .tiny a` — and these entries are the links it does not reach: the `<a>` inside `td.nm` on the match, testing and training-load reports (the rule pads the cell, T8), `a.attn-name` (T9), `p.flag-who > a` on every flag card (T11), `a.nm` on the testing sheet (T6), `p.nm > a` on the users list (T10), the nutrition and leaderboard "Profile ›" links (T14, T16), "Manage tests →" (T17), the sign-in pages' "Back to sign in" (T1), and on the athlete app "← Today" / "← Me" (T2), "Manage who sees you on a leaderboard" (T3), "Back to leaderboards" (T4), "See all 28 days →" (T18). Each is one selector added to that rule's list; nothing on the line moves.
- **T19, T21–T22, T25, T35–T36, T38–T39, T43–T44, T49 — controls that are short at 390 because their own rule sets the box**: the GPS report's heat toggle (22px), the squad report's week arrows (24px, one of them the inert marker of Other findings 3), the athlete's period select on My data (30px), the GPS mode switch and scope chips (35–37px), the squad report's athlete chips (35px), the `/programme` and `/testing` rows squeezed to a 36px column at 390 (Other findings, 5), the theme toggle's Light / Dark on the athlete's Me (36px), the `<a class="btn-ghost">` that `min-height` never reaches because a link is inline (37px; Other findings, 4), the settings rows' buttons (41px). Each is `min-height: var(--tap-min)` (and `min-width` where the target is a square) on the control, never into a neighbour.
- **T15 — the native checkbox**, measured through its wrapping label: 19px on the week-template builder's "on" toggles, both widths. (The threshold editor's baseline radios, T50, are 41px and 1440 only.)

### Short at 390 — the defects (25)

### T1. `p.signin-return > a` — short side 15px

- measured: 86×15px (8). Text: "Back to sign in".
- at both widths: /login/reset, /login/reset/confirm; roles: anonymous.
- where: src/styles/base.css:10088 `.signin-return { font-size: var(--fs-13) }`
- proposed fix: a text link: the padding-and-negative-margin extension §0au already uses (`display: inline-block; padding-block: var(--sp-12); margin-block: calc(-1 * var(--sp-12)); line-height: 20px`, base.css "THE 44px FLOOR"), added to that rule's selector list so nothing on the line moves; never into a neighbour, never overlapping another target.

### T2. `p.tiny > a` — short side 15px

- measured: 34×15px (48), 51×15px (16), 101×15px (10), 133×15px (4). Text: "← Today", "← Me", "View full detail →".
- at 390: /today/waiting, /me/notifications, /me/privacy, /me/status; at 1440: /today/waiting, /me/notifications, /me/privacy, /me/status, /programmes; roles: athlete_adult, athlete_declined, athlete_minor_kai, athlete_minor_niall, coach, medic, nutritionist, sc, sport_scientist.
- where: src/styles/base.css:750 `.tiny { font-size: var(--fs-13) }`; src/styles/base.css:14563 `.main a.tiny, .main .pp-link, .main table.tbl .nm, .main .eyebrow a, .main .tiny a { padding-block: var(--sp-12) }`
- proposed fix: a text link: the padding-and-negative-margin extension §0au already uses (`display: inline-block; padding-block: var(--sp-12); margin-block: calc(-1 * var(--sp-12)); line-height: 20px`, base.css "THE 44px FLOOR"), added to that rule's selector list so nothing on the line moves; never into a neighbour, never overlapping another target.

### T3. `p.cap > a` — short side 15px

- measured: 229×15px (16). Text: "Manage who sees you on a leaderboard".
- at both widths: /my-data/boards; roles: athlete_adult, athlete_declined, athlete_minor_kai, athlete_minor_niall.
- where: src/styles/base.css:737 `.cap { font-size: var(--fs-13) }`; src/styles/base.css:2828 `.card.flush > .cap { padding: 0 var(--pad-card) var(--pad-card) }`
- proposed fix: a text link: the padding-and-negative-margin extension §0au already uses (`display: inline-block; padding-block: var(--sp-12); margin-block: calc(-1 * var(--sp-12)); line-height: 20px`, base.css "THE 44px FLOOR"), added to that rule's selector list so nothing on the line moves; never into a neighbour, never overlapping another target.

### T4. `p > a` — short side 15px

- measured: 122×15px (8). Text: "Back to leaderboards".
- at both widths: /my-data/boards/[id]; roles: athlete_minor_kai, athlete_minor_niall.
- where: (no rule located by class; inherited from an ancestor outside the recorded path)
- proposed fix: a text link: the padding-and-negative-margin extension §0au already uses (`display: inline-block; padding-block: var(--sp-12); margin-block: calc(-1 * var(--sp-12)); line-height: 20px`, base.css "THE 44px FLOOR"), added to that rule's selector list so nothing on the line moves; never into a neighbour, never overlapping another target.

### T6. `a.nm` — short side 15px

- measured: 84×15px (74), 95×15px (72), 79×15px (72), 56×15px (44), 67×15px (38), +52 more sizes. Text: "James Barnes", "Max Chapman", "Callum Northcott".
- at 390: /testing/[id]; at 1440: /squad, /reports/testing, /testing/[id]; roles: basic_sport_scientist, coach, medic, nutritionist, sc, sport_scientist.
- where: src/styles/base.css:755 `.nm { font-size: var(--fs-13) }`; src/styles/base.css:1269 `table.tbl.lb-table .nm { font-size: var(--fs-14) }`; src/styles/base.css:4052 `.gl-then-row .nm { font-size: var(--fs-14) }`
- proposed fix: a text link: the padding-and-negative-margin extension §0au already uses (`display: inline-block; padding-block: var(--sp-12); margin-block: calc(-1 * var(--sp-12)); line-height: 20px`, base.css "THE 44px FLOOR"), added to that rule's selector list so nothing on the line moves; never into a neighbour, never overlapping another target.

### T8. `td.nm > a` — short side 15px

- measured: 84×15px (64), 79×15px (64), 95×15px (64), 75×15px (32), 83×15px (32), +23 more sizes. Text: "Sione Aholelei", "Harry Ainsley", "Rob Baptiste".
- at both widths: /reports/match, /reports/training-load, /programmes/proposals; roles: coach, medic, sc, sport_scientist.
- where: src/styles/base.css:755 `.nm { font-size: var(--fs-13) }`; src/styles/base.css:1269 `table.tbl.lb-table .nm { font-size: var(--fs-14) }`; src/styles/base.css:4052 `.gl-then-row .nm { font-size: var(--fs-14) }`
- proposed fix: a text link: the padding-and-negative-margin extension §0au already uses (`display: inline-block; padding-block: var(--sp-12); margin-block: calc(-1 * var(--sp-12)); line-height: 20px`, base.css "THE 44px FLOOR"), added to that rule's selector list so nothing on the line moves; never into a neighbour, never overlapping another target.

### T9. `a.attn-name` — short side 15px

- measured: 67×15px (16), 70×15px (16), 105×15px (16), 56×15px (16), 59×15px (16), +6 more sizes. Text: "Henry Ross", "Adam Selby", "Viliami Tameifuna".
- at both widths: /reports/squad; roles: coach, medic, sc, sport_scientist.
- where: src/styles/base.css:1095 `.attn-name { font-size: var(--fs-13) }`
- proposed fix: a text link: the padding-and-negative-margin extension §0au already uses (`display: inline-block; padding-block: var(--sp-12); margin-block: calc(-1 * var(--sp-12)); line-height: 20px`, base.css "THE 44px FLOOR"), added to that rule's selector list so nothing on the line moves; never into a neighbour, never overlapping another target.

### T10. `p.nm > a` — short side 15px

- measured: 90×15px (8), 79×15px (8), 83×15px (8), 56×15px (8), 94×15px (8), +26 more sizes. Text: "Adam Selby", "Alex Grant", "Anna Whitmore".
- at both widths: /settings/users; roles: sport_scientist.
- where: src/styles/base.css:755 `.nm { font-size: var(--fs-13) }`; src/styles/base.css:1269 `table.tbl.lb-table .nm { font-size: var(--fs-14) }`; src/styles/base.css:4052 `.gl-then-row .nm { font-size: var(--fs-14) }`
- proposed fix: a text link: the padding-and-negative-margin extension §0au already uses (`display: inline-block; padding-block: var(--sp-12); margin-block: calc(-1 * var(--sp-12)); line-height: 20px`, base.css "THE 44px FLOOR"), added to that rule's selector list so nothing on the line moves; never into a neighbour, never overlapping another target.

### T11. `p.flag-who > a` — short side 16px

- measured: 86×16px (156), 90×16px (144), 63×16px (140), 102×16px (140), 113×16px (120), +21 more sizes. Text: "View Max Chapman's player profile", "View Henry Ross's player profile", "View Adam Selby's player profile".
- at both widths: /flags; roles: basic_sport_scientist, coach, medic, nutritionist, sc, sport_scientist.
- where: src/styles/base.css:3535 `.flag-who { font-size: var(--fs-14) }`
- proposed fix: a text link: the padding-and-negative-margin extension §0au already uses (`display: inline-block; padding-block: var(--sp-12); margin-block: calc(-1 * var(--sp-12)); line-height: 20px`, base.css "THE 44px FLOOR"), added to that rule's selector list so nothing on the line moves; never into a neighbour, never overlapping another target.

### T14. `a.nutr-profile-link` — short side 17px

- measured: 41×17px (32). Text: "Profile ›".
- at both widths: /nutrition; roles: coach, medic, nutritionist, sc, sport_scientist.
- where: src/styles/base.css:7940 `.nutr-profile-link { font-size: var(--fs-12) }`
- proposed fix: a text link: the padding-and-negative-margin extension §0au already uses (`display: inline-block; padding-block: var(--sp-12); margin-block: calc(-1 * var(--sp-12)); line-height: 20px`, base.css "THE 44px FLOOR"), added to that rule's selector list so nothing on the line moves; never into a neighbour, never overlapping another target.

### T15. `label.tiny > input[type=checkbox]` — short side 19px

- measured: 78×19px (48), 50×19px (48), 76×19px (48); hit box read via label. Text: "on".
- at both widths: /schedule/planner/[id]; roles: coach, sport_scientist.
- where: src/styles/base.css:750 `.tiny { font-size: var(--fs-13) }`; src/styles/base.css:14563 `.main a.tiny, .main .pp-link, .main table.tbl .nm, .main .eyebrow a, .main .tiny a { padding-block: var(--sp-12) }`
- proposed fix: `min-height: var(--tap-min)` on the control; never into a neighbour, never overlapping another target.

### T16. `a.lbw-profile-link` — short side 19px

- measured: 45×19px (20). Text: "Profile ›".
- at both widths: /leaderboards; roles: coach, medic, nutritionist, sc, sport_scientist.
- where: src/styles/base.css:8562 `.lbw-profile-link { font-size: var(--fs-13) }`
- proposed fix: a text link: the padding-and-negative-margin extension §0au already uses (`display: inline-block; padding-block: var(--sp-12); margin-block: calc(-1 * var(--sp-12)); line-height: 20px`, base.css "THE 44px FLOOR"), added to that rule's selector list so nothing on the line moves; never into a neighbour, never overlapping another target.

### T17. `a.tst-manage` — short side 19px

- measured: 96×19px (8), 358×19px (8). Text: "Manage tests →".
- at both widths: /reports/testing; roles: coach, medic, sc, sport_scientist.
- where: src/styles/base.css:13619 `.tst-manage { font-size: var(--fs-13) }`
- proposed fix: a text link: the padding-and-negative-margin extension §0au already uses (`display: inline-block; padding-block: var(--sp-12); margin-block: calc(-1 * var(--sp-12)); line-height: 20px`, base.css "THE 44px FLOOR"), added to that rule's selector list so nothing on the line moves; never into a neighbour, never overlapping another target.

### T18. `p.hist-more > a` — short side 20px

- measured: 112×20px (16). Text: "See all 28 days →".
- at both widths: /my-data; roles: athlete_adult, athlete_declined, athlete_minor_kai, athlete_minor_niall.
- where: src/styles/base.css:1784 `.hist-more { padding: var(--sp-12) var(--pad-card-x) }`; src/styles/base.css:1785 `.hist-more { font-size: var(--fs-14) }`; src/styles/base.css:1793 `.hist-more a { min-height: 20px }`
- proposed fix: a text link: the padding-and-negative-margin extension §0au already uses (`display: inline-block; padding-block: var(--sp-12); margin-block: calc(-1 * var(--sp-12)); line-height: 20px`, base.css "THE 44px FLOOR"), added to that rule's selector list so nothing on the line moves; never into a neighbour, never overlapping another target.

### T19. `a.tr-heat-toggle[role=switch]` — short side 22px

- measured: 76×22px (32). Text: "Heat".
- at both widths: /reports/gps; roles: coach, medic, sc, sport_scientist.
- where: src/styles/base.css:10989 `.tr-heat-toggle { font-size: var(--fs-13) }`
- proposed fix: a text link: the padding-and-negative-margin extension §0au already uses (`display: inline-block; padding-block: var(--sp-12); margin-block: calc(-1 * var(--sp-12)); line-height: 20px`, base.css "THE 44px FLOOR"), added to that rule's selector list so nothing on the line moves; never into a neighbour, never overlapping another target.

### T21. `span.week-nav > a` — short side 24px

- measured: 24×31px (16). Text: "Previous week".
- at both widths: /reports/squad; roles: coach, medic, sc, sport_scientist.
- where: src/styles/base.css:12298 `.week-nav { padding: var(--sp-6) }`; src/styles/base.css:12306 `.week-nav a, .week-nav span { padding: var(--sp-6) var(--sp-10) }`; src/styles/base.css:12308 `.week-nav a, .week-nav span { font-size: var(--fs-13) }`
- proposed fix: `min-width: var(--tap-min); min-height: var(--tap-min)` on the control (an `<a>` is inline — `display: inline-flex` first, or `min-height` has no effect); never into a neighbour, never overlapping another target.

### T22. `span.week-nav > span[aria-disabled]` — short side 24px — **aria-disabled, in scope**

- measured: 24×31px (16). Text: "›".
- at both widths: /reports/squad; roles: coach, medic, sc, sport_scientist.
- where: src/styles/base.css:12298 `.week-nav { padding: var(--sp-6) }`; src/styles/base.css:12306 `.week-nav a, .week-nav span { padding: var(--sp-6) var(--sp-10) }`; src/styles/base.css:12308 `.week-nav a, .week-nav span { font-size: var(--fs-13) }`
- proposed fix: `min-width: var(--tap-min); min-height: var(--tap-min)` on the control; never into a neighbour, never overlapping another target.

### T25. `span.rsel-wrap > select` — short side 30px

- measured: 280×40px (58), 280×30px (26), 232×40px (16), 209×30px (16), 276×30px (12), +1 more sizes. Text: "Period shown", "Period for the body weight and wellness ", "Reporting period".
- at 390: /my-data; at 1440: /my-data, /squad/[id], /reports/athlete/[id], /reports/training-load, /reports/match, /reports/compliance, /reports/gps, /reports/injuries, /reports/testing, /analytics; roles: athlete_adult, athlete_declined, athlete_minor_kai, athlete_minor_niall, basic_sport_scientist, coach, medic, nutritionist, sc, sport_scientist.
- where: src/styles/base.css:13522 `.rsel-wrap { min-width: 148px }`; src/styles/base.css:13531 `.rsel-wrap select { width: 100% }`; src/styles/base.css:13537 `.rsel-wrap select { padding: var(--sp-6) var(--sp-28) var(--sp-6) var(--sp-10) }`
- proposed fix: `min-height: var(--tap-min)` on the control; never into a neighbour, never overlapping another target.

### T35. `div.tr-mode-switch > a` — short side 35px

- measured: 84×35px (32), 97×35px (32), 60×40px (32), 71×40px (32). Text: "Training", "Match day", "Day".
- at both widths: /reports/gps; roles: coach, medic, sc, sport_scientist.
- where: src/styles/base.css:5907 `.tr-mode-switch { padding: var(--sp-4) }`; src/styles/base.css:5911 `.tr-mode-switch a { padding: var(--sp-8) var(--sp-18) }`; src/styles/base.css:5912 `.tr-mode-switch a { font-size: var(--fs-13) }`
- proposed fix: `min-height: var(--tap-min)` on the control (an `<a>` is inline — `display: inline-flex` first, or `min-height` has no effect); never into a neighbour, never overlapping another target.

### T36. `a.chip-static` — short side 35px

- measured: 127×35px (16), 94×35px (16). Text: "Viliami Tameifuna", "Adam Selby".
- at both widths: /reports/squad; roles: coach, medic, sc, sport_scientist.
- where: src/styles/base.css:901 `.chip-static { font-size: var(--fs-12) }`; src/styles/base.css:903 `.chip-static { padding: var(--sp-8) var(--sp-14) }`
- proposed fix: `min-height: var(--tap-min)` on the control (an `<a>` is inline — `display: inline-flex` first, or `min-height` has no effect); never into a neighbour, never overlapping another target.

### T38. `a.load-row` — short side 36px

- measured: 36×152px (30), 36×171px (30), 36×190px (10), 402×39px (8), 312×39px (8). Text: "Meal ideas›", "10m sprintSpeed · s · lower is better · ", "CMJ heightPower · cm · higher is better ".
- at 390: /programme, /testing; at 1440: /programme; roles: athlete_adult, athlete_declined, athlete_minor_kai, athlete_minor_niall, coach, medic, nutritionist, sc, sport_scientist.
- where: src/styles/base.css:1140 `.load-row { padding: var(--sp-10) var(--sp-4) }`; src/styles/base.css:1142 `.load-row { font-size: var(--fs-13) }`
- proposed fix: `min-width: var(--tap-min); min-height: var(--tap-min)` on the control (an `<a>` is inline — `display: inline-flex` first, or `min-height` has no effect); never into a neighbour, never overlapping another target.

### T39. `button.theme-seg-btn` — short side 36px

- measured: 55×36px (84), 53×36px (84). Text: "Light", "Dark".
- at 390: /me; at 1440: /me, /settings; roles: athlete_adult, athlete_declined, athlete_minor_kai, athlete_minor_niall, basic_sport_scientist, coach, medic, nutritionist, sc, sport_scientist.
- where: src/styles/base.css:679 `.theme-seg-btn { font-size: var(--fs-12) }`; src/styles/base.css:682 `.theme-seg-btn { padding: 0 var(--sp-14) }`; src/styles/base.css:14524 `.main .back-btn, .main .btn-ghost, .main .btn-primary, .main select, .main .sg-stepper-btn, .main .sg-btn-remove, .main .sg-btn-add, .main .sg-btn-publish, .main .sg-btn-published, .main .sg-btn-discard, .main .nutr-stepper-btn, .main .lbw-segmented button, .main .theme-seg-btn, .main .reorder-btn { min-height: var(--tap-min) }`
- proposed fix: `min-height: var(--tap-min)` on the control; never into a neighbour, never overlapping another target.

### T43. `a.tr-scope-chip` — short side 37px

- measured: 131×37px (32), 160×37px (32), 82×37px (32), 76×37px (32), 72×37px (32), +1 more sizes. Text: "Rest of the week", "Comparable sessions", "Position".
- at both widths: /reports/gps; roles: coach, medic, sc, sport_scientist.
- where: src/styles/base.css:6031 `.tr-scope-chip { padding: var(--sp-8) var(--sp-16) }`; src/styles/base.css:6033 `.tr-scope-chip { font-size: var(--fs-13) }`
- proposed fix: `min-height: var(--tap-min)` on the control (an `<a>` is inline — `display: inline-flex` first, or `min-height` has no effect); never into a neighbour, never overlapping another target.

### T44. `a.btn-ghost` — short side 37px

- measured: 59×37px (16), 171×37px (8), 169×37px (4). Text: "Open team allocation →", "Review clinical notes →", "Export the 1 records from c11-file.csv a".
- at both widths: /settings/groups, /settings/subject-access, /settings/imports; roles: coach, medic, sport_scientist.
- where: src/styles/base.css:803 `.btn-ghost { padding: var(--sp-10) var(--sp-16) }`; src/styles/base.css:804 `.btn-ghost { min-height: var(--tap-min) }`; src/styles/base.css:805 `.btn-ghost { font-size: var(--fs-13) }`
- proposed fix: `min-height: var(--tap-min)` on the control (an `<a>` is inline — `display: inline-flex` first, or `min-height` has no effect); never into a neighbour, never overlapping another target.

### T49. `a.set-row-btn` — short side 41px

- measured: 102×41px (20), 65×41px (20), 76×41px (8). Text: "Locked", "Import files", "Open".
- at both widths: /settings/club; roles: basic_sport_scientist, coach, medic, nutritionist, sc, sport_scientist.
- where: src/styles/base.css:5234 `.set-row-btn { padding: var(--sp-10) var(--sp-16) }`; src/styles/base.css:5235 `.set-row-btn { font-size: var(--fs-13) }`
- proposed fix: `min-height: var(--tap-min)` on the control (an `<a>` is inline — `display: inline-flex` first, or `min-height` has no effect); never into a neighbour, never overlapping another target.


### Short at 1440 only — on record, not a defect by ruling #8 (23)

Every one of these clears 44px at 390 (the staff shell's phone floor reaches it) or is absent there. WCAG 2.5.8 (AA) asks 24px of a pointer target, and all but "Log out" (17px), the week arrows (24px) and the disabled "Published" marker clear that too. Listed so that if the desktop ruling is ever revisited the list is already here.

### T5. `a.tiny` — short side 15px

- measured: 76×19px (12), 109×19px (8), 96×19px (8), 136×15px (8). Text: "Compliance ›", "Team allocation →", "Rehab groups →".
- at 390: clears 44px or absent; at 1440: /dashboard, /injuries, /reports/squad; roles: basic_sport_scientist, coach, medic, nutritionist, sc, sport_scientist.
- where: src/styles/base.css:750 `.tiny { font-size: var(--fs-13) }`; src/styles/base.css:14563 `.main a.tiny, .main .pp-link, .main table.tbl .nm, .main .eyebrow a, .main .tiny a { padding-block: var(--sp-12) }`
- proposed fix: a text link: the padding-and-negative-margin extension §0au already uses (`display: inline-block; padding-block: var(--sp-12); margin-block: calc(-1 * var(--sp-12)); line-height: 20px`, base.css "THE 44px FLOOR"), added to that rule's selector list so nothing on the line moves; never into a neighbour, never overlapping another target.

### T7. `a.pp-link` — short side 15px

- measured: 175×15px (24), 73×19px (12), 125×19px (6), 93×19px (4). Text: "Thresholds ›", "View full detail ›", "What this resolves to for Dan ›".
- at 390: clears 44px or absent; at 1440: /squad/[id], /squad/[id]/gym; roles: basic_sport_scientist, coach, medic, nutritionist, sc, sport_scientist.
- where: src/styles/base.css:4391 `.pp-link { font-size: var(--fs-13) }`; src/styles/base.css:14563 `.main a.tiny, .main .pp-link, .main table.tbl .nm, .main .eyebrow a, .main .tiny a { padding-block: var(--sp-12) }`
- proposed fix: a text link: the padding-and-negative-margin extension §0au already uses (`display: inline-block; padding-block: var(--sp-12); margin-block: calc(-1 * var(--sp-12)); line-height: 20px`, base.css "THE 44px FLOOR"), added to that rule's selector list so nothing on the line moves; never into a neighbour, never overlapping another target.

### T12. `button.nav-signout` — short side 17px

- measured: 71×17px (794). Text: "Log out".
- at 390: clears 44px or absent; at 1440: /dashboard, /squad, /squad/[id], /flags, /reports, /reports/athlete/[id], /denied, /reports/training-load, /reports/match, /leaderboards, /settings, /settings/plan, /settings/club, /settings/retention, /squad/[id]/availability, /squad/[id]/gym, /squad/[id]/nutrition, /squad/[id]/wellness, /schedule, /schedule/new, /schedule/planner, /schedule/planner/new, /schedule/planner/[id], /schedule/planner/apply, /schedule/[id], /schedule/fixtures/new, /schedule/fixtures/[id]/participation, /timetable, /reports/compliance, /injuries, /injuries/new, /injuries/[id], /injuries/rehab-groups, /injuries/team-allocation, /leaderboards/new, /leaderboards/manage, /leaderboards/[id], /nutrition, /programmes, /programmes/exercises, /programmes/[id], /programmes/[id]/athlete/[id], /reports/athlete, /reports/gps, /reports/injuries, /reports/squad, /reports/testing, /testing, /testing/[id], /testing/[id]/[id], /settings/exports, /settings/groups, /settings/groups/new, /settings/groups/[id], /settings/notifications, /settings/profile, /settings/thresholds, /settings/thresholds/new, /schedule/fixtures/[id], /programmes/new, /programmes/proposals, /settings/subject-access, /settings/subject-access/[id]/review, /nutrition/new, /squad/new, /analytics, /settings/audit, /settings/imports, /settings/setup, /settings/users, /settings/users/bulk-invite, /settings/users/[id], /platform/sign-in-probes; roles: basic_sport_scientist, coach, medic, nutritionist, sc, sport_scientist.
- where: src/styles/base.css:532 `.nav-signout { padding: 0 }`; src/styles/base.css:534 `.nav-signout { font-size: var(--fs-13) }`; src/styles/base.css:546 `.nav-signout .ic { width: 17px }`
- proposed fix: `min-height: var(--tap-min)` on the control; never into a neighbour, never overlapping another target.

### T23. `button.back-btn` — short side 29px

- measured: 72×29px (658), 82×34px (122). Text: "Back".
- at 390: clears 44px or absent; at 1440: /squad, /squad/[id], /flags, /reports, /reports/athlete/[id], /denied, /reports/training-load, /reports/match, /leaderboards, /settings, /settings/plan, /settings/club, /settings/retention, /squad/[id]/availability, /squad/[id]/gym, /squad/[id]/nutrition, /squad/[id]/wellness, /schedule, /schedule/new, /schedule/planner, /schedule/planner/new, /schedule/planner/[id], /schedule/planner/apply, /schedule/[id], /schedule/fixtures/new, /schedule/fixtures/[id]/participation, /timetable, /reports/compliance, /injuries, /injuries/new, /injuries/[id], /injuries/rehab-groups, /injuries/team-allocation, /leaderboards/new, /leaderboards/manage, /leaderboards/[id], /nutrition, /programmes, /programmes/exercises, /programmes/[id], /programmes/[id]/athlete/[id], /reports/athlete, /reports/gps, /reports/injuries, /reports/squad, /reports/testing, /testing, /testing/[id], /testing/[id]/[id], /settings/exports, /settings/groups, /settings/groups/new, /settings/groups/[id], /settings/notifications, /settings/profile, /settings/thresholds, /settings/thresholds/new, /schedule/fixtures/[id], /programmes/new, /settings/subject-access, /settings/subject-access/[id]/review, /nutrition/new, /programmes/proposals, /squad/new, /analytics, /settings/audit, /settings/imports, /settings/setup, /settings/users, /settings/users/bulk-invite, /settings/users/[id], /platform/sign-in-probes; roles: basic_sport_scientist, coach, medic, nutritionist, sc, sport_scientist.
- where: src/styles/base.css:11099 `.back-btn { padding: var(--sp-6) var(--sp-12) var(--sp-6) var(--sp-8) }`; src/styles/base.css:11105 `.back-btn { font-size: var(--fs-13) }`; src/styles/base.css:11124 `.back-btn svg { width: 15px }`
- proposed fix: `min-height: var(--tap-min)` on the control; never into a neighbour, never overlapping another target.

### T24. `button.pp-ack-btn` — short side 29px

- measured: 109×29px (32). Text: "Acknowledge flag for Dan Okonkwo".
- at 390: clears 44px or absent; at 1440: /squad/[id]; roles: coach, medic, nutritionist, sc, sport_scientist.
- where: src/styles/base.css:4813 `.pp-ack-btn { padding: var(--sp-6) var(--sp-14) }`; src/styles/base.css:4814 `.pp-ack-btn { font-size: var(--fs-13) }`
- proposed fix: `min-height: var(--tap-min)` on the control; never into a neighbour, never overlapping another target.

### T26. `button.plan-switch` — short side 30px

- measured: 168×30px (2). Text: "Preview this club on the Basic plan".
- at 390: clears 44px or absent; at 1440: /settings/club; roles: sport_scientist.
- where: src/components/PlanPreviewSwitch/PlanPreviewSwitch.tsx:49 (class `plan-switch`; no base.css rule sets height/min-height/padding/padding-block/padding-top/width/min-width/font-size on it)
- proposed fix: `min-height: var(--tap-min)` on the control; never into a neighbour, never overlapping another target.

### T27. `button.sg-segment` — short side 31px

- measured: 62×31px (4), 54×31px (4). Text: "Read", "Edit".
- at 390: clears 44px or absent; at 1440: /schedule; roles: coach, sport_scientist.
- where: src/styles/base.css:8767 `.sg-segment { padding: var(--sp-8) var(--sp-16) }`; src/styles/base.css:8769 `.sg-segment { font-size: var(--fs-13) }`; src/styles/base.css:14555 `.main .sg-viewtab, .main .squad-chip, .main .sg-segment, .main .btn-ghost-pill { min-height: var(--tap-min) }`
- proposed fix: `min-height: var(--tap-min)` on the control; never into a neighbour, never overlapping another target.

### T28. `div.lbw-segmented > button[role=tab]` — short side 31px

- measured: 69×31px (10), 110×31px (10), 85×31px (10). Text: "Result", "Improvement", "Standard".
- at 390: clears 44px or absent; at 1440: /leaderboards; roles: coach, medic, nutritionist, sc, sport_scientist.
- where: src/styles/base.css:8319 `.lbw-segmented { padding: var(--sp-4) }`; src/styles/base.css:8324 `.lbw-segmented button { font-size: var(--fs-13) }`; src/styles/base.css:8326 `.lbw-segmented button { padding: var(--sp-8) var(--sp-16) }`
- proposed fix: `min-height: var(--tap-min)` on the control; never into a neighbour, never overlapping another target.

### T29. `button.nutr-athlete-row` — short side 31px

- measured: 1054×31px (256), 1054×41px (240). Text: "Okonkwo, DanTrending below78.078–112 kg ", "Barnes, JamesOverrideTrending above105.0", "Tameifuna, ViliamiTrending above124.7114".
- at 390: clears 44px or absent; at 1440: /nutrition; roles: coach, medic, nutritionist, sc, sport_scientist.
- where: src/styles/base.css:7790 `.nutr-athlete-row { width: 100% }`; src/styles/base.css:7793 `.nutr-athlete-row { padding: var(--sp-8) var(--sp-6) }`
- proposed fix: `min-height: var(--tap-min)` on the control; never into a neighbour, never overlapping another target.

### T30. `a.sg-weeknav-btn` — short side 32px

- measured: 32×32px (80). Text: "Previous week", "Next week".
- at 390: clears 44px or absent; at 1440: /schedule; roles: coach, medic, nutritionist, sc, sport_scientist.
- where: src/styles/base.css:8790 `.sg-weeknav-btn { width: 32px }`; src/styles/base.css:8791 `.sg-weeknav-btn { height: 32px }`; src/styles/base.css:14548 `.main .sg-weeknav-btn { min-width: var(--tap-min) }`
- proposed fix: `min-width: var(--tap-min); min-height: var(--tap-min)` on the control (an `<a>` is inline — `display: inline-flex` first, or `min-height` has no effect); never into a neighbour, never overlapping another target.

### T32. `button.btn-ghost-pill` — short side 33px

- measured: 56×33px (8). Text: "Edit".
- at 390: clears 44px or absent; at 1440: /squad/[id]; roles: basic_sport_scientist, coach, medic, sport_scientist.
- where: src/styles/base.css:4410 `.btn-ghost-pill { padding: var(--sp-8) var(--sp-16) }`; src/styles/base.css:4411 `.btn-ghost-pill { font-size: var(--fs-13) }`; src/styles/base.css:14555 `.main .sg-viewtab, .main .squad-chip, .main .sg-segment, .main .btn-ghost-pill { min-height: var(--tap-min) }`
- proposed fix: `min-height: var(--tap-min)` on the control; never into a neighbour, never overlapping another target.

### T33. `button.btn-ghost-pill[aria-disabled]` — short side 33px — **aria-disabled, in scope**

- measured: 56×33px (4). Text: "Edit".
- at 390: clears 44px or absent; at 1440: /squad/[id]; roles: nutritionist, sc.
- where: src/styles/base.css:4410 `.btn-ghost-pill { padding: var(--sp-8) var(--sp-16) }`; src/styles/base.css:4411 `.btn-ghost-pill { font-size: var(--fs-13) }`; src/styles/base.css:14555 `.main .sg-viewtab, .main .squad-chip, .main .sg-segment, .main .btn-ghost-pill { min-height: var(--tap-min) }`
- proposed fix: `min-height: var(--tap-min)` on the control; never into a neighbour, never overlapping another target.

### T34. `button.sg-btn-add` — short side 35px

- measured: 93×35px (4). Text: "+ Session".
- at 390: clears 44px or absent; at 1440: /schedule; roles: coach, sport_scientist.
- where: src/styles/base.css:8900 `.sg-btn-add { padding: var(--sp-10) var(--sp-18) }`; src/styles/base.css:8901 `.sg-btn-add { font-size: var(--fs-13) }`; src/styles/base.css:14524 `.main .back-btn, .main .btn-ghost, .main .btn-primary, .main select, .main .sg-stepper-btn, .main .sg-btn-remove, .main .sg-btn-add, .main .sg-btn-publish, .main .sg-btn-published, .main .sg-btn-discard, .main .nutr-stepper-btn, .main .lbw-segmented button, .main .theme-seg-btn, .main .reorder-btn { min-height: var(--tap-min) }`
- proposed fix: `min-height: var(--tap-min)` on the control; never into a neighbour, never overlapping another target.

### T37. `button.btn-primary.nutr-new-plan-btn` — short side 35px

- measured: 230×35px (4). Text: "New plan".
- at 390: clears 44px or absent; at 1440: /nutrition; roles: nutritionist, sport_scientist.
- where: src/styles/base.css:776 `.btn-primary { padding: var(--sp-14) var(--sp-20) }`; src/styles/base.css:777 `.btn-primary { min-height: var(--tap-min) }`; src/styles/base.css:778 `.btn-primary { font-size: var(--fs-15) }`
- proposed fix: `min-height: var(--tap-min)` on the control; never into a neighbour, never overlapping another target.

### T40. `button.rhead-tab[role=tab]` — short side 36px

- measured: 99×36px (28), 97×36px (20), 78×36px (20), 83×36px (10), 140×36px (10), +1 more sizes. Text: "Summary", "By athlete", "By day".
- at 390: clears 44px or absent; at 1440: /reports/compliance, /reports/injuries, /reports/testing; roles: coach, medic, nutritionist, sc, sport_scientist.
- where: src/styles/base.css:13849 `.rhead-tab { padding: var(--sp-10) var(--sp-18) }`; src/styles/base.css:13852 `.rhead-tab { font-size: var(--fs-14) }`
- proposed fix: `min-height: var(--tap-min)` on the control; never into a neighbour, never overlapping another target.

### T41. `a.btn-ghost-pill` — short side 37px

- measured: 83×37px (12), 148×37px (12), 102×37px (10), 107×37px (6), 91×37px (4). Text: "‹Squad", "Availability history ›", "+ Log injury".
- at 390: clears 44px or absent; at 1440: /squad/[id]; roles: basic_sport_scientist, coach, medic, nutritionist, sc, sport_scientist.
- where: src/styles/base.css:4410 `.btn-ghost-pill { padding: var(--sp-8) var(--sp-16) }`; src/styles/base.css:4411 `.btn-ghost-pill { font-size: var(--fs-13) }`; src/styles/base.css:14555 `.main .sg-viewtab, .main .squad-chip, .main .sg-segment, .main .btn-ghost-pill { min-height: var(--tap-min) }`
- proposed fix: `min-height: var(--tap-min)` on the control (an `<a>` is inline — `display: inline-flex` first, or `min-height` has no effect); never into a neighbour, never overlapping another target.

### T42. `button.nutr-daytype-item` — short side 37px

- measured: 230×37px (48). Text: "Training day6.0 g/kg", "Match day7.5 g/kg", "Rest day3.5 g/kg".
- at 390: clears 44px or absent; at 1440: /nutrition; roles: coach, medic, nutritionist, sc, sport_scientist.
- where: src/styles/base.css:7377 `.nutr-plan-item, .nutr-daytype-item { padding: var(--sp-10) var(--sp-12) }`; src/styles/base.css:7385 `.nutr-plan-item, .nutr-daytype-item { width: 100% }`; src/styles/base.css:7391 `.nutr-daytype-item { padding: var(--sp-10) var(--sp-12) }`
- proposed fix: `min-height: var(--tap-min)` on the control; never into a neighbour, never overlapping another target.

### T45. `button.rhead-chip` — short side 39px

- measured: 101×39px (240), 142×39px (122), 80×39px (122), 82×39px (118). Text: "✓Whole squad", "Forwards", "Backs".
- at 390: clears 44px or absent; at 1440: /reports/training-load, /reports/match, /schedule, /reports/compliance, /reports/gps, /reports/injuries, /reports/squad, /reports/testing; roles: basic_sport_scientist, coach, medic, nutritionist, sc, sport_scientist.
- where: src/styles/base.css:13746 `.rhead-chip { padding: var(--sp-10) var(--sp-20) }`; src/styles/base.css:13752 `.rhead-chip { font-size: var(--fs-14) }`
- proposed fix: `min-height: var(--tap-min)` on the control; never into a neighbour, never overlapping another target.

### T46. `a.nav-item` — short side 40px

- measured: 235×40px (6504). Text: "Dashboard", "Squad overview", "Schedule".
- at 390: clears 44px or absent; at 1440: /dashboard, /squad, /squad/[id], /flags, /reports, /reports/athlete/[id], /denied, /reports/training-load, /reports/match, /leaderboards, /settings, /settings/plan, /settings/club, /settings/retention, /squad/[id]/availability, /squad/[id]/gym, /squad/[id]/nutrition, /squad/[id]/wellness, /schedule, /schedule/new, /schedule/planner, /schedule/planner/new, /schedule/planner/[id], /schedule/planner/apply, /schedule/[id], /schedule/fixtures/new, /schedule/fixtures/[id]/participation, /timetable, /reports/compliance, /injuries, /injuries/new, /injuries/[id], /injuries/rehab-groups, /injuries/team-allocation, /leaderboards/new, /leaderboards/manage, /leaderboards/[id], /nutrition, /programmes, /programmes/exercises, /programmes/[id], /programmes/[id]/athlete/[id], /reports/athlete, /reports/gps, /reports/injuries, /reports/squad, /reports/testing, /testing, /testing/[id], /testing/[id]/[id], /settings/exports, /settings/groups, /settings/groups/new, /settings/groups/[id], /settings/notifications, /settings/profile, /settings/thresholds, /settings/thresholds/new, /schedule/fixtures/[id], /programmes/new, /programmes/proposals, /settings/subject-access, /settings/subject-access/[id]/review, /nutrition/new, /squad/new, /analytics, /settings/audit, /settings/imports, /settings/setup, /settings/users, /settings/users/bulk-invite, /settings/users/[id], /platform/sign-in-probes; roles: basic_sport_scientist, coach, medic, nutritionist, sc, sport_scientist.
- where: src/styles/base.css:444 `.nav-item { padding: var(--sp-10) var(--sp-24) var(--sp-10) var(--sp-20) }`; src/styles/base.css:445 `.nav-item { font-size: var(--fs-14) }`; src/styles/base.css:456 `.nav-item { width: 100% }`
- proposed fix: `min-height: var(--tap-min)` on the control (an `<a>` is inline — `display: inline-flex` first, or `min-height` has no effect); never into a neighbour, never overlapping another target.

### T47. `span.sg-viewtab[role=tab]` — short side 40px

- measured: 101×40px (40). Text: "Week plan".
- at 390: clears 44px or absent; at 1440: /schedule; roles: coach, medic, nutritionist, sc, sport_scientist.
- where: src/styles/base.css:14078 `.rhead-tabrow .sg-viewtab { padding: var(--sp-10) var(--sp-18) }`; src/styles/base.css:14081 `.rhead-tabrow .sg-viewtab { font-size: var(--fs-14) }`; src/styles/base.css:14555 `.main .sg-viewtab, .main .squad-chip, .main .sg-segment, .main .btn-ghost-pill { min-height: var(--tap-min) }`
- proposed fix: `min-height: var(--tap-min)` on the control; never into a neighbour, never overlapping another target.

### T48. `a.sg-viewtab[role=tab]` — short side 40px

- measured: 74×40px (40). Text: "Today".
- at 390: clears 44px or absent; at 1440: /schedule; roles: coach, medic, nutritionist, sc, sport_scientist.
- where: src/styles/base.css:14078 `.rhead-tabrow .sg-viewtab { padding: var(--sp-10) var(--sp-18) }`; src/styles/base.css:14081 `.rhead-tabrow .sg-viewtab { font-size: var(--fs-14) }`; src/styles/base.css:14555 `.main .sg-viewtab, .main .squad-chip, .main .sg-segment, .main .btn-ghost-pill { min-height: var(--tap-min) }`
- proposed fix: `min-height: var(--tap-min)` on the control (an `<a>` is inline — `display: inline-flex` first, or `min-height` has no effect); never into a neighbour, never overlapping another target.

### T50. `span > input[type=radio]` — short side 41px

- measured: 1110×41px (12); hit box read via label. Text: "on".
- at 390: clears 44px or absent; at 1440: /settings/thresholds/new; roles: coach, sport_scientist.
- where: (no rule located by class; inherited from an ancestor outside the recorded path)
- proposed fix: `min-height: var(--tap-min)` on the control; never into a neighbour, never overlapping another target.

### T51. `select.exlib-cat-select` — short side 42px

- measured: 358×42px (10). Text: "Filter by category".
- at 390: clears 44px or absent; at 1440: /programmes/exercises; roles: coach, medic, nutritionist, sc, sport_scientist.
- where: src/styles/base.css:12475 `.exlib-cat-select { width: 100% }`; src/styles/base.css:12476 `.exlib-cat-select { height: 100% }`; src/styles/base.css:12477 `.exlib-cat-select { padding: 0 var(--sp-16) }`
- proposed fix: `min-height: var(--tap-min)` on the control; never into a neighbour, never overlapping another target.


### Natively disabled, listed and not counted

### T13. `button.sg-btn-published` — short side 17px (natively disabled)

- measured: 70×17px (4). Text: "Published".
- at 390: clears 44px or absent; at 1440: /schedule; roles: coach, sport_scientist.
- where: src/styles/base.css:8846 `.sg-btn-published { font-size: var(--fs-13) }`; src/styles/base.css:14524 `.main .back-btn, .main .btn-ghost, .main .btn-primary, .main select, .main .sg-stepper-btn, .main .sg-btn-remove, .main .sg-btn-add, .main .sg-btn-publish, .main .sg-btn-published, .main .sg-btn-discard, .main .nutr-stepper-btn, .main .lbw-segmented button, .main .theme-seg-btn, .main .reorder-btn { min-height: var(--tap-min) }`
- proposed fix: `min-height: var(--tap-min)` on the control; never into a neighbour, never overlapping another target.

### T20. `button.btn-ghost` — short side 24px (natively disabled)

- measured: 24×32px (128), 54×32px (64), 80×32px (48), 38×44px (40). Text: "Previous page", "Next page", "+ session".
- at both widths: /reports/athlete/[id], /schedule/planner/[id]; roles: basic_sport_scientist, coach, medic, sc, sport_scientist.
- where: src/styles/base.css:803 `.btn-ghost { padding: var(--sp-10) var(--sp-16) }`; src/styles/base.css:804 `.btn-ghost { min-height: var(--tap-min) }`; src/styles/base.css:805 `.btn-ghost { font-size: var(--fs-13) }`
- proposed fix: `min-width: var(--tap-min); min-height: var(--tap-min)` on the control; never into a neighbour, never overlapping another target.

### T31. `button.nutr-stepper-btn` — short side 32px (natively disabled)

- measured: 32×32px (128), 32×44px (128). Text: "Decrease protein", "Increase protein", "Decrease carbohydrate".
- at both widths: /nutrition; roles: coach, medic, nutritionist, sc, sport_scientist.
- where: src/styles/base.css:7539 `.nutr-stepper-btn { width: 32px }`; src/styles/base.css:7540 `.nutr-stepper-btn { min-height: 32px }`; src/styles/base.css:7545 `.nutr-stepper-btn { font-size: var(--fs-16) }`
- proposed fix: `min-width: var(--tap-min); min-height: var(--tap-min)` on the control; never into a neighbour, never overlapping another target.


Distinct tap-target defects: 51.

### Links inside running text, listed and not counted (WCAG 2.5.5 and 2.5.8 exempt an inline link)

- `p.eyebrow > a` — 458 measurements, 40–…×13px; /reports/athlete/[id] (20), /squad/[id]/availability (20), /squad/[id]/nutrition (20), /squad/[id]/wellness (20), /schedule/[id] (20), /programmes/[id]/athlete/[id] (20), /testing/[id]/[id] (20), /programme/nutrition (16), /squad/[id]/gym (16), /settings/groups (16), /settings/thresholds (16), /leaderboards/manage (14), /squad/[id] (12), /settings/club (12), /leaderboards/[id] (10), /programmes/exercises (10), /programmes/[id] (10), /testing/[id] (10), /settings/groups/[id] (10), /settings/notifications (10), /settings/profile (10), /schedule/fixtures/[id] (10), /schedule/fixtures/[id]/participation (8), /injuries/new (8), /injuries/[id] (8), /injuries/rehab-groups (8), /injuries/team-allocation (8), /reports/athlete (8), /settings/exports (8), /leaderboards/new (6), /programmes/new (6), /settings/plan (4), /settings/retention (4), /schedule/new (4), /schedule/planner (4), /schedule/planner/new (4), /schedule/planner/[id] (4), /schedule/planner/apply (4), /schedule/fixtures/new (4), /settings/groups/new (4), /settings/thresholds/new (4), /programmes/proposals (4), /settings/subject-access (4), /nutrition/new (4), /settings/users/bulk-invite (4), /settings/users/[id] (4), /settings/subject-access/[id]/review (2), /settings/audit (2), /settings/setup (2), /settings/users (2).
- `a.linklike` — 48 measurements, 74–…×14px; /reports/match (32), /me/data-consent (16).
- `p.dash-flags-thresholds > a` — 12 measurements, 48–…×14px; /dashboard (12).
- `span > a` — 136 measurements, 208–…×15px; /settings (136).
- `p.tiny > a` — 20 measurements, 68–…×15px; /settings/club (12), /settings/plan (8).
- `p.cap > a` — 28 measurements, 66–…×15px; /leaderboards/manage (28).
- `a.tiny` — 112 measurements, 8–…×15px; /reports/testing (112).
- `p.ap-figure > a` — 8 measurements, 169–…×15px; /analytics (8).
- `p.import-sub > a` — 4 measurements, 73–…×15px; /settings/imports (4).

## Class 3 — colour as the only carrier of meaning (hand review)

The machine list at the end of this section is every small painted element without text of its own, across all eleven accounts. Each was read in its markup and its stylesheet rule and judged against one question: if the hue were removed, would the meaning survive through a word, a shape, a fill-versus-outline, a position or a number? The constitution's four meanings — rank tint, flag badges, deviation bars, neutral status text — stay as they are in every proposal below: the fix for this class is always to **add** a label, a shape or a position, never to change the colour. The five defects are the baseline's five, each re-read in the code at this commit (the line numbers are tonight's).

### Defects

**3.1 Session type on the dashboard week strip — colour only.**
`src/app/(staff)/dashboard/page.tsx:411` renders each activity as
`<span className="dash-week-act-dot" style={{ background: PIP_COLOR[a.type] }} aria-hidden="true" />`
followed by the session's **title**, not its type. The type (training, gym, rehab, testing, match, recovery, meeting — seven hues, `PIP_COLOR`) is carried by the dot alone; the legend names the hues but the row itself has no word. A title like "Lower A" or "Speed and power" does not say gym or testing. Surfaces: `/dashboard`, the sport scientist, coach and medic (the S&C's and nutritionist's versions have no week strip), both widths. Proposed fix: the type word after the title in the row (`enumLabel(a.type)` as a `.dash-week-act-type` caption, existing tokens), or the pip replaced by the same word.

**3.2 Session type on the schedule grid blocks — colour only for sighted readers.**
`src/components/ScheduleGrid/TimeGrid.tsx:323–370`: a block's `--tone`/`--bc` come from `TYPE_STYLE[b.type]` (`src/lib/scheduleGeometry.ts`) and the block prints time, title and groups. The type is in the `aria-label` (`${b.title}, ${enumLabel(b.type)}, …`, line 328), so a screen reader has it and a sighted reader without hue discrimination does not. The legend row names the hues. Same on the phone day list: `src/components/ScheduleGrid/SchedulePhoneDay.tsx:109` `<span className="sg-phone-row-dot" style={{ background: TYPE_STYLE[s.type].tone }} aria-hidden="true" />` before `{s.title || enumLabel(s.type)}` — the type word appears only when the session has no title. Surfaces: `/schedule`, every staff role, both widths (the timetable's cards print the type word and are fine). Proposed fix: the type word in the block's row line where `b.height >= 63` (the threshold that admits the group line, line 363) and always on the phone row's meta line; no colour change.

**3.3 "Not fully available" on the dashboard — modified versus out by dot colour.**
`src/components/DashboardHeadlineStats/DashboardHeadlineStats.tsx:372–378`: the expanded Available tile lists names, each with `<span className="dash-squad-dot" style={{ background: 'var(--warn)' }} />` for modified and `var(--bad)` for unavailable, and nothing else on the row. The head line gives the two counts ("2 modified, 1 out"), so the reader knows how many of each, not which. Surfaces: `/dashboard`, every staff role with the tile. Proposed fix: the status word as neutral status text on each row ("Modified" / "Out"), the dot kept.

**3.4 Flag severity on the dashboard attention rows — the row's left edge.**
`src/components/DashboardFlagsPanel/DashboardFlagsPanel.tsx:159` moves severity to the row's own left edge (`border-left-color` by `data-severity`). Collapsed, the row shows name, position, the measurement and how long unreviewed; the severity word appears only inside the expanded detail as a pill. Surfaces: `/dashboard`, every staff role that sees flags. Proposed fix: the severity pill (`Pill`, glyph and word, the existing component) in `.dash-flags-meta` on the collapsed row; the edge colour stays.

**3.5 GPS scatter bands — dot colour alone.**
`src/components/TrainingScatter/TrainingScatter.tsx:109–125`: each athlete's dot takes `BAND_STYLE[p.band]` fill and stroke for far/near/mid/low, the legend names the four hues, and the dot's `aria-label` (line 125) carries name, unit and the two distances but **not the band**. Surfaces: `/reports/gps` (Premium; the sport scientist, coach, medic and S&C reached it — the nutritionist and the Basic plan land on the denied screen). Proposed fix: the band word in the `aria-label`, and one shape per band (circle, square, diamond, ring — `border-radius`/`border-style` on the existing box, no new token); the label beside the dot already prints the surname for the labelled set.

### Reviewed and passing, with the cue that carries the meaning

**The athlete app, reviewed for the first time (four accounts, every athlete
route).** The machine list for the athlete app is one entry: the wellness
chart's data dots (`src/components/WellnessChart/WellnessChart.tsx:394–396`,
`div.chart > svg[role=img] > g > circle`), coloured `--warn` above the
athlete's own band, `--bad` below it, `--accent` inside — on `/my-data` at
both widths. **Reviewed and passing.** The dot's height against the drawn
band is the position itself (the band is the grey strip the dots sit in, above
or below), the chart's own comment records the choice of dots over up/down
triangles because "the triangle direction duplicated what the dot's own height
already says", and the caption under the chart counts the out-of-band days in
words. The chart is `role="img"` with a sentence naming the days submitted and
the staff notes. Nothing else on the athlete app paints meaning without a word:
the availability card is a tone family with the status word in it, the to-do
rows are names, the week strip's day kinds are words ("Rest", "MD+2"), the gym
logger's set chips carry a tick or a number, and the queue banner's glyph
(`☁`, `!`) sits beside its sentence.

- **Leaderboard wall tints** (`LeaderboardWall.tsx`, `lib/leaderboardWallMath.ts`): rank tint carries `#n`; improvement carries `▲`/`▼` and the value; standard carries `✓` or the shortfall figure. The four meanings hold.
- **Athlete week strip** (`src/app/(athlete)/today/page.tsx`, `.wk-day`): training is a fill, match a border, recovery a half-weight fill, rest nothing; the kind is a visually-hidden word and the third line prints the kind where there is no MD label. Fill / border / nothing survive without hue.
- **Nutrition check-in strip** (`SelectedAthleteCard.tsx`): Y / R / N letters in the cells.
- **Nutrition logging strip** (`SelectedAthleteCard.tsx`, `.nutr-logging-swatch`): logged is a filled square, not-logged a hollow one — fill versus outline is a second channel; the caption under it gives the count.
- **Nutrition figures outside ±5 %** (`.nutr-col-num`, now `--warn-pill-text` after `42ad4ff`): the totals warning sentence names the deviation and the range marker's position on its track carries it; the colour is the third channel.
- **GPS heat cells** (`reports/gps/page.tsx`): the number is printed in every cell; the heat is a toggleable overlay.
- **CR10 / Today RPE selection**: the selected cell inverts to a solid accent block with white figures — a luminance change, not a hue.
- **Pills** (`components/Pill`): glyph and word are required, not optional.
- **Injury stage ladder** (`StageLadder.tsx`): state words Done / Now / Next / Later.
- **Group swatches** (`GroupSwatch.tsx`): always beside the group's name, by the component's own contract.
- **Availability legend on the athlete-report picker** (`reports/athlete/page.tsx`): counts with words ("4 unavailable"); the rows carry a pill.
- **Schedule legend and comparison bars** (`.sg-legend-swatch` beside its word — "Meeting", "Recovery" — and `.sg-cmp-bar` whose length is the figure).
- **The test history chart's points** (`/testing/[id]/[id]`, `svg[role=img] > g > circle`): the chart is `role="img"` with its sentence; the points are the values at their heights; the table under it prints every figure.
- **Bars and markers** (`.lb-fill`, `.pick-wellness-fill`, `.pp-bench-fill`, `.pc-marker`, `.nutr-range-marker`, `.dash-track-bar-fill`, the sparkline points): length or position is the meaning.

### The machine list the review started from

- `span.nutr-col-num > div > div.nutr-range-track > div.nutr-range-marker` × 512 — /nutrition; roles coach, medic, nutritionist, sc, sport_scientist; beside "".
- `div.sg-grid-inner > div.sg-legend-row > span.sg-legend-item > span.sg-legend-swatch` × 280 — /schedule; roles coach, medic, nutritionist, sc, sport_scientist; beside "Meeting".
- `div.card > div.sg-cmp-row > span.sg-cmp-type > span.sg-cmp-bar` × 280 — /schedule; roles coach, medic, nutritionist, sc, sport_scientist; beside "Recovery".
- `div.card.cmpl-table > div.lb-row > span.lb-track > span.lb-fill` × 250 — /leaderboards/[id]; roles coach, medic, nutritionist, sc, sport_scientist; beside "".
- `div > div.nutr-logging-strip > div.nutr-logging-cell > div.nutr-logging-swatch` × 224 — /nutrition; roles coach, medic, nutritionist, sc, sport_scientist; beside "S".
- `a.pick-row > span.pick-wellness > span.pick-wellness-track > span.pick-wellness-fill` × 96 — /reports/athlete; roles coach, medic, sc, sport_scientist; beside "".
- `div.lbw-controls-row > div.lbw-legend > span.lbw-legend-item > span.lbw-swatch` × 80 — /leaderboards; roles coach, medic, nutritionist, sc, sport_scientist; beside "Bottom of the group".
- `div.card > svg[role=img] > g > circle` × 80 — /testing/[id]/[id]; roles coach, medic, nutritionist, sc, sport_scientist; beside "".
- `div > span.pick-legend > span > i` × 48 — /reports/athlete; roles coach, medic, sc, sport_scientist; beside "4 unavailable".
- `div.chart > svg[role=img] > g > circle` × 40 — /my-data, /squad/[id]/wellness; roles athlete_adult, athlete_declined, athlete_minor_kai, athlete_minor_niall, coach, medic, nutritionist, sc, sport_scientist; beside "".
- `section.card.pp-card > div.pc-row > div.pc-track > div.pc-marker` × 36 — /squad/[id]/nutrition; roles coach, medic, nutritionist, sc, sport_scientist; beside "".
- `div > div > svg.nutr-sparkline > circle` × 32 — /nutrition; roles coach, medic, nutritionist, sc, sport_scientist; beside "".
- `div.dash-track > div > div.dash-track-bar-outer > div.dash-track-bar-fill` × 20 — /dashboard; roles coach, medic, nutritionist, sc, sport_scientist; beside "".
- `section.card.pp-card > div.pp-bench-row > div.pp-bench-bar > div.pp-bench-fill` × 20 — /squad/[id]; roles coach, medic, nutritionist, sc, sport_scientist; beside "".

## Other findings, reported and not fixed

Things the sweep or its reading turned up that are none of the three classes, or are a class defect with a cause outside the rules. Nothing here was changed.

1. **The Next.js default 404 renders black on the athlete shell's dark ground (C1, C2).** A minor or a declined athlete opening a gym session that is not theirs (`/gym/[id]`, `/my-data/gym/[id]` — `notFound()` from `startOrGetSessionLog`) gets Next's own not-found page: "404" at 1.23:1 in dark, unthemed, with no way back. There is no `not-found.tsx` at `src/app/`, `src/app/(athlete)/` or `src/app/(staff)/`. The fix is a themed not-found page in each shell (the athlete one with the shell's Back), which is a new screen and a decision, not a token swap.
2. **`KNOWN_BELOW_AA` in `scripts/check-contrast.ts` is down to `accent` (dark)** and that entry's own text names the fix (`--accent-text` at the call sites: the staff phone tab bar's active tab, C10/C11). Once that swap lands the list should be empty and the guard should fail again if `--accent` is ever used as ink.
3. **The nutrition workspace's inert "Next week ›" marker** (`src/app/(staff)/nutrition/page.tsx:362`) is a `<span class="btn-ghost" aria-disabled="true" style={{ opacity: 0.4, pointerEvents: 'none' }}>`: `aria-disabled` on something that is not a control and cannot be reached, an inline opacity literal (`--o-disabled` is 0.45), and `pointer-events: none`, which is the pattern the constitution's BlockedButton rule replaced. The squad report's forward week arrow does the same (`src/app/(staff)/reports/squad/page.tsx:156`, `<span aria-disabled="true" data-disabled="true">`, faded to 0.5). Both are listed under the fades in Class 1 (C4, C6) and T22; the attribute says "disabled control" about a decoration.
4. **The staff `.btn-ghost` floor does not reach `<a class="btn-ghost">`.** `base.css:804` gives `.btn-ghost` `min-height: var(--tap-min)`, but a link is `display: inline`, where `min-height` has no effect — "Open team allocation →", "Review clinical notes →" and the import screen's export link measure 37px at every width (T44). The same class behaves differently on a `<button>` and an `<a>`, which is the kind of thing a source-level guard asserts as fixed.
5. **Two list rows squeeze to a 36px column at 390** — `a.load-row` on `/testing` (each test) and on the athlete's `/programme` ("Meal ideas ›"): the row's grid puts the link in a narrow column (36×152 and taller, T38). In Class 2 as a tap-target defect; noted here because the cause is layout, which the sweep may not change.
6. **The dead selector on the athlete week strip** (`src/styles/base.css:2404`, `.wk-day[data-kind='match'] .wk-day[data-kind='training'] …`, a descendant chain of four `.wk-day`s that never nest) still stands from the baseline; the rule has never applied. Either it wanted commas or it is leftover.
7. **The theme switch's own transition can fool a rendered-DOM measurement.** The first pass of this run measured the sidebar's dark ink against the light ground on two heavy pages, because `transition: color var(--t-state)` had not finished when the page was read. `scripts/a11y-sweep.mjs` now turns transitions off before measuring and waits for the root's `--bg` to change, and the two pages were re-measured. Worth knowing for the guard proposal above: a rendered check has to measure end states.

## What was not measured

Text painted by `::before`/`::after` and SVG `<text>`; an element positioned over something other than its ancestors (a badge floated across a card edge), which is measured against its ancestors' ground; and the two athlete-app dialogs that need a live session state (the gym logger's finish-early and correction sheets, the RPE row's send) — the route was measured in its landing state. The nutritionist's proposals screen, the analytics boards and the plan page were measured as the denied screen for the roles that cannot open them, which is the surface those roles see.

## Stop

The prompt's step 1 ends here: the list, before building. Nothing in `src/` changed for this document. The fix pass — one commit per class per app, staff and athlete separately, with the measured value before and after in both themes — starts from this list once it has been read, and every entry above that says "report and stop" or "a ruling is needed" waits for that ruling.

**Before step 2, the guard coverage pass (ruled 15 September, `decisions/decision-batch-2026-09-15-pm.md` item 5) has run.** The reporter behind this document (`scripts/a11y-sweep-report.mjs`) now counts its run files — eleven, or it fails — and every one of the 33 scripts under `scripts/` that enumerates the filesystem asserts the number it expected to find through `scripts/lib/coverage.mjs`; `scripts/test-guard-coverage.ts` (prebuild) holds them to it. Two silent skips were found by the pass itself: `test-athlete-token-audit` had listed a component that never existed (`TestSparkline`, a local function of my-data) and swallowed the missing directory for months, and `test-control-names-resolve` swallowed unreadable files and directories the same way.

## Step 2 — the fixes (15 September, night; the overnight queue's P3)

Isabella's four rulings on this list, applied one commit per class per app, staff and athlete separately, with the value before and after in both themes. "Before" is the entry above; "after" is measured on the same rendered element by the same tool (`scripts/a11y-sweep.mjs` run with its floor lifted so passing measurements are recorded too), and checked against the token arithmetic (`--muted` on each entry's composited ground) before the swap was chosen. Not touched, by the ruling: Class 3 (five defects) and the seven other findings, C1–C2 (the themed not-found page, built separately on 15 Sept), C8, C9, C12, C16, C42 and C46 — none of the four rulings names them, so they stand as listed.

### Class 1, staff app — `--faint` family, the four fades, the one-offs

**Ruling 1, the `--faint` / `--faint-on-tint` family: swap to `--muted` wherever the measurement fails, one move, both themes, grounds and tints untouched.** Applied at the rule the entry names; measured `--muted` on every ground the entry lists first. Three entries could not take `--muted` and are marked:

| Entry | Where | Before (light / dark) | Ink now | After (light / dark) |
|---|---|---|---|---|
| C13 `.dash-week-col-empty` | base.css | 3.08 / 3.73 on today's column (`--wash-accent-strong` over `--bg`) | **`--text`** — `--muted` measures 4.04 on that column and `--muted-on-tint` 4.49; nothing lighter than `--text` clears it. The entry's own proposed fix. | 11.35 / 9.81 |
| C14 `.pp-wellness-mini .v` | base.css (the entry's "where" pointed at `.step .val .v[data-empty]`; the rule that paints "76" is this one) | 3.20 / 3.85 on the hero's wash | **`--muted-on-tint`** — `--muted` is 4.17 on the hero's accent wash; the hero card already reads its captions in this context ink (the 15 Sept context-ink fix), and it is the ink ruling 4 names for the same wash in the athlete app | 4.64 / 5.63 |
| C15 `.pp-wellness-mini .l`, `.pp-detail-cell .l` | base.css | 3.20 / 3.85 | **`--muted-on-tint`**, as C14 | 4.64 / 5.63 |
| C17 `.rep-exports` | base.css | 3.52 / 3.59 | `--muted` | 4.59 / 5.25 |
| C18–C20 `.pp-flag-threshold`, `.pp-flag-evidence`, `.pp-flag-raised` | base.css | 3.70 / 3.91 | `--muted` | 4.83 / 5.72 |
| C23 `.dash-stat-label`, C24 and C32 `.dash-stat-foot` | base.css | — / 3.89 (C32 — / 3.97) | `--muted` | 5.50 / 5.69 (C32 5.66 / 5.82) |
| C25 `.lbw-sel-thead` (C26 `div.r` inherits it — the entry's "inline" attribution was wrong; no inline colour exists) | base.css | — / 3.89 | `--muted` | 5.50 / 5.69 |
| C27 `.lbw-leader-board`, C28 `.lbw-leader-unit` | base.css | — / 3.89 | `--muted` | 5.50 / 5.69 |
| C29, C31 the locked settings rows' label and count | `settings/page.tsx` (inline `--faint`) | 4.21 / 3.89 | `--muted` (ruling 1 — the cause is the ink, not a fade; the row carries no opacity) | 5.50 / 5.69 |
| C30 `.set-row-sub` | base.css | 4.21 / 3.89 | `--muted` | 5.50 / 5.69 |
| C33 `.launch .launch-head .eyebrow` (`/login` at 390) | base.css | 3.98 / — | `--muted` | 5.19 / 6.59 |
| C34 `.rhead-eyebrow` | base.css | 3.98 / — | `--muted` | 5.19 / 6.59 |
| C35 the athlete picker's legend count | `reports/athlete/page.tsx` (inline) | 3.98 / — | `--muted` | 5.19 / 6.59 |
| C36 "Page 1 of 54" (`settings/audit/page.tsx`), "( cm )" on a heavy test row (`reports/athlete/[athleteId]/page.tsx`) | inline | 3.98 / —; 4.48 / — | `--muted` | 5.19 / 6.59; 5.85 / 6.60 |
| C37 `table.tbl th` | base.css — the rule behind every table header; only the subject-access table sits on `--bg` | 3.98 / — | `--muted` (every table header in the staff app moves one step with it: 6.34 / 7.09 on a card, where `--faint` was passing at 4.86) | 5.19 / 6.59 |
| C41 `.inj-unit` | base.css | — / 4.11 | `--muted` | 5.99 / 6.02 |
| C43 `.pp-bench-meta` | base.css | 4.18 / 4.16 | `--muted` | 5.45 / 6.09 |
| C44 `.lb-gap` | base.css | 4.48 / 4.39 | `--muted` | 5.85 / 6.42 |
| C45 the training report's row sub-label on the highlighted row | `reports/gps/page.tsx` (inline) | 4.41 / — | `--muted` | 5.75 / 6.69 |

Not in the family and left as they were: every other inline `var(--faint)` in the staff app sits on a white card, where `--faint` measures 4.86 and the sweep did not fail it.

**Ruling 2, the faded aria-disabled controls (C4–C7): the fade stays for natively disabled controls only; an aria-disabled control takes a dedicated unavailable ink that reads as muted and passes 4.5 in both themes; the token is recorded in the design system.** The token is `--unavailable` (`src/styles/tokens.css`, dated; `docs/06-design-system.md` §2.3): light `#5d687b`, dark `#90a0cb`, derived the §3.7 way from the ink scale's hue and saturation as the lightest value clearing 4.5 on every flat ground an unavailable control sits on (light: 4.61 on `--bg`, 5.63 on `--surf`, 4.88 on `--surf2`, 4.51 on `--phone-bg`; dark: 5.32, 5.73, 4.60, 6.54). It sits between `--faint` and `--muted`, one visible step lighter than the enabled control's `--muted`. The fade rules were split: `:disabled` keeps `--o-disabled`, `[aria-disabled='true']` takes the ink.

| Entry | Where | Before (light / dark) | After (light / dark) |
|---|---|---|---|
| C4 `span.btn-ghost[aria-disabled]` "Next week ›" | `nutrition/page.tsx` (the inline `opacity: 0.4` → `color: var(--unavailable)`) | 1.74 / 2.27 | 4.61 / 5.32 |
| C5 `.btn-ghost-pill[aria-disabled]` "Edit" on the profile's status header | base.css | 1.83 / 2.33 | 4.64 / 5.63. The pill sits on the hero's accent wash, where `--unavailable` measures 4.1 and the only inks clearing 4.5 are `--muted-on-tint` and darker, so the ink is the same as an enabled pill's — and the cue is **shape** (Isabella's closing fix, 15 Sept): no border, no background, "Edit" as plain text beside the bordered chips; the reason on tap unchanged. Probed against every aria-disabled control on the pages that have one: no other ended up identical to its enabled peer. |
| C6 `.week-nav span[data-disabled]` "›" on /reports/squad | base.css (`--faint` at 0.5 → `--unavailable`, no fade) | 1.88 / 2.04 | 4.88 / 4.60 |
| C7 `.squad-chip[aria-disabled]` on /leaderboards/new | base.css | 2.00 / 2.59 | 5.63 / 5.73 |

`.lbw-chip-disabled` (the leaderboard wall's family chips when a family is unavailable — the same shape as C7, never rendered disabled by the fixture) and `[data-blocked]` (the BlockedButton pattern) fell outside the ruling's letter and were left on `--o-disabled` and `--muted`; both take `--unavailable` since Isabella's closing fix of 15 Sept — one state, one treatment. C8 and C12 (the report hub's faded cards for a role without access) are cards, not controls, and are not in the ruling.

**Ruling 4, the one-offs (staff).**

| Entry | Where | Before (light / dark) | Ink now | After (light / dark) |
|---|---|---|---|---|
| C3 `.pp-flag-value` | `PlayerProfileFlags.tsx` (inline tone colour → `--warn-pill-text`; the tone still marks the card's edge) | 1.48 / 2.49 | `--warn-pill-text` on every severity's wash (6.33 / 6.17 warn, 5.99 / 6.57 bad, 5.46 / 6.08 accent) | 6.33 / 6.17 |
| C10, C11 `.ph-tab[data-active]` — the staff phone tab bar's active tab at 390 | base.css | — / 2.87 | `--accent-text`, as `KNOWN_BELOW_AA`'s own note asked. The exemption stays, narrowed in its note: the athlete tab bar's active glyph (`.athlete-tab-glyph`, an icon on `currentColor`) still paints `--accent` as `color`. | 7.05 / 6.68 |

### Class 1, athlete app — the two family entries and the three sub-lines

**Ruling 1 (C21, C22)** and **ruling 4 (C38–C40: `--muted-on-tint` if it measures clean on the composited ground, `--text` if not — measured, not assumed).** `--muted-on-tint` on `--wash-accent` over `--phone-bg` is 4.55 light (dark aliases `--muted`, 6.88) — clean, so it is the swap; `--text` was not needed.

| Entry | Where | Before (light / dark) | Ink now | After (light / dark) |
|---|---|---|---|---|
| C21 `.gym-set-key` (the logger's not-reached set keys) | base.css | — / 3.89 | `--muted` | 5.50 / 5.69 |
| C22 `.legal-pending-ref` (the LEGAL-* placeholders on the consent screens) | base.css | 4.21 / 3.89 | `--muted` | 5.50 / 5.69 |
| C38 `.mute-switch-sub` | base.css | 4.09 / — | `--muted-on-tint` | 4.55 / 6.88 |
| C39 `p.mute-note` (`.tiny` gave it `--muted`; the rule now sets the ink) | base.css | 4.09 / — | `--muted-on-tint` | 4.55 / 6.88 |
| C40 `p.import-sub` on the consent screen's emphasised card | base.css, scoped as `.card[data-emphasis] .import-sub` — `.import-sub` itself is a staff-wide class that passes at 5.19 on the page ground and is unchanged | 4.09 / — | `--muted-on-tint` | 4.55 / 6.88 |

### Class 2, staff app — the text links join the floor, the controls get the floor themselves

**Ruling 3.** The fourteen text links join the selector list of the staff shell's 44px floor rule (`base.css`, "THE 44px FLOOR", the `@media (max-width: 767px)` block — phone widths, as ruling #8 scopes it), with the padding-and-negative-margin extension so nothing on the line moves; the eleven short controls get `min-height: var(--tap-min)` on themselves (`min-width` too for the two square week arrows), an inline `<a>` becoming `inline-flex` first so the floor takes; the native checkbox is measured through its wrapping label and the label carries the floor. Measured after at 390 by the same tool, with a probe for overlapping hit boxes on every affected route (none introduced; the pre-existing one is noted).

| Entry | Where | Before (short side) | After |
|---|---|---|---|
| T1 `.signin-return a` "Back to sign in" (/login/reset, /confirm) | joins the list, unscoped by `.main` — the sign-in pages are outside the staff shell | 86×15 | 86×44 |
| T6 `a.nm` on the testing sheet | `.main a.nm` | 56×19 | ≥44 tall |
| T8 `td.nm > a` (match, training-load, proposals) and T10 `p.nm > a` (users) | `.main .nm a` | 56×15 | ≥44 tall |
| T9 `a.attn-name` | `.main .attn-name` | 56×15 | ≥44 tall |
| T11 `p.flag-who > a` | `.main .flag-who a` | 60×16 | ≥44 tall |
| T14 `a.nutr-profile-link` "Profile ›" | `.main .nutr-profile-link`, plus the same balance sideways (`--sp-2` each side) because it was 41 wide | 41×17 | 45×44 |
| T16 `a.lbw-profile-link` | `.main .lbw-profile-link` | 45×19 | 45×44 |
| T17 `a.tst-manage` "Manage tests →" | `.main .tst-manage` | 358×19 | 358×44 |
| T19 `a.tr-heat-toggle` "Heat" | the same padding and negative margin, keeping its `inline-flex` (a track sits beside the word) | 76×22 | 76×46 |
| T15 `label.tiny > input[type=checkbox]` (the week-template builder's Wellness / RPE / Nutrition) | `.main label.tiny:has(> input[type='checkbox']) { min-height }` — the label is the hit box | 50×19 | ≥44 tall |
| T21, T22 the squad report's week arrows (`a` and the inert `span[aria-disabled]`) | `inline-flex`, `min-width` and `min-height` | 24×31 | 44×44 |
| T35 `.tr-mode-switch a` | `inline-flex` + `min-height` | 60×40 | ≥44 tall |
| T36 `a.chip-static` | `min-height` | 94×35 | ≥44 tall |
| T38 `a.load-row` on /testing | `.main a.load-row { min-height }` — see the note | 36×152 | **36×152, unchanged**: the row is squeezed to a 36px column by the page's own `1fr 340px` grid at 390 (Other findings, 5 — a layout defect, not a target size; `min-width` here would push into the neighbour column, which the ruling forbids). Stands. |
| T43 `a.tr-scope-chip` | `inline-flex` + `min-height` | 72×37 | ≥44 tall |
| T44 `a.btn-ghost` (groups, subject access, imports) | `.main a.btn-ghost { display: inline-flex }` so `.btn-ghost`'s existing `min-height` finally applies to a link | 59×37, 171×37 | ≥44 tall |
| T49 `a.set-row-btn` | `min-height` | 65×41 | ≥44 tall |

Not changed, and on record: the testing report's by-athlete table at 390 has a 37px row pitch, so the existing §0au extension on `table.tbl .nm` (which already reached these `a.nm` links — they were not short at 390) overlaps each name's hit box with the next by 7px; that pre-dates this pass and is a row-height question, not a selector. The remaining short targets on the re-swept routes are the report's own exempt groups: T20's natively disabled `btn-ghost` arrows, the planner's "+ session" (inline `minHeight: 32`), and the links inside running text (`a.linklike`, `th.r > a.tiny`, `p.import-sub > a`).

### Class 2, athlete app — the four back links and the three controls

Same rule, same block (the athlete shell is `.phone-body`; the staff floor's `.main` prefix does not reach it, so the athlete selectors join the list with their own prefix). The athlete app at 1440 renders as a phone frame, but ruling #8 counts 390 only, and these additions live in the same phone-width block as the rule they join.

| Entry | Where | Before (short side) | After |
|---|---|---|---|
| T2 `p.tiny > a` "← Today", "← Me" (/today/waiting, /me/notifications, /me/privacy, /me/status, /me/data-consent) | `.phone-body .tiny a` — joins the list; the same balance sideways too (`--sp-6` each side) because "← Me" was 34 wide and stands alone on its line | 34×15, 51×15 | 46×44, 63×44 |
| T3 `p.cap > a` "Manage who sees you on a leaderboard" | `.phone-body .cap a` | 229×15 | 229×44 |
| T4 `p > a` "Back to leaderboards" (the empty state on a board that is not available) | `.phone-body .empty a` | 122×15 | 122×44 |
| T18 `p.hist-more > a` "See all 28 days →" | `.phone-body .hist-more a` (the row was 44; the link inside it was 20) | 112×20 | 112×44 |
| T25 `.rsel-wrap select` "Period shown" on My data | `.phone-body .rsel-wrap select { min-height }` — the period field grows from 30 to 44 | 280×30 | 280×44 |
| T38 `a.load-row` "Meal ideas ›" on /programme | `.phone-body a.load-row { min-height }` | 312×39 | 312×44 |
| T39 `.theme-seg-btn` Light / Dark on Me | `.phone-body .theme-seg-btn { min-height }` (the track already had the floor; its segments did not) | 55×36 | 55×44 |

Verified at 390 by the same tool as the adult athlete and a minor (the board that is not available); the overlap probe found none. Screens re-checked by eye at 390: the back links, "See all", the period field, the theme segment and "Meal ideas ›" sit where they were.

### Class 3, staff app — a word or a shape beside every colour

Built after Classes 1 and 2 were done and verified, as the queue allowed ("deferred unless everything above is done and verified"), from the sheet rows' own recommendations. In every case the colour stays and a second channel is **added**; nothing is recoloured. Verified on dev at 1440 as the sport scientist (a gym session planted on scratch for the current week and removed after; HIE values set on one August session's GPS records so the scatter would draw, and put back to null).

| Defect | What was added | Where |
|---|---|---|
| 3.1 the dashboard week strip's session type | the type word after the title — "Lower A · Gym" — in `--text` at regular weight (a muted ink cannot clear today's strong wash, C13) | `dashboard/page.tsx`, `.dash-week-act-type` |
| 3.2 the schedule grid's block type, and the phone day list | the type word on the block's third line ahead of its groups where the block is 63px or taller ("Gym · Backs + Forwards"); the type word at the head of every titled phone row's meta line | `TimeGrid.tsx`, `SchedulePhoneDay.tsx` |
| 3.3 modified versus out on the Available tile's list | "Modified" / "Out" as neutral status text at the row's end, the dot kept | `DashboardHeadlineStats.tsx`, `.dash-stat-expand-status` |
| 3.4 severity on the collapsed attention rows | the severity `Pill` (glyph and word) in the row's meta column beside the unreviewed duration; the left edge stays | `DashboardFlagsPanel.tsx` |
| 3.5 the GPS scatter's bands | one shape per band — far a filled circle, near a filled square, mid a ring, low a dashed circle — on the dots and the legend alike, from the existing box's radius and border style; the band in words in every dot's label | `TrainingScatter.tsx`, `.tr-scatter-dot[data-band]` |

Not built, still on record: Other findings 5 (the `/testing` list squeezed to a 36px column at 390 by the page's own `1fr 340px` grid — a layout defect) and 6 (the dead `.wk-day` selector chain). Other findings 1–4 were closed by earlier commits tonight (the themed not-found page; the tab bar's `--accent-text`; the nutrition marker's fade, C4; the `<a class="btn-ghost">` floor, T44); 7 needed no change.
