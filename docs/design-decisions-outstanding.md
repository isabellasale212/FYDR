# Design decisions outstanding — every unbuilt item across `docs/designs`

**Living list, opened 2026-09-12.** One row per item that a design board proposed and the
build did not do. The source of each row is the flow's A/B/C/D record in
`docs/overnight-records-2026-09-12.md`; this sheet is the decision view of the same items.
When a new design folder lands, its record is written, its A items (and B items with a
nearest existing token) are built, and its C and D items are appended here in the same
format. When an item is built or declined, its row is struck through with the commit or
the decision, not deleted.

**Cost:** small = under an hour · medium = an afternoon · large = a day or more, or its own
brief. **⚠ migration** = needs a database migration. **⚠ permissions** = touches who may
read or write something (CLAUDE.md §5: the test comes before the rule).

**Reconciled 2026-09-14 (reviewer, bookkeeping pass) against `docs/decisions/` — `decision-batch-2026-09-13.md`, `design-system-adoption.md`, `absence-rule.md`, `premium-downgrade.md`, `lawful-basis-open.md`, `scope.md`.** Every row that was built is struck with its commit; every row that was decided is struck with the ruling and its date, or, where the ruling is in but the build is not, marked **DECIDED · unbuilt** so it counts as a build item and not a decision. The count of what is genuinely open is in `docs/architecture/to-do.md` §8.

**Reading the groups.** (a) is safe to say yes to as a batch. (b) is where a board disagrees
with something already decided and recorded — each needs an explicit "reverse" or "keep".
(c) needs data or a query we do not have today; the cost is mostly there. (d) cannot be
expressed in the 171 tokens; each is a proposed new token (or a substitute), per §0.01.

---

## Decisions — 2026-09-12 (Isabella), recorded by the reviewer

Read each row below against its group; a row not named here is **approved and queued**
("everything else in groups (a) to (d)").

**Struck — already live:** STAFF-SS-01 C5 (the last-admin guard at the database is migration
0101, on production since 2026-09-11; 0102 covers the self-grant).

**Declined:** ATH-ADULT-08 D1 (the answers' words stay "Yes / Roughly / No") · ATH-ADULT-12
D2 (the active segment stays the accent-filled pill) · ATH-ADULT-12 C4 (deltas stay "on last
week") · ATH-ADULT-12 B6 (hero eyebrows stay `--muted`) · ATH-ADULT-12 D5 (readiness stays
0–100) · **the 3px bar pattern** wherever it appears (ATH-ADULT-12 B5's `--border-accent-w`
row bar and the same bar in STAFF-SS-01 B1 — the rest of B1's substitutions stand) ·
ATH-ADULT-10 C2 (no confirmation before finishing early) · **motion tokens** (ATH-ADULT-09 B3
and any other `--ease-*` / duration proposal — the "no motion tokens" rule stands) · **the
second nutrition question** (ATH-ADULT-08/12 D2/D6) · STAFF-SS-01 C4 ("Send a reminder").

**Gold — kept on navigation, reversed inside the gym logger:** ATH-ADULT-12 D4 / STAFF-SS-01
D2 declined (the gym glyph keeps its gold on the athlete tab bar and the staff sidebar);
ATH-ADULT-09 D4 **approved** (inside the logger the logged keys, the progress fill and the
"recommended" line move to the accent family).

**Approved and queued — everything else in (a) to (d)**, including the gym logger rebuild
(ATH-ADULT-09 C1) **with two new tokens, `--hit-lg: 56px` and `--hit-md: 52px`** (09 B2 —
a §0.01 system decision: the builder dates them beside their values in `tokens.css` and
records them in the Decisions Log; the other (d) rows that name a token are approved on the
same footing, each dated at the value). STAFF-SS-02-05 C9 is approved with a wider rule than
its row: **the coach does not see body mass at all — the section as well as the three
buttons** (Q27 of the data-architecture briefing, closed 2026-09-12).

**Later the same day (evening):** PATTERN-S4 D1 (sessions live on create, no publish/discard) — **DECLINED**: publish stays; "nothing changes on their phone until you publish" is the schedule's core promise and dropping it discards the offline-publish protection just built (§0al). Every S4 row that depends on D1 falls with it. And a new rule outside the sheet: the injured-athlete proposal gate applies to every role except the medic, at the database (to-do §0bb).

**Two decisions from the same message, outside the sheet:** the coach keeps the permission
to create an injury record, and the `/injuries` board shows "+ Injury" to the coach as well
(the coach's form is the non-clinical one, PATTERN-S3 C9; clinical fields stay medic-only)
— filed as a defect on to-do §0av. And Q27 above.

---

## (a) Cheap and uncontroversial

| Flow | ID | What it changes | Why it isn't built | Recommendation · cost |
|---|---|---|---|---|
| ~~ATH-ADULT-04~~ | ~~C4~~ | ~~The check-in page would say *who* corrected an entry.~~ **Built with C1** — visibility.md withholds nothing about it and My data already names the person; "Corrected by {name} on {date}" · | | |
| ~~ATH-ADULT-06~~ | ~~C1~~ | ~~A subhead under the RPE title.~~ **Kept as built** (decided 2026-09-12) · | | |
| ~~ATH-ADULT-09~~ | ~~B4~~ | ~~The set keys go from 42px to 44px tall.~~ **Built** · | | |
| ~~ATH-ADULT-09~~ | ~~C5~~ | ~~Wake Lock and a buzz on log.~~ **Built** (`lib/wakeLock.ts`; on open, on visibility, released on leave; 10 ms buzz where present) · | | |
| ~~to-do §0az~~ | ~~—~~ | ~~The group reorder arrows: a silent no-op for the medic, the S&C and the nutritionist.~~ **Built** 2026-09-12 (added here as uncovered by the sheet: the to-do decision names the BlockedButton treatment; `moveGroup` checks its rows) · | | |
| ~~ATH-ADULT-10~~ | ~~C1~~ | ~~"Finish early" moves to the header.~~ **Built** with 09 C1 (dashed, neutral, 44px beside the title while sets remain; "Finish session" is the footer's primary once every set is logged) · | | |
| ~~ATH-ADULT-13~~ | ~~C4~~ | ~~"4 of 6 shown".~~ **Declined** · | | |
| ~~ATH-ADULT-13~~ | ~~C5~~ | ~~No tab bar on the session detail.~~ **Declined** — the bar stays · | | |
| ~~ATH-ADULT-13~~ | ~~D1~~ | ~~The §0v "What you reported" card is replaced by a per-row "Corrected · was 100 kg × 8" marker.~~ **Closed 2026-09-14 (reconciliation): the card stays.** 13 C2 and C3 are built (2026-09-13); the row marker they were waiting on needs the 3px bar, declined 2026-09-12 with 12 B5 — so the neutral "Corrected" pill on the row (13 C3) plus the card is the settled shape · | | |
| ~~STAFF-SS-01~~ | ~~A1~~ | ~~Doubtful and Ruled out as tone-family cards.~~ **Built** (the availability line's fill and border on the two readiness rows) · | | |
| ~~STAFF-SS-01~~ | ~~A2~~ | ~~Summary cards say which state they are in.~~ **Built** ("Closed · opens a list" / "Open · showing the list", ▸/▾, a --surf2 well closed, the surface with an accent ring open) · | | |
| ~~STAFF-SS-01~~ | ~~A4~~ | ~~Missing check-ins by run length, "Not submitted".~~ **Built** (`lib/missingRuns.ts`; the Wellness in list, longest run first, last entry date) · | | |
| ~~STAFF-SS-01~~ | ~~D3~~ | ~~"Ready for {matchday}" only within 14 days.~~ **Built** (`FIXTURE_RANGE_DAYS = 14`; "Squad readiness · No fixture in the next 14 days" beyond it). The board's "the week card leads and the matchday card is absent" is a structure change, with C2's remaining steps · | | |
| ~~STAFF-SS-01~~ | ~~Q7~~ | ~~Only the active chip on a phone.~~ **Left as is** (decided 2026-09-12) · | | |
| ~~STAFF-SS-02-05~~ | ~~C4~~ | ~~Who will read what, before a status change lands.~~ **Built** (`AvailabilityAudience` on both forms; the coaches by role — no per-athlete coach exists in the data) · | | |
| ~~STAFF-SS-02-05~~ | ~~C8~~ | ~~Empty panels state the requirement.~~ **Built** (body weight, nutrition plan, flags; the injury panel from A4) · | | |
| ~~STAFF-SS-02-05~~ | ~~D2~~ | ~~One rule for blocked controls.~~ **Built** (`BlockedButton`: aria-disabled, the reason on tap/focus, never a title — the builder's chips, the weigh-in trio, the bio Edit, the correction button, the leaderboard family chips; 06-design-system.md §7.0) · | | |
| ~~PATTERN-S3~~ | ~~C4~~ | ~~Who sees what, by name.~~ **Built** with STAFF-SS-02-05 C4 · | | |
| A11y baseline | class 3 · 3.1 | The dashboard week strip carries each session's **type** by dot colour alone (`dashboard/page.tsx:409`, `PIP_COLOR` seven hues, the legend at line 361); the row prints the session's title, which need not say gym or testing. | Found by the accessibility sweep's hand review (`docs/a11y-sweep-baseline-2026-09-15.md`, Class 3), 2026-09-15, before the sweep was stopped for conformance. Survives conformance: a label, not a value. | **Add the type word** to the row (`enumLabel(a.type)`, existing tokens); the colour stays. In the sweep's class-3 staff commit, after conformance · small |
| A11y baseline | class 3 · 3.2 | The schedule grid's blocks carry the session type by tone alone for a sighted reader (`TimeGrid.tsx:323–370`; the `aria-label` has it); the phone day list's dot the same (`SchedulePhoneDay.tsx:109`, the type word only when a session has no title). | Same review. The timetable's cards print the type word and are fine. | **Add the type word** to the block where `b.height >= 63` and always to the phone row's meta line; no colour change. Class-3 staff commit · small |
| A11y baseline | class 3 · 3.3 | The dashboard's "Not fully available" list tells modified from out by dot colour alone (`DashboardHeadlineStats.tsx:370–381`, `--warn` / `--bad`); the head line gives the counts, not which. | Same review. | **"Modified" / "Out"** as neutral status text on each row; the dot stays. Class-3 staff commit · small |
| A11y baseline | class 3 · 3.4 | The dashboard attention rows carry flag severity on the row's left edge only when collapsed (`DashboardFlagsPanel.tsx:156–159`, `base.css:6405–6407`); the severity word appears only in the expanded detail. | Same review. | **The severity `Pill`** (glyph and word, the existing component) in `.dash-flags-meta` on the collapsed row; the edge stays. Class-3 staff commit · small |
| A11y baseline | class 3 · 3.5 | The GPS scatter's far / near / mid / low bands are dot fill alone (`TrainingScatter.tsx:112–124`); the legend names the hues, the dot's `aria-label` has name, unit and distances but not the band. | Same review. Premium, sport scientist only. | **The band word in the `aria-label`, one shape per band** (`border-radius` / `border-style` on the existing box, no new token). Class-3 staff commit · small |

## (b) Reverses something we decided

| Flow | ID | What it changes | Why it isn't built | Recommendation · cost |
|---|---|---|---|---|
| ~~ATH-ADULT-08~~ | ~~D1~~ | ~~"Yes, most days / Some days / No".~~ **Declined** — Yes / Roughly / No stay · | | |
| ~~ATH-ADULT-09~~ | ~~D4~~ | ~~The logger drops the gold.~~ **Reversed and built** (fill, keys — logged / current / not reached —, the live head, the deviation line "prescribed 100 kg · +2.5" in --muted; the gold stays on navigation) · | | |
| ~~ATH-ADULT-12~~ | ~~D1~~ | ~~Five segments.~~ **Reversed and built** (Wellness · Gym · Sessions · Nutrition · Tests; Leaderboards keeps its footer row) · | | |
| ~~ATH-ADULT-12~~ | ~~B3~~ | ~~Labels at 11px.~~ **Built** with D1 (`--fs-11`) · | | |
| ~~ATH-ADULT-12~~ | ~~C9~~ | ~~Wraps at Larger Text.~~ **Built** with D1 (basis auto, flex-wrap) · | | |
| ~~ATH-ADULT-12~~ | ~~D2~~ | ~~The active segment as a white card.~~ **Declined** — the accent-filled pill stays · | | |
| ~~ATH-ADULT-12~~ | ~~D3~~ | ~~Deltas never coloured.~~ **Reversed and built** (`.rd-delta` and `.hist-delta` in --muted, the figure --text bold, ↑ ↓) · | | |
| ~~ATH-ADULT-12~~ | ~~C4~~ | ~~Deltas vs the 28-day average.~~ **Declined** 2026-09-12 — the comparison stays "on last week" · | | |
| ~~ATH-ADULT-12 / STAFF-SS-01~~ | ~~D4 / D2~~ | ~~The gym glyph loses its gold on navigation.~~ **Declined** 2026-09-12 — the gold stays on the athlete tab bar and the staff sidebar · | | |
| ~~ATH-ADULT-12~~ | ~~D5~~ | ~~Readiness "of 5".~~ **Declined** — 0–100 stays · | | |
| ~~ATH-ADULT-12~~ | ~~D7~~ | ~~Accent and neutrals only on the athlete's chart.~~ **Built** as `accentOnly`, an athlete-only prop (the line and band were already the accent; the out-of-range markers now are too on My data; staff pages unchanged) · | | |
| ~~ATH-ADULT-12~~ | ~~B6~~ | ~~Eyebrows in --faint.~~ **Declined** · | | |
| ~~ATH-ADULT / catalogue~~ | ~~self-export~~ | ~~An athlete exports their own athlete report (the catalogue's "confirm", and the SAR route it would double as).~~ | **Isabella, 2026-09-13 (`docs/reports-catalogue-source.md`): NOT BUILT.** Athletes export no report; they ask a coach or sport scientist out of band. No request button, no queue, no new entity. **The one affordance that existed — Me › "Export my data" and its CSV route `/me/export` (the Article 20 export) — was removed the same day** with `lib/queries/myDataExport.ts`; `12-me.md`, `20-route-map.md` and `09-security` §Article 20 record it. A portability request goes through the subject-access process. | Done (removed) |
| ~~catalogue~~ | ~~squad weekly send~~ | ~~The squad weekly report goes out automatically on a Monday morning to a distribution list.~~ | **Isabella, 2026-09-13: NOT BUILT.** On demand only, at any point in the week; no scheduler, no outbound email, no distribution list. **Nothing existed to remove**: the three cron jobs on the database are thresholds, compliance expectations and the retention preview, and no code sends any report anywhere. `21-squad-weekly-report.md` says so; `08-notifications.md`'s weekly-digest rows are the superseded catalogue. | Done (nothing to remove) |
| ~~STAFF-SS-01~~ | ~~—~~ | ~~The bottom bar + More sheet, superseding §0af's compact top bar.~~ **Decided and built `af09c17`** (2026-09-12) · | | |
| ~~PATTERN-S3~~ | ~~D2~~ | ~~A one-time "you were told" card at the top of the athlete's Today, displacing the availability line's emphasis until the status screen is opened.~~ **Built 2026-09-14 with C1** — `StatusToldCard`, the emphasised card while unread, one 56px action to `/me/status` · | | |
| ~~PATTERN-S4~~ | ~~D1~~ | ~~Sessions are live the moment they are created, moved or removed — no pending queue, no Publish, no Discard.~~ **DECLINED 2026-09-12 (Isabella, the evening ruling above): publish stays.** Every S4 row that depended on it fell with it (C2/B1/B3, C3, C9/C10 — struck below). | Reverses the schedule's held-until-publish model (07-schedule.md §6: one session published alone puts a half-updated week on phones; §0al's offline hold and the ghost/Restore were built on it today). Most of the S4 board (the grid-click popover writing on Add, a drag writing at once, the three-timeframe confirmation, "N sessions live") depends on this. | **Decide once.** If reversed: the publish machinery, the sessionStorage hold, the ghost/Restore and the banner go; each write is already its own audited act (0104) · large |
| ~~PATTERN-S3~~ | ~~D1~~ | ~~The coach sees the protocol stage.~~ **Enforced** (`lib/restrictions.ts` drops protocol / stage / diagnosis entries from the restriction line at every query read, for every viewer; the seed no longer writes one — SS-02-05 C7 done with it) · | | |

## (c) Needs new data or a query

| Flow | ID | What it changes | Why it isn't built | Recommendation · cost |
|---|---|---|---|---|
| ~~ATH-ADULT-04~~ | ~~C1~~ | ~~The check-in page shows a "Corrected" pill and the correction when staff corrected the day.~~ **Built** (the day's revision chain; pill + "Corrected by {name} on {date}"; "You sent … at" is the athlete's own time) · | | |
| ~~ATH-ADULT-08~~ | ~~C1~~ | ~~A check-in can be corrected once; a second attempt sees a "spent" card.~~ **Built** — migration 0107, the chain read, the spent state, My data's Corrected mark · | | |
| ~~ATH-ADULT-08~~ | ~~C2~~ | ~~"Correction saved" on this page.~~ **Built** · | | |
| ~~ATH-ADULT-08~~ | ~~C3~~ | ~~The caption "You can correct this once after you submit."~~ **Built** · | | |
| ~~ATH-ADULT-08 / 12~~ | ~~D2 / D6~~ | ~~A second weekly question, "How well did you fuel around training?" (1–5), on the check-in and in My data's history.~~ **DECLINED 2026-09-12 (Isabella, the header above: "the second nutrition question"); CLAUDE.md rule 8's one question stands.** | A data change: a column, the registry, CLAUDE.md rule 8's wording ("one question"). No frame draws it. **⚠ migration** | **Decide the product question first**; if yes, a migration + registry entry + form + history in one brief · large |
| ~~ATH-ADULT-09~~ | ~~C3~~ | ~~The weight stepper's increment comes from the exercise.~~ **Built** — migration 0108 `exercises.weight_step_kg`, set on the library's create form; existing exercises keep 2.5 (no edit form for a library row exists — recommend: add the step to the exercise detail's edit when that screen is built) · | | |
| ~~ATH-ADULT-09~~ | ~~C4~~ | ~~Queued sets send on `online` and from the logger itself.~~ **Built** — `lib/gymOutboxFlush.ts` shared by the logger and Today's flusher; "· 2 waiting to send" on the progress row · | | |
| ~~ATH-ADULT-09~~ | ~~C6~~ | ~~The session-complete summary.~~ **Built** — MET-040 / MET-041 registered; `fetchPersonalBestsBefore`; `lib/gymSummary.ts` · | | |
| ~~ATH-ADULT-10~~ | ~~C3~~ | ~~The early-finish summary.~~ **Built** with 09 C6 (a dashed card, not a 3px bar) · | | |
| ~~ATH-ADULT-11~~ | ~~C2~~ | ~~"Set 1 corrected · was 100 kg × 8" on the logger.~~ **Built** — the page reads the session's revision chains; the strip sits under the exercise's keys · | | |
| ~~ATH-ADULT-12~~ | ~~C2~~ | ~~Sessions and Nutrition become hero-card tabs: RPE eleven-bar chart with this week solid, "n = 3 rated sessions this week"; nutrition weeks as question/answer rows.~~ **Cut from v1, not deferred** (Isabella, `docs/decisions/decision-batch-2026-09-13.md`, "The My data hero cards are CUT", 14 Sept 2026): the cards need two sports science rules — what "steady" means and the minimum sample — that a builder cannot invent; polish on a screen that already works. The defaults put to Isabella are recorded there if ever revived · | | |
| ~~ATH-ADULT-12~~ | ~~C3~~ | ~~One plain-English line per hero ("Readiness steady over 28 days, averaging 67." / "Two CMJ results is not enough to show a trend.").~~ **Cut from v1, not deferred** — the same ruling as C2: "steady" and the minimum sample are judgements Isabella has chosen not to make for v1; the tests tab's wording problem (a CMJ run twice a season) is noted there · | | |
| ~~ATH-ADULT-12~~ | ~~C5~~ | ~~The gym hero becomes "Back squat best 102.5 kg, up 5 kg this block".~~ **Built 2026-09-13** (`lib/gymHero.ts`, `fetchBestSetsInPeriod`): "102.5 kg · Back squat best · × 5 · Sat 5 Sept · up 5 kg on your best before Mon 17 Aug" — the period's window as the comparison (blocks carry no dates the athlete sees), said in the line; the lift with the most working sets in the period; down said as down, an equal best as equal, no earlier best "the first Back squat logged". The zero headline is words ("Nothing logged · in this period"); the four-week Sessions card keeps its own stated span beneath. | | |
| ~~ATH-ADULT-12~~ | ~~C6~~ | ~~An empty period says when the last entry was and offers "Show this season".~~ **Built** on four tabs, to PATTERN-S6's grammar (`fetchMyLatestRecord`, `lib/myDataEmpty.ts`) · | | |
| ~~ATH-ADULT-12~~ | ~~C7~~ | ~~Tests lists the tests assigned to the athlete; one with no result reads "Not logged".~~ **Built** as the club's live test definitions (there is no per-athlete assignment in the schema — `test_definitions` is org-wide; a narrower assignment would be a migration, recommend not unless a club asks) · | | |
| ~~ATH-ADULT-13~~ | ~~C1~~ | ~~The session detail's eyebrow "Gym · Lower A · complete".~~ **Built** · | | |
| ~~ATH-ADULT-13~~ | ~~C2~~ | ~~Tapping a set row on the session detail opens the logger's correction; the per-row "Correct" buttons go.~~ **Built 2026-09-13** as recommended: each row links to `/gym/[sessionId]?log=[logId]&correct=[setId]`; the logger opens a past log by id (read, never created — a log not on record is a 404) with that set's correction open beneath the summary; the detail's inline form and its second `revise_gym_set_log` path are gone. | | |
| ~~ATH-ADULT-13~~ | ~~C3~~ | ~~A "Corrected" pill on the gym history row; the dated sentence on the detail.~~ **Built** — the flag off the live sets' `revision_of` in the list read; no 3px bar · | | |
| ~~STAFF-SS-01~~ | ~~A3 / C3~~ | ~~The attention panel counts athletes; the Flags tab badge shows the same number.~~ **Built** — `athleteTotal` on the attention read, `fetchOpenFlagAthleteCount` for the badge · | | |
| ~~STAFF-SS-01~~ | ~~C2~~ | ~~The dashboard rebuild as a series.~~ **All steps built:** A1, A2, A4, the thresholds line (2026-09-12); the role versions, the matchday lead card and the week strip yielding on a heavy morning — five or more athletes needing attention, never while the strip is the lead (`weekStripYields`, `lib/dashboardLead.ts`) — 2026-09-13. D6–D8 record where the board and the matrix or the registry part. | | |
| STAFF-SS-01 | C4 | "Send a reminder" on a missing check-in. | Declined 2026-09-12 because nothing in the product sends anything. **Re-opened 2026-09-13** (`docs/platform-decision.md`, S9: "it returns now that a sender is required" — staff notifications are actions a person takes, not a clock). Waits on the S9 send path (service worker, VAPID keys, a subscription table). | With S9 · small once a sender exists |
| ~~STAFF-SS-01~~ | ~~C5~~ | ~~The last-admin guard enforced at the database.~~ **Struck 2026-09-12 (Isabella): already live** — migration 0101, 0102 covers the self-grant; 0109 (§0bd, 2026-09-13) extends it to the account's status. | | |
| ~~STAFF-SS-01~~ | ~~D6~~ | ~~The nutritionist's lead card "keeps the three availability counts with no names".~~ **Built as the board says, 2026-09-13.** First built as "absent" from `docs/access-matrix.md` §4.2, which withheld MET-013 from the nutritionist — a paragraph migration 0074 (decided 2026-09-06: the nutritionist reads the censored injury and availability view, never a diagnosis) had superseded; the matrix's §2, §4.1 and §4.2 are corrected from the live policies in the same commit. The counts draw; the named lists and the Available tile do not (the board's choice, not a permission). | | |
| STAFF-SS-01 | D7 · **DECIDED · unbuilt** | **Ruled 2026-09-13 (batch B3): body mass becomes a flaggable metric, thresholded on CHANGE OVER TIME, never an absolute value — a registry entry and a threshold row; then both cards fill by themselves.** *Verified 2026-09-14: not built — `metric_definitions.wellness.body_mass_kg` is still `leaderboard_eligible = false` with no threshold metric for it on either project; the thresholds in use are readiness, sleep, soreness, ACWR and wellness compliance.* The row as raised: the nutritionist's attention card "keeps body-mass rows and drops load"; the S&C's is "load and weigh-ins only". | No threshold raises a flag about body mass (`metric_definitions` seeds `wellness.body_mass_kg` ineligible) or in the nutrition domain, so a weigh-in never reaches either card; the nutritionist's reads "No open flags right now" until one exists. Built 2026-09-13 on the flag domains that exist (S&C: GPS, session RPE, gym, testing; nutritionist: nutrition). | **Decide whether body mass becomes a flaggable metric** (a threshold on the day-to-day change, say) — a registry entry and a threshold row, then both cards fill by themselves · medium |
| ~~STAFF-SS-01~~ | ~~D8~~ | ~~Retire MET-014 from the registry; MET-015 stays on the lead card's tail line.~~ **Built 2026-09-16**: `docs/metrics.md` MET-014 is marked RETIRED with its definition kept for the record and its identifier not reused; `01-dashboard.md`'s table follows; MET-015 unchanged on the tail line. | | Done |
| ~~STAFF-SS-01~~ | ~~D4~~ | ~~Only the medic sees an unavailability *reason*; non-clinical reasons (Academic) show without the "Medical" label.~~ **Read and built 2026-09-13 with the lead card.** The reason CATEGORY is readable by every staff role at the database (access-matrix §4.1: "status, restrictions, and reason category"), so withholding it from every role but the medic on the dashboard is a display rule; the clinical reason (the diagnosis) is medic-only under RLS (`clinical_medical_only`, 0012) and the page calls that read for the medic alone. A non-clinical reason renders to the medic without the Medical eyebrow. | | |
| ~~STAFF-SS-02-05~~ | ~~C1 / B1~~ | ~~Every profile opens with one emphasised status header.~~ **Built 2026-09-13** (`lib/profileHeader.ts`, `.pp-hero`): the wash family for the board's tint (B1), the sub line "Hooker · #2 · Forwards, Rehab", the restriction line with the coach-visible note and the expected return, the owner line "Set by medical staff · Ruth Callaghan · Wed 5 Aug", the bio row (the coach's without Weight), the plan line and its link as the card's last row; the separate plan bar is gone. Built differently: Hand stays in the bio row and Group lives in the sub line only (the board's Group cell would repeat it); the wellness dial stays on the name row (the board did not draw it and did not remove it). | | |
| ~~STAFF-SS-02-05~~ | ~~C2 / B4~~ | ~~The squad list on a phone as 60px rows.~~ **Built** — one markup, a phone grid below 768px with explicit table roles · | | |
| STAFF-SS-02-05 | C3 / B2 · **unblocked, unbuilt** | The phone profile gets a sticky 52px jump bar ("Flags · panel 1 of 10") and an "All panels" sheet listing every panel with a value each. | A new component; SS-01's sheet is the shape. **C4 is now decided (B5, below), so nothing blocks this.** *Verified 2026-09-14: not built.* | Build with C4 · medium |
| STAFF-SS-02-05 | C4 / B3 · **DECIDED · unbuilt** | **Ruled 2026-09-13 (batch B5): one order, one exception — the sport scientist's panel order for every role, Body weight raised above Flags for the S&C and the nutritionist.** *Verified 2026-09-14: not built (no per-role order in `src/lib` or the profile page).* The row as raised: panels ordered by role from one library; a panel a role cannot see is absent — no heading, lock or count. | **The inventory is done** (2026-09-13, the record): the per-role absences are the Availability panel (AVAILABILITY_EDIT), Body weight (BODY_MASS_VIEW), SAR (sport scientist) and the medic's clinical block — all already absent, not locked; every other panel is common, differing in which controls are live or blocked. What remains is the ORDER per role, which the board's frames give only for the sport scientist and "differences" — the other four orders are a decision. **⚠ permissions** | **Decide the four orders** (recommend: the sport scientist's order for everyone, Body weight raised above Flags for the S&C and the nutritionist), then build with C3 · medium |
| ~~STAFF-SS-02-05~~ | ~~C5~~ | ~~Read-only panels end with an owner line.~~ **Built** on the Injury card (injury-linked availability, `set_by`) and the Nutrition plan panel (the rule's author) via `ReadOnlyOwner`. **Not yet:** the programme block — `programme_assignments` carries no author; add `assigned_by` when the S5 series lands (⚠ migration, small) · | | |
| ~~STAFF-SS-02-05~~ | ~~C7~~ | ~~Restriction text never names a protocol.~~ **Built** with PATTERN-S3 D1 (the read-side filter and the seed) · | | |
| ~~STAFF-SS-02-05~~ | ~~C9~~ | ~~Body mass hidden from the coach.~~ **Built** as a flat rule, not a setting (decided 2026-09-12: the coach does not see body mass at all — the section as well as the buttons): `BODY_MASS_VIEW` in access.ts; the profile card, the nutrition page's card and the export columns. **Remaining halves, recommend when the club asks:** (1) `wellness_entries.body_mass_kg` is still readable by the coach at the database (the athlete's own check-in field) — an RLS column gate would need a view or a split table (⚠ migration); (2) the athlete nutrition page still shows the coach the per-kg rates beside the daily totals ("30.5 kcal/kg" and "3,200 kcal"), from which a mass is one division away — if the rule is meant strictly, hide the per-kg rates for the coach (small) · | | |
| ~~PATTERN-S3~~ | ~~C1~~ | ~~The athlete is told once about a status change: an emphasised card on Today that stays until the status screen has been opened.~~ **Built 2026-09-14** — migration 0122 `availability.athlete_seen_at`, `mark_availability_seen()` (the athlete's own open row, once); the card shows while a staff-set row is unread and goes the first time `/me/status` is opened; no timer, no dismiss · | | |
| ~~PATTERN-S3~~ | ~~C2~~ | ~~An athlete status screen answering three questions (can I train today / what can I do / when am I back), the restriction list as rows, the stage ladder, the academy slot.~~ **Built 2026-09-14** — `/me/status` (`docs/athlete/screens/22-my-status.md`, `lib/statusScreen.ts`): three cards with answers as sentences, restrictions as 44px rows after `restrictionLine`, the ladder with state words where a protocol exists, the academy slot as a place not a policy, the medical detail lower; doors from the told card, the Me row and the availability card's last line · | | |
| ~~PATTERN-S3~~ | ~~C3~~ | ~~Stages numbered with state words; advancing moves one stage and asks for a rewritten restriction line and a criteria-reviewed confirmation; any other stage needs a reason.~~ **Built 2026-09-14** — migration 0123 `injury_protocols` + `injury_stage_events` (medic and athlete-self SELECT only; `open_injury_protocol`, `move_injury_stage`), the ladder on the injury record for the medic; the advance writes the new line to the open availability row; `restriction_line_is_clean` refuses clinical words at the database and `lib/restrictions.ts` still strips at every read. Stages are numbered, never named (the board's bare counter) · | | |
| ~~PATTERN-S3~~ | ~~C5~~ | ~~The absence form.~~ **Built** on `SetAvailabilityFormCoach` (reason chips first, Modified / Unavailable, Available withheld, the well, "Mark available again" to end one). Permissions unchanged — the database's (coach + sport scientist non-injury, 0068; the medic unconditionally). **Not built, with a recommendation:** the board's From / Until dates — the read model has no scheduled interval (`effective_to` null = open), so an Until date would hide the absence from every current read; recommend a `scheduled_until` column read by `fetchCurrentAvailability` when the S3 C7 history lands (⚠ migration); the "Already recorded this season" list is C7's · | | |
| ~~PATTERN-S3~~ | ~~C6~~ | ~~Rehab programme proposals with three states (Proposed / Approved / Returned) and a required return reason, in one list the S&C and the medic both see.~~ **Built 2026-09-14** — migration 0124 (`returned`, `decided_by/at`, `return_reason`, `decide_proposal`), `/programmes/proposals` (`docs/screens/66-rehab-proposals.md`) for the S&C and the medic; the injury record's Sign off / Request changes go through the same function. Approved stores `active` (approval assigns); the returned row stays as the record and the S&C proposes again · | | |
| ~~PATTERN-S3~~ | ~~C7~~ | ~~Availability history.~~ **Built** at `/squad/[athleteId]/availability` with a CSV — no migration: the table is already the ledger (close-then-insert, one open per athlete), "what changed" is computed against the row before (`lib/availabilityHistory.ts`). Injury events on the board's rows are the injury record's (with C8) · | | |
| ~~PATTERN-S3~~ | ~~C8~~ | ~~Body site and side are not coach-visible: a coach reads the status word, the restriction line and the expected return, nothing else.~~ **Built 2026-09-14, decided off by default** — migration 0122 `organisations.coach_sees_injury_site`, SELECT on `injuries` re-granted by column without `body_area`/`side`, the `injuries_staff` view masking both unless `injury_site_visible()`; the switch on Club details (`#injury-site`), audited; every staff read of the two columns moved to the view; "Injury" / "Site withheld" where masked · | | |
| PATTERN-S3 | C9 · **unblocked, unbuilt** | The injury form split by permission (clinical fields in their own column, save works without them) and a pitch-side four-field form at 44px. | A restructure of `NewInjuryForm` / `InjuryMedicalForm`. **C8 is decided and built (2026-09-14: body site and side off by default) — the coach's form carries status, restriction and expected return, nothing else.** *Verified 2026-09-14: the two forms untouched since `b5dabcd` (C8) and the layer-three spacing pass.* | **Build** · medium |
| ~~PATTERN-S4~~ | ~~C2 / B1 / B3~~ | ~~Clicking an empty grid slot opens a 320px popover … and "Add session" writes it.~~ **Fell with D1** (declined 2026-09-12: publish stays). The grid's own draft card (a held draft, published from the banner) is the shape that stands. | | |
| ~~PATTERN-S4~~ | ~~C3~~ | ~~A drag writes the move immediately; typed edits are held until Save changes.~~ **Fell with D1**: nothing writes until Publish. | | |
| PATTERN-S4 | C4 · **DECIDED · unbuilt** | A session in the past, or already rated, opens read-only with the reason. | **Ruled 2026-09-13 (batch B6): a rated session opens READ-ONLY, with the reason said out loud ("This session has been rated by 14 athletes. Ratings are tied to its date and duration, so it cannot be changed. Cancel it and create a new one…"); the rating neither follows nor detaches.** *Verified 2026-09-14: not built — no such sentence in `src/`.* | Build · small |
| ~~PATTERN-S4~~ | ~~C5~~ | ~~Expected attendees are counted as distinct athletes across the selected groups, with the denominator.~~ **Built 2026-09-13** (`lib/scheduleExpected.ts`, `fetchSquadSize`): "Publishes to Backs + Forwards · 18 of 30 athletes are expected" on the panel's preview footer — the workspace already resolved a session's athletes as one Set across its groups, so the number was distinct; it needed saying with its denominator. **Left open, one decision:** whether an athlete marked unavailable is still expected — today they are (the count is membership). Recommend: stays expected and shows as absent; the RPE expectation follows the session, not the status · — | | |
| ~~PATTERN-S4~~ | ~~C6 / B4~~ | ~~The phone: day-first with the dashboard's week strip and 44px rows — no grid at 375.~~ **Built 2026-09-13** (`SchedulePhoneDay`, `lib/schedulePhoneDay.ts`): the strip at 74px a tile (day, date, MD, minutes; today selected), the day heading with its 44px + (the new-session page — forms stay pages), 44px rows linking to each session and fixture, "Nothing on Thursday.", the Next line; the grid, its panels and Read/Edit not drawn below 768px. B4: the shell's own surface, hairline and accent for the board's bar tokens. The week nav, banner and template toolbar stay above it (S6 C5's "Week templates disabled offline" is a separate row). | | |
| PATTERN-S4 | C7 · **DECIDED · unbuilt** | Applying a template states where each offset lands and what happens to what is already there. | **Ruled 2026-09-13 (batch B7): applying a template REPLACES the week, with the consequence named before the button ("This will remove 4 sessions already in this week and add 9 from the template.") — B11's dialog is the one place a confirmation earns its keep.** *Verified 2026-09-14: not built — `/schedule/planner/apply` still offers add / replace_planned / fill_gaps as three links and no consequence sentence.* | Build, with B11's dialog · medium |
| ~~PATTERN-S4~~ | ~~C8~~ | ~~A group with no session reads a dash and "5 athletes · nothing scheduled", not 0m.~~ **Built 2026-09-13.** The panel already listed every group with members; the words were the gap. | | |
| ~~PATTERN-S4~~ | ~~C9 / C10~~ | ~~The destructive confirmation answers three timeframes; every count states its denominator ("6 sessions live in the athlete app").~~ **Fell with D1** for the timeframes (a removal is held until Publish, so "now / this week / later" has no meaning). The denominator half is built where it applies: "18 of 30 athletes are expected" (C5), "5 athletes · nothing scheduled" (C8). | | |
| ~~PATTERN-S5~~ | ~~C1~~ | ~~A logged set keeps the prescription it was logged against — reps, load and step snapshotted at logging, so editing a block never rewrites what a past set is compared to.~~ | **Built 2026-09-13** (migration 0111 on scratch): `gym_set_logs.prescribed_reps / prescribed_load_kg / prescribed_step_kg`, written by the logger with the prescription as resolved for that athlete at logging; the _current view carries them; a correction copies the original's and ignores its payload's. Read from the row by the correction's reference line, My data's session detail ("Prescribed" column, "Not recorded" for pre-0111), and both CSV exports. Proven on scratch: the programme moved 12 → 20, the logged set still read 12. | Done |
| PATTERN-S5 | C2 / C5 / C6 | Every prescription write states its effective date ("Save from Sat 12 Sept"; "38 sets already logged keep the prescription they were logged against"); the mid-block confirmation screen; the primary carries the number or date it commits. | With C1. | After C1 · medium |
| PATTERN-S5 | C3 / C10 | The per-athlete adjustment screen: one card with controls, parent rows in `--muted`, a required note, remove-override as a tertiary; the phone version stacked. | The override note is not required server side (Q5). **⚠ migration** (a check) | Build with the check · medium |
| ~~PATTERN-S5~~ | ~~C4~~ | ~~An assignment's headline is the distinct athlete count, with its arithmetic.~~ **Built 2026-09-13** (`lib/assignmentCount.ts`): "15 of 30 athletes" over "15 in Forwards + 1 named − 1 counted twice" on the programme's Assigned card; the same squad denominator as S4 C5. | | |
| ~~PATTERN-S5~~ | ~~C7~~ | ~~A nutrition plan authored as rules per kilogram with the worked example, "not set" as a dashed frame, the squad mean with its n, "Coach-set guidance, not a clinical prescription".~~ | Authored side: already on the running screen (2026-09-13). Resolved side, **Isabella's ruling 2026-09-13: neither spec — show the club default, labelled as the club default**, so the number is present and its provenance is on the face of it. **Built the same day**: a provenance line under every resolved target (the athlete's own card, the staff athlete nutrition page, the profile card) — "The club default target, the same for everyone on it." / "Your group's target." / "Set for you.", with "Not scaled to your weight — no weigh-in on record." when there is none; the board (`PATTERN-S5-final/notes.md`) and `06-athlete-nutrition.md` §States updated to the code plus the label. | Done |
| PATTERN-S5 | C8 | "each side" beside a unilateral exercise's name. | The exercise record has no unilateral flag. **⚠ migration** | Schema first · small |
| PATTERN-S5 | C9 | Weeks run down, sessions across; a week not reached reads "not reached", never 0 of 24. | The block view does not exist in this shape. | With C2 · medium |
| ~~PATTERN-S6~~ | ~~C1~~ | ~~The "Waiting to send" queue screen (oldest first; what, its denominator, the time saved; "4 entries · 5 writes"; no per-item retry; "Nothing is waiting" with the last send time) and "See what is waiting" under Today's count.~~ | **Built 2026-09-13**: `/today/waiting` (`docs/athlete/screens/19-waiting-to-send.md`), rows on the app's own `.hist-row`, header "4 entries · 5 writes", the empty with the last send (`fydr-outbox-last-sent`, written by the flusher); "See what is waiting" under Today's count; Today now counts entries the way the queue does (a gym session is one entry). A gym set is queued with its session's name, total sets and date so the row can be named offline. | Done |
| PATTERN-S6 | C2 · **DECIDED · unbuilt** | **Ruled 2026-09-13 (batch B8): Today lists the athlete's gym session as a row with its count, "6 of 12 sets · 2 waiting to send" — a genuinely new query, accepted.** *Verified 2026-09-14: not built — Today's to-do list still carries wellness, RPE and the nutrition check-in only.* The row as raised: Today's gym row reads "6 of 12 sets · 2 waiting to send". | Today has no gym row to put it on: the to-do list carries wellness, RPE and the nutrition check-in, and an unfinished gym session is "a real, separate query this pass doesn't" make (the page's own note). The waiting count exists (`queuedGymSets`, client-side). **⚠ decision:** does Today list today's gym session (open or not) as a row? | Decide the row, then small · small after |
| PATTERN-S6 | C3 · **DECIDED · unbuilt** | **Ruled 2026-09-13 (batch B9): a part-filled form survives session expiry in localStorage, per form, the gym drafts' own mechanism; it moves into the shared IndexedDB outbox when S11 builds it.** *Verified 2026-09-14: not built — nothing preserves a part-filled form across re-authentication.* The row as raised: session expiry mid-form: answers held on the device, "Sign in and send", back to the same sheet with every answer set. | Nothing preserves a part-filled form across re-authentication; the JWT is 30 min with refresh. **⚠ decision** (where the hold lives — localStorage per form, as the gym drafts do). | Decide, then build · medium–large |
| PATTERN-S6 | C4 · **rule DECIDED (B12) · unbuilt, waits on S11** | Staff typed writes queue ("Saved on this phone · sends when you have signal. Nobody else can see this until you have signal."), the staff queue time-critical first with "When it sends", raising a flag offline. | Closed 2026-09-12 as post-pilot; **in scope from 2026-09-13** (`docs/platform-decision.md`, `docs/decisions/scope.md`: both apps must have a robust offline outbox). **Not built yet, by instruction**: the clinical-conflict rule inside it — a coach's held Unavailable arriving after a medic has set a different value — is undecided (`architecture/to-do.md` §4). Built on the S11 shared outbox (IndexedDB, service worker), not the per-screen localStorage queues; the S11 inventory says what exists. **The clinical-conflict rule is now ruled (batch B12, 2026-09-13): a held availability write that lands after a newer value exists is never applied — refused and shown to the coach as a conflict naming both values, their times and who set each, with discard or set-it-again-now; the medic's value stands.** | Build on the S11 foundation · large |
| PATTERN-S6 | C5 | A drag on the grid disabled offline with the footer reason; + Session / + Fixture queue; Week templates disabled. | With C4 — **in scope from 2026-09-13**, not built yet. | With C4 · medium |
| ~~PATTERN-S6~~ | ~~C6~~ | ~~A failed live write on the schedule undoes itself: the block returns, a dashed ghost reads "Did not save", the notice names both times and the athletes.~~ **Built 2026-09-13** (`lib/scheduleFailedWrite.ts`): in the held model the failed write is a publish the server refused for one session — the change stays held, the block is drawn where the athletes still have it, the attempted position is a "Did not save" ghost (the removal ghost's dashed treatment, the words say which — D2 settled as recommended), the banner reads "Gym A did not save — … The athletes still have Mon 7 Sept · 07:00; you tried 07:15 · 30 athletes affected." with Try again. A dropped connection keeps §0al's one line and ghosts nothing. | | |
| ~~PATTERN-S6~~ | ~~C7~~ | ~~Permission denied: "This is not available to you…", the signed-in identity and role, a reference code, Back to dashboard.~~ | The copy built 2026-09-13 (`/denied`). **The log built the same day on Isabella's ruling** (reversing the deferral: under a v1 that gets sold, a denial a user cannot describe is a support call that cannot be resolved): migration 0112 `log_access_denial` writes one `access.denied` row to the club's audit log as the person refused (gate, path, roles) and returns "D-<id in base 36>"; every gate calls it before redirecting; the screen shows "Reference D-1TLB. Quote it to your club's administrator — it is in Settings › Audit log."; the audit screen shows and finds the row by the code. | Done |
| ~~PATTERN-S6~~ | ~~C8~~ | ~~Staff empty states to the one grammar (what, why, what would fill it, the most recent on record, "Nothing is missing", one action that widens the window).~~ | **Built 2026-09-13, complete** (`lib/staffEmpty.ts`, `lib/dashboardEmpty.ts`): the athlete, training, squad weekly and testing reports, and the dashboard's three (the day timeline with the nearest session day on record and one action to it; a session card's all-clear over its expected count; the flags panel's all-clear over the scope's size). Review fix on the testing report: an empty scope is told apart from a test never run, and every "Show the whole squad" action clears the shared cookie (§0ak). | Done |
| ~~PATTERN-S6~~ | ~~C9~~ | ~~One `role="status"` and one `role="alert"` region per surface, written on transition only.~~ | Audited 2026-09-13 (the record, "C9 audit"): 118 live-region attributes in 90 files, 108 conditional; at runtime 34 of 37 surfaces carry no region on load — the rule already holds in effect because a conditional region mounts once per transition. The one violation (OutboxFlusher's alert per conflict) fixed the same day. **Isabella's ruling 2026-09-13: not now — deferred on its merits, not on scope.** Revisit only if screen-reader testing shows a mounted alert being missed, then per shell. | Deferred on merits · — |
| PATTERN-S6 | C10 · **DECIDED · unbuilt** | **Ruled 2026-09-13 (batch A20): a policy refusal marks a held item "could not be sent, the week has closed", shown once, with Discard.** *Verified 2026-09-14: not built for the week-closed case — the §0bc closed-session flag (`session_log_closed`, 2026-09-13) is the same shape for gym sets and is the pattern to copy; no "week has closed" sentence exists.* The row as raised: retry schedule, queue limits, and a "too late to send" state. | No cap, no expiry, no backoff; a check-in past its three-week write window is refused by policy for ever and counted as waiting for ever. **⚠ decision** | Recommend: a policy refusal (42501 / WITH CHECK) marks the item "could not be sent — the week has closed", shown once like a conflict, with Discard · small–medium |
| PATTERN-S6 | D1 | `--pill-offline` / `--on-offline`. | A new token; **in scope with C4 from 2026-09-13**. If C4 is built the offline tone maps to an existing wash first; a new token only if none reads right. | With C4 · — |
| ~~PATTERN-S6~~ | ~~D2~~ | ~~`--line-dashed-failed`.~~ **Settled with C6 (2026-09-13):** the existing dashed ghost and the words "Did not save"; no new token. | | |
| ~~PATTERN-S6~~ | ~~D3~~ | ~~`QueueRow` component.~~ | Built with C1 2026-09-13 as the app's own `.hist-row` (date / detail / value) inside `components/WaitingQueue` — no new component shape; the staff variant's consequence line waits with C4. | Done |
| ~~PATTERN-S7~~ | ~~C1~~ | ~~The report shell for all six reports: title, the catalogue's definition sentence, period and group, the figure with denominator and exclusions, one chart, table, exports top right, print.~~ | **Built 2026-09-13, complete.** The header was already the shared template; the definition sentence (reconciled to Isabella's catalogue — training and match carry none, raised on the sheet) and the one emphasised figure card (`components/ReportFigure`) on all six reports, on screen and in the PDFs; `TableShell` (`components/TableShell`) around every report's main table — title, the sort order in words where the table is a ranking, the count with its denominator, a body that scrolls sideways on a phone. One chart at most per report is already the case. | Done |
| ~~PATTERN-S7~~ | ~~catalogue · training~~ | ~~The catalogue's confirmed training sentence — "Session load is RPE multiplied by session minutes, summed over the period…" — on `/reports/training`.~~ | **Ruled by the addendum and reconciled 2026-09-13 (ruling four)**: the built per-session GPS board is the **GPS report** (premium) and now carries the confirmed GPS sentence on screen, in the CSV and in the PDF ("Per-session GPS totals for each athlete, from the files imported for that session. An athlete with no GPS file for a session shows as no record, never as zero."); the RPE × minutes sentence belongs to the **Training load report**, the seventh, every club — held in `lib/reportCatalogue.ts` as `TRAINING_LOAD_DEFINITION` with its off state until the split builds the route. The builder's compliance and injuries drafts are replaced by the addendum's confirmed sentences; the module carries no draft. | Done; the split builds the seventh |
| ~~PATTERN-S7~~ | ~~catalogue · match (Q4)~~ | ~~The match sentence, on hold pending "does the app record who played and how many minutes?"~~ **Ruled 2026-09-14 (the batch, "The match report, both halves approved") and BUILT 2026-09-15 — migration 0127 `match_participation`, the post-match sheet on the fixture, the attach action, `/reports/match` with the confirmed sentence (the Match participation row below).** The read that answered it: | **Answered from the code 2026-09-13: no.** The app records attendance at a session (`session_attendance`: full / modified / absent / excused with a non-clinical reason, a coach's write from the timetable) and the week's published team allocation (`team_allocations`, the selection list). No column anywhere holds minutes played, starters, bench or substitutions; `fixtures.result` is free text. GPS `duration_s` per athlete is a proxy for time on the pitch only where a unit was worn (premium). **Under v1 this is a gap to close, not a fallback to word** (`docs/design-programme-corrections-2026-09-13.md`: "a product sold to rugby clubs that cannot say who played is not complete"). Needs: a place to record who played and minutes (⚠ migration — per athlete per fixture: started / came on / minutes; a coach's write on the fixture), then the match report reads it. | Isabella writes the sentence and rules on the capture (a coach's post-match sheet on the fixture) · medium |
| PATTERN-S7 | catalogue · testing (Q3) | The confirmed testing sentence's last clause: "an athlete who has never been assigned a test does not appear for it." | **Answered from the code 2026-09-13: tests are not assigned.** `test_definitions` is what the club measures, org-wide; `test_results` is one athlete's result on one date; there is no assignment entity and no group or athlete scoping on a definition, so "never assigned" cannot be told apart from "no result". On the built page the by-athlete grid shows every athlete in scope for every test (a missing result in words) and the by-test ranking lists only athletes with a result and says how many have none. The sentence is on the page as confirmed; the clause has no fact behind it. | Either reword the clause ("an athlete with no result for a test is shown as such, never as zero") or add test assignment (a migration: tests per group or athlete, and the report scoped by it) · small / medium |
| ~~PATTERN-S7~~ | ~~catalogue · squad weekly window (Q2)~~ · **Done (`35145e5`, 2026-09-13)** | The confirmed sentence "The week Monday to Sunday, club local time" against the built window. | **Found 2026-09-13**: the built squad weekly window was a trailing seven days ending today (`from = today − 6`), so the sentence was true only on a Sunday. **Fixed in the commit after the reconciliation**: the window is the calendar week Monday to Sunday in the club's timezone, the current week running Monday to today, the pager moving a week at a time; `?to=` links still resolve to their week. The shared Monday boundary itself exists — `mondayOf` is the one rule for the schedule grid, the dashboard strip, the timetable, the nutrition check-in (`week_start`, checked at the database) and My data; RPE has no week of its own (a rating is per session per day). The dashboard's strip draws Monday to Saturday. | Done (the window); the six-day strip is the dashboard board's own choice · — |
| ~~PATTERN-S7~~ | ~~catalogue · load (Q1)~~ · **Answered — nothing to build; the dead `sessions.planned_load` column is a note, not an item** | "Is training load stored or derived, and where does 'load 2,910' on a week template come from?" | **Answered from the code 2026-09-13: derived, twice.** Actual session load is MET-007, RPE × duration from the athlete's rating (`training_entries`), computed in application code and summed into acute/chronic. A week template's "load 2,910" is planned load: Σ `duration_min × planned_rpe` over the template's sessions, computed on render (`weekTotalLoad`, `lib/queries/weekTemplates.ts`), with a day holding an unscored session flagged. `sessions.planned_load` exists as a column (0003) and is written and read by nothing but the 0104 audit trigger — a dead column. | Nothing to build; drop or populate `planned_load` when the schedule board is next touched · — |
| ~~PATTERN-S7~~ | ~~C2~~ | ~~Every figure with its denominator and an exclusions sentence ("Nobody is excluded"); missing values as words; worst first.~~ **Built across all six reports, 2026-09-13** (`lib/reportFigures.ts`): compliance, injuries, squad weekly, training, testing and the athlete report ("11% · 4 of 35 submitted · last 28 days"; "Building baseline" on a load tile under its own 21-day floor; "No result", "No comparison", "None assigned", "Nothing submitted"). No dash stands in for a value on any of the six. | | |
| ~~PATTERN-S7~~ | ~~C3~~ | ~~The export dialog names the file before it is written; the audit row gains the row count; a medical export carries "Contains medical information. Handle under the club's data policy."~~ | **Built 2026-09-13** with S8 C8: `components/Dialog` is B11's one pattern (native `<dialog>`, `--w-dialog`, scrim, 44px buttons); `ExportDialog` on all six reports names the file ("Export compliance-2026-08-17-to-2026-09-13.csv"), states the window, the scope with its count, every filter, "15 rows, one per athlete", the medical line on the medic's injury copy, and the audit sentence; Download is the route, Cancel writes nothing. One descriptor (`lib/exportDescriptor.ts`) feeds the dialog, the file's `#` header (definition first, exported-by last) and the audit metadata (`file`, `rows`, `filters`, `medical`). Verified as Ruth on injuries: the dialog, the file's header, the audit row {file, rows: 10, filters: ["The medic's copy"], medical: true}. **Found:** the medic's injury CSV holds the same eight columns as the coach's — no diagnosis column exists in the export, so "the medic's copy" is the same rows with the line; the medic-only columns are a catalogue question. Guard `test:export-dialog`. | Done |
| PATTERN-S7 | C4 · **DECIDED · unbuilt** | One renderer for print and PDF. | **Ruled 2026-09-13 (batch B10): the PDF survives and Print opens it; printing becomes a two-step action, accepted.** *Verified 2026-09-14: not built — seven `@react-pdf` routes and the `@media print` block at `base.css:8129` both still exist.* D3's print tokens land with it (A18). | Build · medium |
| ~~PATTERN-S7~~ | ~~C5~~ | ~~The group filter reaches the export routes as the page reads it (the cookie), so a bare export URL is scoped the way the screen was.~~ **Already true — confirmed and pinned 2026-09-13.** All five report export routes go through `resolveGroupFilter` (URL, then the sticky cookie); the exports hub posts the page's resolved scope. The row's "reads `?groups=` only" was stale. Guarded in `test-pattern-s7.ts`. | | |
| ~~PATTERN-S7~~ | ~~C6~~ | ~~Analytics as four panels: definition line with n, zero-based bars with the axis in words, day/week grain summed or meaned by measure, the tap-persistent readout, "Not submitted" stubs, the club threshold as a named dated zone, suppression with one action, no export.~~ **Built 2026-09-14** — `lib/analyticsPanels.ts` (the rules), `AnalyticsPanel` (the plot), `/analytics` rewritten; `/analytics/build` removed with it (D2). The week-bar rule is in `docs/metrics.md`'s preamble · | | |
| ~~PATTERN-S7~~ | ~~C6 · the load panel's measure~~ | ~~Built as session load so analytics works for a free club.~~ **Superseded by Isabella 2026-09-14** (`docs/decisions/absence-rule.md`, "Analytics is premium"): analytics is not for a free club at all; GPS metrics join the panels as a select on the load panel (session load the default — 15 Sept: the measure most likely to have data on first open — and the GPS family; the heading follows the measure) and the destination is gated at the database (0125) as well as the route · | | |
| ~~PATTERN-S7~~ | ~~C7~~ | ~~The compliance report says its cutoff in words.~~ **Built 2026-09-13**: the two sentences on the Summary card — the RPE cutoff from `rpeClosesAt`'s rule, and that wellness has none. | | |
| ~~PATTERN-S7~~ | ~~C8~~ | ~~Suppression below five athletes with data on every report and panel — one shared rule.~~ **Built 2026-09-13** (`lib/smallSample.ts`): the positional band's and the profile shading's floors now read it; the training report's heat ramp, the testing report's median and quartiles, the compliance squad mean and the analytics builder's population mean ask it, each with the one notice ("Shading is off — 3 athletes have data, fewer than 5. The individual numbers are unchanged."). An ACWR's 21-of-28 and a rolling band's 10 observations are one athlete's floors and stay their own. | | |
| ~~PATTERN-S7~~ | ~~C9~~ | ~~The period control explains why the narrow choice is usually wrong; navigation both ways.~~ **Built 2026-09-13 on the compliance report** (`lib/periodNav.ts`) — the one report with a day anchor: "‹ Previous 7 days" / "Next 7 days ›" move `?to=` by the window's length (never past today; a season, a year and "all" are not walked), and under the control "Over 7 days one missed morning moves an athlete's rate by 14 points — read four weeks for the habit, 7 days for this week." The other reports resolve from today and have no anchor to walk — with C1's shell if wanted. | | |
| ~~PATTERN-S7~~ | ~~C10 / C11~~ | ~~The role note on the report; exports first under the title on a phone; the training report's phone reading is the PDF.~~ | **Built 2026-09-13.** C10: `lib/reportRoleNote.ts` from `access.ts`'s own sets — one line under the definition on injuries (medic / everyone else), compliance (the nutritionist) and the athlete report (three readings); null where a report reads the same for every role. C11: below 768px the shared header puts the eyebrow-and-exports row directly under the title by CSS order on `.rhead` (desktop untouched); the training board adds a phone-only line, "This board is laid out for a desktop. On a phone, read it as the PDF — Export PDF is above." | Done |
| ~~PATTERN-S7~~ | ~~D1~~ | ~~Readiness plotted "out of 5" on the wellness panel.~~ **Declined** — the same question as ATH-ADULT-12 D5, declined 2026-09-12 (MET-001 is 0–100 in both apps). | | |
| ~~PATTERN-S7~~ | ~~D2~~ | ~~`/analytics/build` — keep or remove.~~ | **Decided (batch A19), struck 2026-09-13**: `/analytics/build` stays until PATTERN-S7 C6 replaces it. **Removed 2026-09-14 with C6.** | Done |
| PATTERN-S7 | D3 | `--print-paper: #ffffff`, `--print-ink: #12161c` (the only theme-ignoring tokens), `--w-dialog: 640px`. | **`--w-dialog: 640px` dated 2026-09-13 in tokens.css with C3.** `--print-paper` and `--print-ink` wait for C4 (B10, one PDF renderer — with Isabella, not queued). | Half done: the print pair with C4 · — |
| PATTERN-S11 | inventory | Installability and offline — the board from `docs/platform-decision.md`. | **Step 1 inventory done 2026-09-13** (the record, "PATTERN-S11"): the manifest holds name, standalone, two colours and two icons (an `any` SVG, a 180px PNG) — no 192/512 PNG, no maskable, no splash, no iOS install teaching; **no service worker at all** (no app shell, no background sync, no push possible); four athlete queues in `localStorage` (`fydr-outbox-wellness/-training/-nutrition/-gym-set`, `lib/outbox.ts`), flushed only by `OutboxFlusher` on `/today` and on `online`, every flush, no backoff, no cap; staff writes not queued (the schedule's held week is `sessionStorage`); visible queue state and last-send time exist (S6 C1). Not designed. | Board next · the inventory names three open questions |
| PATTERN-S11 | "send now" | The platform decision asks for a manual "send now"; the S6 board deliberately has none ("the send is not the athlete's job"; the queue screen reads "there is nothing to press"). | Two rulings contradict. The platform decision is the later one and asks for it explicitly. **⚠ decision** | Recommend: a "Send now" on the queue screen only, never per item, the S6 words otherwise kept · small once the S11 foundation exists |
| PATTERN-S12 | inventory · **mechanism built, board not drawn** | Premium — the board from `docs/platform-decision.md`. **What S12 was for is now built without a board (2026-09-14/15): the plan page, D-20 without exception, `PlanGateCard`, the tier gate at the database (0119, 0125), the downgrade sentence and `org.tier.changed` audited (rows below). Whether a board is still wanted is Isabella's one-liner; the reviewer's reading is no.** | **Step 1 inventory done 2026-09-13** (the record, "PATTERN-S12"): the tier is one column (`core`/`performance`, no UI flips it, billing out of scope); premium is Analytics (destination), the analytics bar chart, the training report (both boards) and its exports, the athlete report's GPS panel, GPS vendor import and its exports, GPS leaderboards — every gate `isPremium` in the app; **at the database no policy reads the tier**, only `compute_leaderboard` (0094) refuses a gps board on `core` (and the dormant healthkit consent rule); `gps_records` reads are deliberately not tier-gated (SAR), so a Basic staff token can read GPS rows via PostgREST. Downgrade — **ruled the same day** (`docs/decisions/premium-downgrade.md`: kept and hidden under D-20, restored on upgrade, under the normal retention clock): the code already keeps and hides GPS data, refuses gps leaderboards and removes analytics; the one-place sentence, the retention run's coverage of `gps_records`, and the audit row on a tier flip are the gaps. The training-report card on the hub is a destination treated as a region under D-20. Not designed. | Board next · five open questions in the record |
| ~~PATTERN-S8~~ | ~~C1~~ | ~~The setup checklist a new club lands on: add athletes, make groups, set thresholds, invite staff, assign roles — each with a count and a done / outstanding state; a list of remaining defaults, not a gate.~~ | **Built 2026-09-13** (D8 answered: no creator means default): `/settings/setup` — five numbered steps with Done/Outstanding pills, counts with their nouns ("5 groups · 3 athletes in none", "0 of 6 lines changed — all Fydr defaults", "6 of 7 accounts signed in", "1 account has no role"), a primary button on each outstanding step, the five-segment bar, "N of 5 done", and the thresholds consequence card while they are outstanding; the hub's Club group carries "Setup checklist · 2 of 5 done" for good; the dashboard shows one status line with Continue setup while anything is outstanding (not the emphasised card, not a gate). Verified on scratch: 5 of 5 done as found; with the five thresholds' creator nulled, "4 of 5 done", the thresholds row outstanding with the card, the hub row "4 of 5 done", the dashboard line — then restored. Guard `test:setup-checklist`. | Done |
| ~~PATTERN-S8~~ | ~~C2~~ | ~~The settings hub in four groups on one 900px screen; cards with counts on a phone, the long lists one level down; Log out a real button.~~ | **Built 2026-09-13**: `/settings` is four cards — Club, People, Data, You — of destination rows with counts ("4 groups", "5 active", "37 active accounts", "0 open", "0 files", "1,988 in 90 days", "Two-factor required"); measured 900px tall at 1440 × 900 as the sport scientist (was 3,772), one column of stacked cards at 375 with 70–92px rows. A row a role cannot open states its reason from `access.ts`'s own sets instead of linking. The Plan card and Integrations moved to `/settings/club` (with the club details form), profile, avatar, password and two-factor to `/settings/profile` (the route-map's name), `#password` anchoring the two-factor notice. Log out stays the A1 button, at the foot of the You card. `src/lib/settingsHub.ts`, guard `test:settings-hub`. | Done |
| ~~PATTERN-S8~~ | ~~C3~~ | ~~The users list with search by name or email and filters by role and status; no sideways table at 375.~~ | **Built 2026-09-13**: search by name or email (was there) plus one role chip row and one status chip row, all 44px; the count line reads back with its denominator and the filters in words ("5 of 37 accounts · coaches"); an empty result is one sentence with a Clear filters button; `?q=&role=&status=` land the list filtered. The row is `.um-row` — name, role toggles, status — and stacks into a card below 768px: measured at 375 with 37 accounts, no sideways scroll on the page or any card. Athlete accounts stay in the list (invitations and linking live here); the board's staff-only table is not followed, "Athlete" is a chip instead. `src/lib/userFilters.ts`, guard `test:user-filters`. | Done |
| ~~PATTERN-S8~~ | ~~C4~~ | ~~A role change previews what it grants and what it removes before the button.~~ | **Built 2026-09-13**: `src/lib/roleGrants.ts` — 24 capabilities, each one of `access.ts`'s exported sets with a sentence (the guard fails if a set is added without one); `roleChangePreview(held, pending)` diffs the union before and after ("Adding Medic: gains 5 things and removes nothing; 14 things unchanged."), lists Grants and Removes, and states the rules it trips — D-25 nutritionist-plus-injury-role, nutritionist alone, athlete only, no role, removing Medic ("Clinical records they wrote stay") — with the effect line (next page load; audit log with your name). On `/settings/users/[id]` the chips move a pending set; the preview sits in the Roles card with Save roles and Cancel; nothing writes before Save. "What this user can see" now reads the same catalogue (the old four-role summary is gone). The list's inline toggles are gone: rows state roles as facts with a Change roles link to `#roles`. Refused chips are BlockedButtons (reason on tap), not `title`s. Run-verified on scratch: S&C added to Peter Ackland then removed, two `user_roles.changed` audit rows, roles back to coach. Guard `test:role-grants`. | Done |
| ~~PATTERN-S8~~ | ~~C5~~ | ~~Groups: a warning when a group in use by a session or programme is renamed or archived, with what happens.~~ | **Built 2026-09-13**: `fetchGroupUsage` counts live members, upcoming and past sessions (through `session_participants`), active programme assignments, live nutrition targets and rules, leaderboards and active thresholds; `src/lib/groupUsage.ts` turns them into sentences. Rename: a line above Save once the name differs ("Forwards is in use. Renaming it changes the name on 2 upcoming sessions, 2 active programme assignments, 1 nutrition target and 1 nutrition rule and 43 past sessions … including in the past. Nothing else changes …"). Archive: a card at the foot of the group page stating what stops, **what keeps running** (membership rows are untouched, so sessions still expect its members, programme assignments keep running, group targets keep applying — verified in `schedule.ts` and `resolve_nutrition_targets`, neither reads `groups.deleted_at`) and the way back; Archive arms the card, "Archive Forwards" + "Keep it" write nothing until the second press. Rehab groups: a BlockedButton with the reason. Run-verified on scratch: Academy archived and restored, `deleted_at` back to null. **Found:** `groups` has no audit trigger — an archive or rename writes no audit row (the audit-widening item). Guard `test:group-usage`. | Done |
| ~~PATTERN-S8~~ | ~~C6~~ | ~~Thresholds in plain English with the owner and date the dashboard quotes; a 28-day preview of how many athletes a rule would have flagged, writing nothing.~~ | **Built 2026-09-13**: every rule carries "Set by Jane Pemberton · Thu 6 Aug" / "One of the club defaults · unchanged since …" (D8: no creator means default; the same `updated_at` the dashboard quotes). The preview IS the evaluator, not a second compute: migration 0113 `preview_threshold_rule(fields…)` / `preview_threshold(id)` run 0052/0053's own `_threshold_breach_on_day` with its gap rule over the trailing 28 club-local days for every athlete in scope, STABLE, cooldown ignored, sport scientist and coach only (pgTAP 690, 19 assertions: writes nothing, the medic and anonymous get nothing, another club's rule previews as nothing). On each rule and above Create on the new-rule form: "3 of 30 athletes · last 28 days", names most days first with day counts, "0 of 30" never a bare 0, and the caveat (writes nothing; counts days, not flags). ~1s per preview on scratch, on demand. Guard `test:threshold-preview`. | Done |
| ~~PATTERN-S8~~ | ~~C7~~ | ~~The audit log's filters as a sheet on a phone (the button reads back the count it will show), a person filter, the header carrying the active filter count.~~ | **Built 2026-09-13**: below 768px the filter card and the kind chips give way to a 44px "Filters · N" button and the staff shell's sheet (scrim, dialog) holding Kind, From, To, Staff member (the person filter, every staff account), Athlete, Search and an all-time switch; the sheet's button reads back the count from `/settings/audit/count` (the page's own query builder with `head:true`, the same 30-day default, the same sport-scientist gate — 403 for anyone else) as the controls change: "Show 128 entries", "Show 1 entry", "Show — nothing matches", "Counting…". The header carries a pill "N filters" for the filters a person chose — the 30-day default is not sent and not counted, on desktop or phone. Verified on scratch at 375: open → Counting… → kind `route` → "Show 2 entries" → apply → `?type=route`, 2 rows, "1 filter". Guard `test:audit-filter-sheet`. | Done |
| ~~PATTERN-S8~~ | ~~C8~~ | ~~The export file reads its own filters back; the audit row carries the row count.~~ | **Built 2026-09-13** with S7 C3: every report CSV's header is the descriptor's sentences (report and window, scope with its count, filters or "No filter beyond the window and the scope above.", rows with their noun, the medical line where it applies, exported by whom and when); the export builder's files do the same per domain ("Body mass omitted — not visible to your role" on a coach's wellness file) and its audit row carries `files` (domain and rows each) and `rows`. | Done |
| ~~PATTERN-S8~~ | ~~C9~~ | ~~Retention states its consequence before the button: rows, athletes, which are current.~~ | **Built 2026-09-13**: the preview now carries the athletes behind the injury records it would redact and how many are still on the squad, by name; `lib/retentionWords.ts` says the consequence in the same card as Run — "Running now will delete 3 import files older than 30 days, and redact the clinical detail on 14 closed injury records and archive them. This cannot be undone." / "Those records belong to 11 athletes, 2 of them still on the squad: …" / the preview-only rows as visibility / "Nothing is eligible today … Run has nothing to do." (Run not offered). Run now opens B11's dialog (bad edge, "Run retention"). The preview is logged as `retention.preview` with the counts and no rows or names (A6, closes D4). Run-verified on scratch: a seeded 40-day-old import batch → "delete 1 import file" → the dialog → Run retention → "1 import file deleted", `retention.run` {import_batches_deleted: 1}, the row gone. Guard `test:retention-consequence`. | Done |
| ~~PATTERN-S8~~ | ~~C10~~ | ~~Subject access: the staff side and the athlete side on one pattern, reusing the athlete report where it fits.~~ | **Built 2026-09-13**: `lib/subjectAccess/words.ts` — one set of words for both sides (`sarDueWords` "19 days left"/"due today"/"3 days overdue" with tone, `sarNextStep` "Waiting on the medic to review the clinical notes" / "…waiting on the sport scientist to release the pack" / "Released", `sarAthleteLine`, `sarPackContents` = the pack's own manifest, `SAR_HOW_TO_ASK`). Staff queue rows carry the due words and the next step. **The athlete side is new**: `/me/privacy` (route-map 43, from Me) — the request sentence from the athlete's own `sar_requests` rows (migration 0114 `sar_requests_athlete_select`, read only, clinical reviews closed; pgTAP 700, 11), the four steps to ask, who sees what, what the club holds (the manifest). "Reusing the athlete report" is not what fits after the self-export removal: the pack's manifest is the shared thing, so the athlete reads the same category list the pack is made from. Run-verified on scratch: a request opened for Dan Okonkwo shows on both sides in the same words; James Barnes reads his released one. Guard `test:subject-access-pattern`. | Done |
| ~~PATTERN-S8~~ | ~~C11~~ | ~~An import holds what it cannot match, named on screen; matching once teaches the vendor's spelling.~~ | **Built 2026-09-13, schema first**: migration 0115 — `import_held_rows` (a valid row under an unmatched or ambiguous name, kept with its date and values, resolved by status held → matched / discarded, never deleted) and `athlete_import_aliases` (one remembered spelling per club, normalised at the table, never re-pointed); both the sport scientist's, pgTAP 710 (18). The parser reads the aliases before giving up on a name and holds instead of rejecting; the imports page shows the held rows above the history with a picker, Match (writes the GPS row with the import's own upsert, remembers the spelling, says both) and Discard. Two spellings of one athlete on one date reject the second naming the first. Run-verified on scratch: a file with "D Okonkwo (GK)" and "K Mercer" → 1 in, 2 held; Match → record written, alias remembered; Discard → nothing written; the same file again → the (GK) row resolves to Dan through the alias. Guard `test:import-held`. **Found:** a discarded spelling is held again next time (no "never import this name" memory) — small follow-up if a club asks. | Done |
| ~~PATTERN-S8~~ | ~~C12~~ | ~~Tables become cards below 900px: audit, retention, subject access.~~ | **Built 2026-09-13**: one rule, `table.tbl.tbl-cards`, at the declared 900px boundary — a row becomes a bordered card, a cell a labelled line (`data-label` = the column heading, drawn as a block above the value so a cell with several children keeps them together), the heading row hidden but not removed. Applied to the audit log, the retention schedule, the nightly reports, the retention preview and the subject access queue; the action cell carries no label. Measured at 375: no sideways scroll on any of the three pages, no overflow inside any card; at 1440 the tables are unchanged. Guard `test:table-cards`. | Done |
| ~~PATTERN-S8~~ | ~~C13~~ | ~~The athlete's notifications screen with "Mute everything" as the one switch at the top, no confirmation, and the sentence that nothing stops being recorded.~~ | **Built 2026-09-13**: the top card is one `role="switch"` — "Mute everything", the whole row the control (56px), no confirmation; on = `muteAll`, off = `unmuteAll` (restores each type's prior state, §0z), state read from the rows; the sentence beneath: "Nothing stops being recorded. Your check-ins, ratings and sessions are still expected and still count, and no coach is told you muted this. Availability changes and privacy notices still reach you — those two never turn off." The board's 52×30 track is the product's existing 40×22 switch shape (the heat toggle's: --hair off, --accent on) — no new shape (§0.01). Run-verified as Dan on scratch: mute → every push chip off, persists on reload; switch off → 1 on / 13 off, as before. Guard `test:notification-mute-restore` repointed. | Done |
| ~~Children's Code~~ | ~~default 1~~ | ~~Under-18 and academy athletes are excluded from ranked boards and streak mechanics by default; a guardian-recorded consent lifts it.~~ | **Ruled and built 2026-09-13.** Ruling one: "academy" means under 18 by date of birth, not membership of a group called Academy — age is the legal trigger, a group name is a club convention; closed with age only. Ruling two: **the minor's own opt-in is removed entirely until the guardian route (S9) exists** — migration 0116 re-creates `compute_leaderboard` with `not athlete_is_minor(a.id)` alone (a self-granted `leaderboard_visibility` row lifts nothing; rows stay as history), the staff wall applies the same age-only rule, `LeaderboardConsentToggle` and its grant/read queries are deleted, Me › Leaderboards states the rule and offers a minor no control (the adult opt-out is absent for them too), Me's row reads "Not named". pgTAP 040/200/290 rewritten to the ruling. Verified on scratch: Kai Mercer given a live self-consent row → 0 rows on the published board, absent from the wall, "1 under-18 athlete is not ranked …". Guard `test:ranked-board-minors`. The guardian route is S9's. | Done |
| ~~Children's Code~~ | ~~default 2~~ | ~~Reminders default off, for everybody.~~ | **Built, then ruled 2026-09-13 (ruling three): athlete defaults stay off exactly as built — every disableable athlete push and email off, the two undisableable P1 notices on; staff alerts restored to on** (high-severity flag, digest, escalation, availability changed, injury reported, RTP due, restriction conflict, weekly compliance digest, import failed, integration failing, privacy notice declined) — the reasoning is the Children's Code, about children; a medic missing an injury alert is a safety problem. Stored rows untouched (null = inherit). Guard `test:reminder-defaults`. | Done |
| Children's Code | registration | No field anywhere records that a medic is a registered practitioner — the gap that decides whether the Article 9 health-care condition is available, and so the wording of the consent flow. | Verified 2026-09-13: no registration number, body or status on `users`, `user_roles` or `organisations`; nothing in migrations or `src/lib`. **Isabella's, with a solicitor.** Not built. | Decide · — |
| ~~PATTERN-S8~~ | ~~D1~~ | ~~Apple Health is listed as a Premium feature.~~ | **Struck 2026-09-13 — Apple Health is removed from the product** (`docs/platform-decision.md`): there is no native iOS app and none is planned, and it was the only capability that required one. Not deferred, not parked. The staff Settings row, the Premium-list line, `lib/queries/healthkit.ts` and the consent toggle are deleted; the `device_metrics` table and the `healthkit_sync` consent purpose stay in the database, dormant. | Done (removed) |
| ~~Apple Health~~ | ~~consent rows~~ | ~~34 `healthkit_sync` consent rows remain on production.~~ **Kept, by decision — nothing to build.** | **Checked 2026-09-13**: keep the rows — consent history is not erased. The surface that collected them is already gone: the toggle left `/me` on 8 September, and `HealthkitConsentToggle` and `lib/queries/healthkit.ts` were deleted on 13 September (ea3b3e6) with the rest of the Apple Health removal; nothing reads or writes `healthkit_sync` now, and the `consent_purpose` enum value stays for the history's sake. | Done — nothing to build |
| ~~PATTERN-S8~~ | ~~D2~~ | ~~Catapult drawn as "Connected · Sync now".~~ | **Decided (batch A3), struck 2026-09-13**: Catapult stays "Import files" — a CSV drop, not a connection; the board is wrong. Built as A7. | Done |
| ~~PATTERN-S8~~ | ~~D3~~ | ~~Session and schedule changes written to the audit log.~~ | **Decided (batch A5), struck 2026-09-13**: they join the next audit-trigger batch (the architecture list's item); the log says so meanwhile (A5). Not the builder's until that batch is queued. | Done as a decision |
| ~~PATTERN-S8~~ | ~~D4~~ | ~~Whether a retention preview is itself logged, and its row list retained.~~ | **Decided (batch A6) and built with C9, 2026-09-13**: logged as its own action, `retention.preview`, with the counts per category and the athlete totals; no rows and no names retained. | Done |
| ~~PATTERN-S8~~ | ~~D5~~ | ~~Revoking an invitation kills the link at once.~~ | Read 2026-09-13: no revoke control; the invite is a single-use Supabase token nothing invalidates early; a deactivated account's unfollowed invite still signed the person in (the access-token hook then handed them an empty token). **Ruled (batch B1) and built 2026-09-13: deactivate IS the revoke.** `POST /settings/users/[id]/status` (sport scientist only, never on their own account, the target scoped to the session's org) writes `users.status` through the same RLS write and refusal words, then bans the auth user (`updateUserById`, `ban_duration` 100 years / `none`), so the invite, a magic link and a password sign-in are all refused at the token from the same moment; reactivating lifts the ban. Both panels call the route; the caption says both consequences before the press; a failed ban is reported as "deactivated in Fydr, but the link may still work" with the audit row (`user.invites_revoked`, `sign_in_blocked_at_auth`) recording which happened. Verified on scratch: a test coach with an outstanding invite, deactivated as Jane through the detail page → `banned_until` 2126, the invite link → `/login?e=invite-link`; reactivated → ban lifted; the audit trail carries all four actions. Guard `test:revoke`. | Done |
| ~~Decision batch~~ | ~~tier gate to the database~~ | ~~Keep and hide is enforced only in the app; a base club's GPS data is reachable through a direct API call.~~ | **Built 2026-09-13**, migration 0119: every staff SELECT/INSERT/UPDATE policy on `gps_records`, `import_batches`, `import_held_rows` and `athlete_import_aliases` gains `auth_org_is_premium()` (0061's helper). A Basic club's staff read no rows — empty, never an error — and the rows return on upgrade. **The written, tested exception** (pgTAP 740, 18): the service role (the SAR pack, `sarPackAssembly.ts`) and the athlete's own rows (`gps_records_self_select`, Article 15 is theirs). `compute_leaderboard`'s gate (0094) unchanged; the analytics bar chart stays app-side (a premium view of free rows). Three role tests (080, 100, 710) put the fixture club on Premium first, since they test roles, not tier. Guard `test:tier-gate-rows`; 12-product-tiers.md §8 records it. | Done |
| ~~PATTERN-S9~~ | ~~1–6, the two named rows~~ | ~~Athlete consent, first run, install teaching.~~ | **Built and gated 2026-09-14** (overnight): 0120 (two records, only one named; in_data; the guardian field and route; the gates at the row; the guard; the seed backfill), 0121 (athlete_devices); artboards 1–6, the withdrawal row with LEGAL-3F beside it, the reachability figure. **Not complete while any LEGAL- placeholder is unfilled** — LEGAL-1A, 3A, 3B, 3C, 3D, 3E, 3F, 4A all drawn, undrafted. Spec `docs/athlete/screens/21-consent-first-run.md`. Guard `test:s9-consent`. | Built, gated |
| ~~PATTERN-S9~~ | ~~body site and side~~ | ~~Artboard 2's coach card says "no body site and no side"; today a coach sees the body area and side of an open injury.~~ **Closed 2026-09-14 with PATTERN-S3 C8** — the card is worded from the club's own setting: the board's "no body site or side" while off, the honest wider sentence while a club has turned it on · | | |
| ~~Match participation~~ | ~~Step 1 (5a)~~ | ~~Who played and for how many minutes is not recorded anywhere.~~ **Both halves approved and built 2026-09-15** — migration 0127 `match_participation` (started, came on, minutes nullable; SESSION_EDIT writes, every staff role reads, the athlete their own; audited); the post-match sheet on the fixture; the attach action on a fixture without a session and on an unlinked match session (the 11 July orphan stays until a coach attaches it — no backfill); the match report, the eighth, at `/reports/match` with the confirmed sentence, CSV and PDF · | | |
| ~~Premium contents~~ | ~~Step 1 (5b)~~ | ~~`docs/12-product-tiers.md` §3.2 still says the analytics builder is "Both" while §3.1 row 27 (and the code) make the whole destination Premium.~~ **Approved and done 2026-09-15**: §3.2's analytics row reads "P, the whole destination (row 27)"; §3.3 marked closed · | | |
| ~~Premium contents~~ | ~~Step 1 (5b)~~ | ~~On Basic the athlete report's GPS columns vanish silently; the absence rule wants a premium region inside a base page to show an upsell card.~~ **Approved and built 2026-09-15**: a `PlanGateCard` on the Load page's GPS section, one line on the summary Load card, the PDF names the section's absence (`lib/premiumWords.ts`) · | | |
| ~~Premium contents~~ | ~~Step 1 (5b)~~ | ~~A downgraded club keeps its GPS flags (raised while paying) with no word that the rule no longer reads.~~ **Approved and built 2026-09-15**: the domain word reads "GPS · Premium rule, dormant on the Basic plan" on the Flags screen and the profile's Flags card (`flagDomainWord`); the kept-history sentence goes on the plan page · | | |
| ~~Premium~~ | ~~the plan page (decided 2026-09-14)~~ | ~~Where a basic club learns premium exists: one place — a plan page in Settings.~~ **Built 2026-09-15** — `/settings/plan` (sport scientist), the inventory from `lib/premiumWords.ts`, the price a placeholder, the kept-history sentence from `premium_history_kept()` (0126); with it the three gaps: `org.tier.changed` audited, the retention run retires GPS rows past the cutoff on any plan, the SAR pack skips retired rows · | | |
| ~~Premium~~ | ~~the PlanGate pages~~ | ~~The GPS report and the imports area still show a `PlanGate` upsell page at the URL on Basic.~~ **Approved and built 2026-09-15**: both refuse through the denied screen (logged), the GPS report card is absent from the reports index and the Vendor imports row from the hub on Basic; `PlanGate` (the page) removed, `PlanGateCard` (the region) stays — a GPS board's page shows the card in its frame · | | |
| PATTERN-S9 | finding 1 · the decline is visible | The squad list shows "no data consent" by name, beside "Saying no does not affect selection." | Operationally right and in tension with the sentence. Not resolved in code. **For the solicitor, beside LEGAL-3C.** | Decide · — |
| PATTERN-S9 | seed backfill | 0120 backfills the performance record (version `seed-pre-S9`) for every linked athlete so the staff screens did not empty; the health record is not backfilled. | All data is synthetic (the decision batch). **Decide:** keep the backfill, or require every existing athlete through 3A on their next open (recommended for a real club: yes — the flow's job). One unlinked Ashcombe athlete and the Walkthrough Reference row now read "not yet decided". | Decide · — |
| PATTERN-S9 | wellness under Article 9 | Sleep, soreness, mood and stress are block 1 (performance) on the board and gated by the performance record. | Whether they are health data under Article 9 is the solicitor's, beside LEGAL-3B. If yes, the wellness gate moves to the health record — one predicate change. | Decide · small |
| PATTERN-S9 | rule two of the password rules | "Not a password you already use somewhere else" cannot be checked by code; built as the athlete's own checkbox in the rule's row, counted in "n of 3 rules met". | The board draws a system tick. Recommend the checkbox stays: a tick the system cannot earn is a claim it cannot make. | Decide · — |
| PATTERN-S9 | finding 5 · ATH-ADULT-03's submit | The check-in's submit measures 53px; the board's committing height is 56px (`--tap-commit`). | Left alone as instructed; the restyle pass was cancelled. **For the accessibility sweep.** | Decide · one line |
| A11y baseline | why the guards missed it | `test:a11y-floor`, `test:nav-hit-floor`, `check:contrast` and `test:staff-phone-shell` §5 are all **source-level**: they read `base.css`, `tokens.css` or a `.tsx` and assert a rule or attribute is present; none renders a page. `a11y-floor` pins alerts and headings only. `nav-hit-floor` pins two athlete-shell controls and defers the staff `.back-btn` (29px) by its own header. `check:contrast` measures token hexes on `--bg` and `--surf` only — never a composited tint, never `--surf2` (dark `--faint` on it is 3.89:1, the most repeated failure), never a colour the stylesheet does not state (the browser's `#757575` placeholder), and its `KNOWN_BELOW_AA` exempts `warn-text` light and `accent` dark, both still shipping at 12–13px and 2.87:1. `staff-phone-shell` asserts a class list has `min-height: 44px`; anything not on the list is untested, and a listed rule can miss its element (`table.tbl .nm` pads the cell, not the link; `.btn-ghost`'s `min-height` does nothing on an inline `<a>`). | The full answer, with the measurements behind each claim, is `docs/a11y-sweep-baseline-2026-09-15.md` "Why the existing guards did not catch this" (2026-09-15). Independent of both closing passes: it is about what the guards read, which neither pass changes. | **A rendered check, not a longer list**: `scripts/a11y-sweep.mjs` as a fixture run in `prebuild` or nightly, as each role, asserting zero contrast and tap-target rows above an agreed floor; `KNOWN_BELOW_AA` retired once its two entries are fixed. Decide · medium |
| A11y baseline | the measurements | Coach, medic and S&C at 1440 and 390 in both themes, at commit `272d625`: 42 contrast rules below 4.5:1 (7 of them `aria-disabled` and in scope), 44 controls under 44px (19 at 390, the constitution's own floor), the rest of the roles not run. | **Stale by design** (Isabella, 2026-09-15): conformance moves colour, spacing and radius values first, so the sweep re-runs from the top against the settled product. Kept in `docs/a11y-sweep-baseline-2026-09-15.md` so the re-run can say what conformance moved. | Not a defect list. Re-derive after conformance · — |
| ~~A11y baseline~~ | ~~rulings visible now~~ | ~~Five questions the re-run will ask that no value change answers: whether the 44px floor applies at desktop width (the constitution says "on a phone", §0au scoped the staff floor to below 768, the sweep prompt says any surface); the `--o-disabled` fade on BlockedButtons (no text token clears 4.5:1 through 0.45); `--lb-row-selected` single-valued in dark (1.09:1 under `--text`, a ground problem); a `.field::placeholder` rule with an existing token; `--warn-text` light → `--warn-pill-text` at small sizes.~~ **All five ruled in `docs/decisions/decision-batch-2026-09-14.md` (#8–#12) and the four fixes built 2026-09-16**: no tap-target minimum on desktop (44px is a touch rule); the 45% fade stays, recorded as deliberate at `--o-disabled`; `--lb-row-selected` has a dark value (#1f2f56, `--text` 11.2:1) and `.lbw-age` reads `--muted-on-tint`; `input::placeholder` / `textarea::placeholder` read `--faint-on-tint` (4.90 light / 5.03 dark on `--field`); amber at 12–13px reads `--warn-pill-text` (7.3:1) — the batch named `--on-warn-strong`, which is the failing value itself (#b07d0a) and theme-neutral; `--warn-pill-text` is its darker sibling with a dark value, and in light it is System A's `--on-warn` exactly. | | Done |
| ~~System A adoption~~ | ~~--sidebar-w~~ | ~~System A's shell token says 214px; the built sidebar (`.app`'s grid, `base.css`) is 236px.~~ **Deliberate divergence, Isabella 2026-09-15** (`docs/decisions/design-system-adoption.md`): that is layout, layout was out of scope for the whole adoption, and moving a sidebar moves every page's content column. `--sidebar-w` reads 236px — the token says what the product does. Not a miss. | | Done · none |
| Performance pass | which routes get a skeleton — **GATED** | Five routes carry a streaming `loading.tsx` (dashboard, GPS report, athlete report, squad weekly, player profile): the ones the 16 Sept measurement left at 8–20 sequential query rounds after the fixes, ~1s+ from this machine. | **Isabella, 2026-09-16: the production re-measurement is the gate, not a follow-up.** The numbers are a laptop talking to Ireland at 55–70ms a round trip; on Vercel the function sits in Dublin beside the database at one or two, so the pages may already be fast enough that every skeleton flashes. The round-trip work stands on its own; the skeletons have to earn their place. | **After the deploy, re-measure on production with `FYDR_QUERY_TIMING=1` for a window** (the recipe is in `docs/perf-measurements-2026-09-16.md`) **and remove every skeleton whose route comes in under about 300ms** · small |
| Performance pass | the four dependent chains | `/dashboard` (20 rounds), `/reports/squad` (12), `/squad/[id]` (10), `/reports/athlete/[id]` (11) still wait on lookups that each need the one before, inside `queries/dashboard.ts`, `squadWeeklyReport.ts`, `playerProfile.ts`, `athleteReport.ts`. | Restructuring each module to fetch its ids in one round and its rows in a second is a day's work each; not tonight's. They carry skeletons meanwhile. | **Do the dashboard first** — it is the landing page and the deepest chain · large |
| Performance pass | the profile skeleton's reach | A `loading.tsx` at `squad/[athleteId]` also wraps `/wellness`, `/gym`, `/nutrition`, `/availability` beneath it, so those show the profile's skeleton shape while they load. | Next's boundary is per segment; a child gets its own only by having its own file. | Give each domain page its own if its shape jars; otherwise leave · small |
| ~~Focus ring~~ | ~~dark's colour~~ | ~~Isabella asked for 3px of the accent at a higher alpha than `--ring-select`, visible in dark. The dark accent (`#2a6ddf`) at full alpha is 2.87:1 on dark `--bg` — under the 3:1 floor — so dark's `--ring-focus` is the cyan the theme already uses for focus (`--focus` `#33b6ff`, 6.1:1), not the accent.~~ **Approved, Isabella 2026-09-16, as a deliberate divergence**: the dark accent cannot clear 3:1 on a dark ground at any alpha, so the instruction to use the accent was wrong for dark; the theme's own cyan at 6.1:1 is right. Recorded in `docs/decisions/design-system-adoption.md`. | | Done · none |
| Performance pass | the timer stays in the code | `src/lib/supabase/queryTiming.ts` is compiled into every build, inert unless `FYDR_QUERY_TIMING=1` is in the server's environment; with it set, every Supabase request's path and duration go to stderr. | Kept so the measurement can be repeated on production; nothing is stored or sent. | Keep; never set the flag on production's environment except for a measured window · none |
| PATTERN-S9 | the guardian link's rate limit | guardian_decide is anon-callable by token (64 hex chars, sha256 stored, single use, seven days). | No limiter on attempts. Recommend one with the S11 send path. | With S11 · small |
| ~~PATTERN-S8~~ | ~~D6~~ | ~~"Export links expire after seven days".~~ | **Decided (batch A2), struck 2026-09-13**: no such sentence — exports are direct downloads, there is no link to expire. | Done |
| ~~PATTERN-S8~~ | ~~D7~~ | ~~Role change timing stated as "next page load".~~ | **Decided (batch A1), struck 2026-09-13**: "next request", which is what the code does (0010's claims_version bump). C4's effect line says "Takes effect on their next page load — a screen they already have open stays until they move": the same fact for a reader, since a request is what a page load is to them; the words "next request" are the spec's. | Done |
| ~~PATTERN-S8~~ | ~~D8~~ | ~~The checklist's "thresholds still default" step.~~ | **Decided (batch A7), struck 2026-09-13**: "no creator" is the marker for a default threshold. Built into C6's owner line and C1's checklist. | Done |
| ~~PATTERN-S8~~ | ~~D9~~ | ~~A saved report reverts to the whole squad when its group is archived, and says so.~~ | **Built 2026-09-13 (Isabella's ruling, with the §0ak cookie fix)**: `resolveGroupFilter` checks every requested id, URL or cookie, against the live groups once per request and drops the rest, so every scope on every screen is a live group's; the staff shell rewrites the cookie without the dropped ids and says so once ("Filter updated: Leadership was archived. Showing Whole squad."); the scope label never reads "1 unknown group". No saved-report entity exists — the cookie and links were the "saved report". | Done |
| ~~RPE package~~ | ~~one · club setting~~ | ~~RPE becomes a club setting; when off, every dependent surface says so rather than showing an empty column or a zero (the absence rule).~~ | **Built 2026-09-13** (`docs/decisions/decision-batch-2026-09-13.md`). Migration 0118: `organisations.collects_rpe` (default on) and `generate_compliance_expectations` writes no `training_rpe` row while it is off; pgTAP 730. The switch is a card on Settings › Club (`#rpe`), the sport scientist's, both consequences printed before it is pressed, audited as `org.collects_rpe.changed`. Every surface that rests on the rating carries the off words from one file (`lib/rpeSetting.ts`): the compliance figure ("Counted: …" — the addendum's "which entry types it counted" — and "Session ratings are not counted …"; the RPE domain card "Not collected by this club"; the CSV caption; RPE expectations generated while on are kept but not counted while off), the dashboard's RPE track (a sentence, not a bar), the effort board page and the manage list, the analytics presets and builder (ACWR, load and RPE metrics keep their card and control; the plot gives way to the sentence), the squad report's load section, the athlete report's load card and the profile's ACWR dial; the athlete's Today lists no "Rate" row, `/rpe/[session]` is a "Nothing to rate" card, My data's sessions tab says so in the athlete's words. Guard `test:rpe-setting`. **Open, not built:** the per-set gym RPE and week-template `planned_rpe` are not this setting's (they stay 1–10 and on); the RPE push prompt and nudge follow the expectation, so an off club sends none once push exists. | Done |
| RPE package | gym session RPE | The gym logger's per-set RPE and the session-level gym rating: do they follow the club setting? | Raised in the sweep report (`docs/overnight-records-2026-09-12.md`). The setting governs the **session** rating (CR-10 on `training_entries`); the per-set gym RPE is a lifting cue on `gym_set_logs`, 1–10, and `planned_rpe` on templates is planning. Recommend: leave both outside the setting — a club that does not want athletes rating training may still want the S&C's per-set cue, and neither feeds load, compliance or a board. | Decide · small either way |
| ~~RPE package~~ | ~~two · one tap on Today~~ | ~~The prompt is one tap, not a form: a row on Today carrying the scale itself, no sheet.~~ | **Built 2026-09-13** (`components/TodayRpeRow`). The rating row on Today carries the CR-10 grid the screen draws (the same 58px cells, buttons not radios so an arrow key cannot send); one tap sends the rating with the session's scheduled minutes through the same outbox path as the form; the other cells disable the moment a tap lands; the row becomes its own receipt ("Recovery run rated 7 · Hard" then "Sent…" or "Waiting to send — saved on this phone…") and stays — no auto-advance; a waiting receipt turns to Sent when the entry leaves the outbox (`OUTBOX_CHANGED_EVENT`). "45 min · change or add a note" on the row's line still opens `/rpe/[id]`, which is now the long form (minutes, note), the deep-link destination and the home of the closed/already-rated/off states; a session with no scheduled length hands over to it. Guard `test:today-rpe-row`. **Open:** whether `/rpe/[id]` should lose its own form once push deep-links land on Today (S9) — recommend keep, for the minutes and the note. | Done |
| ~~Training report split~~ | ~~GPS report · Training load report~~ | ~~The existing per-session GPS board becomes the GPS report, premium; the RPE × minutes report is built as the Training load report, every club, the seventh.~~ | **Built 2026-09-13.** `/reports/gps` (key `gps`; `/reports/training` is a permanent redirect carrying the query; exports `gps-report-*`); `/reports/training-load` (key `trainingLoad`, `REPORT_ACCESS`), its colocated period module (month by default, `day` refused, the compliance anchor rule), CSV and PDF; `lib/queries/trainingLoadReport.ts` counts only required `training_rpe` expectations, sums `session_load` through the current-revision view, never converts an absent rating to 0 ("No ratings" in words on screen and in the file; a 0 rating is a real load of 0). The addendum's sentence on screen, in the CSV's first line and under the PDF's title; the off state on the page (with a link to Settings › Club), in the CSV and in the PDF, and on the hub card's line. Hub: seven cards, "Training load" beside "GPS report", a `training` tone composed from the pitch domain's tokens. Specs `23-gps-report.md` (renamed), `65-training-load-report.md` (new), `17-reports-hub.md`, route map, IA, tiers, the working catalogue rows 1, 2 and 8. **Found and fixed on the hub:** the injuries card's tone is `medic` and the CSS rule was still `medical` (pre-rename), so it alone had no tint; the rule now matches. **Open:** whether a rating for a session the athlete was not expected at should ever count here (today: no, by the definition). | Done |
| ~~Four small things~~ | ~~sidebar footer~~ | ~~The sidebar footer prints the raw role enum ("sport_scientist").~~ | **Fixed 2026-09-13**: both staff shells' footers (the desktop sidebar's `.nav-who`, the phone More sheet's `.ph-sheet-who`) print `staffRoleLabel(roles)` — "sport scientist", "coach and S&C" — the table the exports intro and the refusal screen already read. Pinned in `test:role-model`. IA §4.1 records the footer. | Done |
| Four small things | groups audit trigger | `groups` has no audit trigger: a rename or archive writes no audit row (found at PATTERN-S8 C5). | **Queued with the audit-trigger batch** (Isabella, 2026-09-13; the architecture list's "Audit trigger widening: 26 of 59 tables", where session and schedule changes already sit — S8 D3). Not built on its own: one batch, one migration, one volume check, so `groups` joins the list rather than getting a trigger of its own. Nothing to do here until that batch is queued. | With the audit batch · — |
| Four small things | medic's injury CSV | The catalogue's confirmed injury sentence promises "Diagnosis, mechanism and clinical notes appear only in the medic's copy", but the CSV holds the same eight columns for both roles (found at PATTERN-S7 C3): "the medic's copy" today is the same rows with the confidentiality line. | **Proposal (Isabella asked for one, 2026-09-13).** When `isMedical`, `reports/injuries/export/route.ts` appends four columns after Expected return, read from `injury_clinical` through the medic-only path the page already uses: **Diagnosis** (`diagnosis`), **Mechanism** (`mechanism`), **Severity** (`severity`, the enum in words), **Clinical notes** (`clinical_notes`, the one column no athlete ever sees — 0005's comment; in the file only because the file is the medic's and carries the medical line). Leave `tissue_type`, `imaging`, `referral` and `treatment_plan` out of the CSV: the sentence names three things, and a spreadsheet of referrals and treatment plans is a clinical record system, which Fydr is not (CLAUDE.md §7). The coach's copy is unchanged. The PDF follows the same rule (the medic's copy gets the same four columns). Row counts and the audit metadata already say `medical: true`. The definition sentence then reads true. Guard: `test:export-dialog` gains "the medic's CSV carries the four columns; the coach's does not". Cost: small — one query branch, four columns, two routes. **Decide:** whether clinical notes belong in an export at all, or the sentence should read "Diagnosis and mechanism appear only in the medic's copy". Recommend the four columns as proposed; the notes are already on the medic's screen and the file is the medic's. | Decide · small |
| Four small things | re-held discarded import spelling | A spelling the sport scientist discarded is held again the next time a file carries it (found at PATTERN-S8 C11: no "never import this name" memory). | **A sheet item, not a fix** (Isabella, 2026-09-13). Two shapes if it is ever wanted: (a) a `discarded` alias on `athlete_import_aliases` (a spelling that resolves to nobody, skipped silently with a count in the import's own summary — "2 rows skipped: names this club has told Fydr to ignore"); (b) leave it held every time, since a held row costs one look and a silent skip can lose a real athlete under a new spelling. Recommend (b) — today's behaviour — until a club asks; a held row that says why it is held is the constitution's "missing is words". | Decide when a club asks · — |

## (d) Needs a new token

| Flow | ID | What it changes | Why it isn't built | Recommendation · cost |
|---|---|---|---|---|
| ~~ATH-ADULT-09~~ | ~~B1~~ | ~~The logger's numbers at 48px.~~ **Built** with C1 on the existing `--fs-48` step (no `--t-num-hero` token: the value exists; the summary's totals read the same step) · | | |
| ~~ATH-ADULT-09~~ | ~~B2~~ | ~~`--hit-lg` 56px and `--hit-md` 52px.~~ **Built** — approved 2026-09-12 with C1; both in `tokens.css` dated with Isabella's name, read by `.gl-primary` and `.gl-step` only; 06-design-system.md §11.1 · | | |
| ~~ATH-ADULT-09~~ | ~~B3~~ | ~~The set chip fills over 300ms on `--ease-ring`.~~ **DECLINED 2026-09-12 ("motion tokens" in the header) and again with 09 C1 ("the fill is instant").** Note: System A's `motion.css` was adopted 2026-09-14 — the app now has motion tokens; this row stays declined on its own merits, not on the old rule · | | |
| ~~ATH-ADULT-09~~ | ~~C1~~ | ~~The set-by-set rebuild.~~ **Built** (commit after `1bf38b9`): one exercise at a time, the two numbers at 48px between the steppers, the one primary labelled with the write, the chips as state display with no disabled control, the deviation as information, what is next beneath. The closed-session finding is closed with it: not-reached and current chips are not controls, and no logging primary is offered on a closed session. B3 (the chip-fill motion) stays declined — the fill is instant · | | |
| ATH-ADULT-10 | C2 · **two rulings conflict — Isabella** | A confirmation before finishing early ("Keep logging" primary; "Finish early" secondary with "4 sets short"; what is saved and where it goes). | **DECLINED 2026-09-12** (the header: "no confirmation before finishing early"). Then **batch B11 (2026-09-13) says the approved dialog pattern "unblocks the finish-early confirmation (ATH-ADULT-10 C2)" — and in the same paragraph rules that a dialog exists only where the action is destructive or irreversible, never for a confirmation that merely slows someone down.** Finishing early is irreversible since §0bc (a complete session takes no new set), which is the case for; "4 sets short" is already on the button, which is the case against. The dialog component exists (`components/Dialog`, with S7 C3). | **One line from Isabella: declined stands, or B11 reverses it** · small either way |
| ~~ATH-ADULT-11~~ | ~~C1~~ | ~~The footer swaps to Save correction / Cancel.~~ **Built** with 09 C1 (the correction opens from a logged chip or the strip's "Correct it"; the two numbers edit it) · | | |
| ~~ATH-ADULT-12~~ | ~~B1~~ | ~~Charts at `--chart-h: 84px` with a `--chart-stroke: 2.5px` line — the first plotted-series tokens in the athlete app.~~ **Fell with the C2 cut (2026-09-14, "The My data hero cards are CUT")** — the tokens were to land with the Sessions chart, which is not being built · | | |
| ~~ATH-ADULT-12~~ | ~~B2~~ | ~~Prior values in every chart in `--blue-200`.~~ **Built 2026-09-13 where a bar chart exists** — the gym weeks: prior completed weeks in `--wash-accent-strong`, the latest in the accent, a partial week its own mix. The readiness chart on My data is a line with dots (the board drew bars — 12 C2); its mapping goes with C2. | | |
| ~~ATH-ADULT-12~~ | ~~B4~~ | ~~The empty-state "Show this season" button at `--hit-lg` 56px.~~ **Built with 12 C6 at 44px** (`.empty-period` action, min-height 44px). | | |
| ~~ATH-ADULT-12~~ | ~~B5~~ | ~~Corrected markers in `--pill-accent` / a 3px `--border-accent-w` bar.~~ **The 3px bar DECLINED 2026-09-12 (the header); the neutral pill built with 13 C3 (2026-09-13).** Closed · | | |
| ATH-ADULT-12 | C8 | The period control on the title line as a menu (accent with a chevron; the current option carries the wash and a tick). | `PeriodSelector` is a shared staff `<select>` (ten pages); a custom menu is a new component shape. | Keep the select; if the menu is wanted it is an athlete-only component, own brief · medium |
| ~~STAFF-SS-01~~ | ~~B1~~ | ~~The dashboard's emphasised card in `--blue-100` + `--blue-200`; the 3px `--border-accent-w` bar on dense rows; `--pill-accent` + `--accent` actions.~~ **Done:** the lead card carries the wash family (2026-09-13, with C2); the 3px bar was declined 2026-09-12; the actions are the existing ghost/primary controls. | | |

---

**Not on this sheet, because they were built:** every A item of 04, 06, 08, 09 (A3, A4), 10
(A1), 12 (A1–A4), 13 (A1–A5), the STAFF-SS-01 shell (C1, A5, B1 for the shell), and
STAFF-SS-02-05's A1–A6 with PATTERN-S3's A1–A3, PATTERN-S4's A1–A3 and A5–A8, and
PATTERN-S5's A1 and A7 (2026-09-12) — see the records and the handovers in the commit log.
