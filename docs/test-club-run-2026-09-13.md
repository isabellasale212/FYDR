# Test-club run-through — the step log (started 2026-09-12 20:40, scratch only)

The plan is `docs/test-club-run-plan-2026-09-13.md`; Isabella's answers to its §0, 2026-09-12: scratch;
the reviewer runs `create:org --commit` (target confirmed on the dry run first); **Harlow Vale RFC,
senior women's squad** — the target market, and this club stays as the standing demo club; the roster
and staff renamed for a women's squad, keeping the under-18, the one who turns 18 in the season and
the rehab case; the GPS CSV from the app's template; the medic enrolled in TOTP with the secret kept
out of chat and the repo.

Nothing here touches production (`asbxorjytxsvrzefwzqp`). Every write is inside `org_id
d4174e69-2a77-45ac-a580-868a5c213d51`. Ashcombe, Conor and Matt Reid untouched. No code changes.

**Method.** Headless Chrome per identity (`--remote-debugging-port 9510–9517`, own profiles under
`~/.fydr-hv-profile-*`), driven over CDP; every screen at **1280×900 and 375×812**; the wrong side of
every gate probed at the database (PostgREST with the wrong role's own session — `rls-probe.mjs`) as
well as at the screen; `audit_log` read after every write (`hv-audit-ro.mjs`, read-only). Screenshots in
the session scratchpad `hv-shots/`.

**Passwords.** The reviewer never types a password. Each identity arrives through its single-use
invite link (which signs them in) or a scratch-only magic link (`link.mjs`, which refuses production);
the "Choose your password" step is rendered and checked but not completed. Flagged as a boundary in
the step log where it matters.

## Identities

| Role | Name | Sign-in | Profile port |
|---|---|---|---|
| Sport scientist, first admin | Priya Nair | p.nair@harlowvale.example | 9510 |
| Head coach | Bethan Rees | b.rees@harlowvale.example | 9511 |
| Medic (TOTP) | Dr Hannah Lisk | h.lisk@harlowvale.example | 9512 |
| S&C | Amara Okafor | a.okafor@harlowvale.example | 9513 |
| Nutritionist | Laura Quinn | l.quinn@harlowvale.example | 9514 |
| Second sport scientist | Samantha Byrne | s.byrne@harlowvale.example | 9515 |
| Athlete (adult) | see roster | | 9516 |
| Athlete (under 18) | see roster | | 9517 |

Roster (12) — the plan's appendix renamed for a women's squad:

| # | Name | DOB | Position | Group |
|---|---|---|---|---|
| 1 | Megan Hollis | 1998-03-14 | Loosehead prop | Forwards |
| 2 | Abena Mensah | 1996-11-02 | Hooker | Forwards |
| 3 | Erin Rooke | 2001-07-19 | Lock | Forwards |
| 4 | Bryony Talbot | 1999-01-28 | Flanker | Forwards |
| 5 | Zara Oduya | 2000-05-05 | No. 8 | Forwards |
| 6 | Siobhan Kavanagh | 1997-09-30 | Scrum-half | Backs |
| 7 | Ffion Prosser | 2002-02-11 | Fly-half | Backs |
| 8 | Chidinma Achebe | 1998-12-08 | Centre | Backs |
| 9 | Holly Fenwick | 2003-06-22 | Wing | Backs |
| 10 | Maeve Delaney | 1995-04-17 | Full-back | Backs — on a rehab path from day one |
| 11 | Tia Nkemelu | 2009-10-03 | Wing | Academy — **under 18 all season** |
| 12 | Caitlin Brice | 2008-11-20 | Centre | Academy — **turns 18 on 20 Nov 2026**, in season |

## Day 0 — the club exists

| Step | Actor | Action | Expected | Measured | Audit | Gate probes |
|---|---|---|---|---|---|---|
| 1 | reviewer, `create:org` | Dry run printed `database stfgzkuvczbpxyevxkak.supabase.co`, then `--commit` (20:40): org `d4174e69`, season 2026/27 (1 Jul 2026 – 30 Jun 2027, current), auth user `28b787f5`, users row active, `sport_scientist` granted, one invite link. Tier `performance` (= "Premium"), `Europe/London`, `GB`. | one org, one season, one admin, single-use link | as expected | *(not read — the script's own writes are service-role; see step 3 for the first audited write)* | **Invisible to Ashcombe at the database:** as Jane (SS, 9500) `organisations` returns her one row; `organisations?id=eq.d4174e69`, `seasons?org_id=eq.…`, `users?org_id=eq.…` all `0` rows. |
| 1b | anyone | The same invite link a second time (profile 9515, cold) | refused | `/login?e=invite-link` — refused, no session | — | — |
| 2 | Priya | Follows the link at 1280 | signed in, asked to choose a password | `/login/reset/confirm` — "Choose your password. You will be signed in as soon as it is set." Two password fields, 12-character minimum. **Password not set (reviewer rule).** | — | — |
| 2b | Priya | `/dashboard` without setting a password | ? | **Reaches the dashboard signed in** — the invite token alone is a session. She has an account with no password; her next sign-in needs a reset link. *Observation, not a defect: the confirm page says she "will be signed in as soon as it is set", so the copy promises a gate the flow does not have. Filed as F-05.* | — | — |
| 2c | Priya | Every sidebar row, empty club, 1280 and 375 | each screen's empty state | Dashboard: "No open flags right now", "0 of 0 today · not expected", "Nothing scheduled" ×6, "Fit and available · no restriction" — calm, with **no thresholds in existence** (F-03). Squad: "0 athletes in the squad" + "No athlete matches that filter." (F-02). Schedule: "No templates yet", seven empty days, Published pill blocked. Reports: six report cards, all reachable. Nutrition: "No plans yet", day types at defaults 6.0 / 7.5 / 3.5 g/kg, "No athletes in the current group filter." (F-02). Gym programme: "No programmes yet" + New programme + Exercise library. Leaderboard: "No athletes in this filter — clear the filter" (F-02). Analytics: "Nobody in scope — widen it" (F-02). Settings: Plan (Premium on), Integrations ("Catapult Openfield · Connected" — F-04), Thresholds "0 active", the eleven setting rows. No sideways scroll on any row at 375; the phone bar is Dashboard · Squad · Schedule · Flags · More on every row; every control ≥44px at 375. | — | — |
| 3 | Priya | Club details: logo uploaded (96px PNG) and saved; profile phone saved | saved, logo shown, two audit rows | "✓ Saved." twice; the logo renders at 48px from `storage/…/logos/d4174e69…`. Beneath the profile: "! Your role requires two-factor authentication. Set it up below — this club's policy is that coach, medical and admin accounts carry a second factor" (prompted, not enforced — plan §0 item 7). | **1 of 2**: `users.update {changed:[phone]}` by `28b787f5` as `sport_scientist`, with `ip_address`. The organisation save wrote **no row** — known: `organisations` is the shape exclusion in §0e ("it IS the org"), still unresolved. Recorded in the audit matrix. | — |
| 4 | Priya | Plan | Premium rows present | Settings › Plan: the toggle reads Premium; "GPS exports, Training report, Analytics · bar chart, Apple Health connection" listed; Analytics in her sidebar. | — | — |
| 5 | Priya | Groups: Forwards, Backs (positional), Academy (age), each with a description and colour, from `/settings/groups/new` | three groups, audited | "3 groups. Every athlete is in at least one group." — positional 2, age 1, reorder arrows on the positional pair. **Team selections: "No teams are set up yet, so there is nothing to select into"** and no way to make one — `teams` has an insert policy but no screen writes it (F-07). `/injuries/team-allocation` on the empty club: "Unallocated 0 · Everyone is on a team this week." (F-07). | **none** — `groups` carries no audit trigger (F-06; the 34 audited tables read from `pg_trigger` on scratch, `hv-triggers-ro.mjs`). | — |
| 6 | Priya | Thresholds: "Start with the default set", then one deactivated (Sleep dropped), one created ("Readiness under 2": readiness below a fixed 2, high, notifies coach + medic) | seeded set, edits audited | Empty state names the failure mode ("Nothing is being watched yet, so no flag can be raised") and offers the five-rule starter set; after: "4 active, 1 inactive" → "3 active, 2 inactive" → "4 active, 2 inactive". **No threshold can be edited** — a row offers Deactivate / Retire only; `58-thresholds.md` row 50 says a threshold "opens for editing, keeping a revision" (F-09). The new-threshold form's notify chips read "Sport_scientist", "Strength_conditioning" (F-01). **D-39 is half built:** the dashboard read "Open flags 0 · all acknowledged" on a club with no rules at all; only the thresholds screen says nothing is watched (F-03). | 5 × `thresholds.insert` (the seed, actor Priya), `thresholds.update {changed:[is_active]}`, `thresholds.insert` — **7 of 7** | — |
| 7 | Priya | Notifications: "High-severity flag raised" push → off | persists | "Push off" after a reload. | none — a personal preference (`notification_preferences` is not on the audited list; accepted). | — |
| 7b | Hannah (medic) | Two-factor: "Set up two-factor authentication" → QR + key → code from the key (computed in the scratchpad, `totp.py`; the key is in `hv-totp-secret.txt`, nowhere else) → "Confirm and turn on" | enrolled | "Two-factor authentication is on for your account · Status On · Added Sat 12 Sept · no recovery code — an admin has to remove this for you." Enrolment is Auth-side (`auth.mfa_factors`); the admin's removal route writes its own `audit_log` row (`settings/users/[userId]/mfa/route.ts:52`). | none for the enrolment (Auth schema only; accepted) | — |
| 8 | Priya | Invite people: Bethan Rees (coach) | account, link, audit | "Account created — Send this link to Bethan Rees yourself — no invite email went out." (reserved `.example` domain). Link single-use, confirms the address, no password set. **Bulk invite is athletes only** — the four remaining staff went through the single form, one each (by design; four forms). All six listed. **Every invited account shows "● Active" before its link is used** — `user_status` has `invited`, the route writes `active`; the list cannot tell an accepted account from a pending invite (F-12). "1 users" (F-10). | `users.insert`, `users.update {claims_version}`, `user_roles.insert {role:coach}`, `user.created {roles:[coach]}`, `invite.email_not_sent {note: reserved .example}` — **5 rows per invite** | **Screen:** on Priya's own row Medic is `disabled` + title "You cannot grant yourself the medic role. Another sport scientist can."; Sport scientist `disabled` + title "This is the only admin in the club — …"; Deactivate `disabled`, no reason (F-11: `disabled`+`title`, not BlockedButton — on a phone no reason is reachable). **Database, Priya's own session:** `POST user_roles {medic, self}` → **403** 0102's message; `DELETE user_roles?role=eq.sport_scientist&user_id=eq.self` → **403** 0101's message; **`PATCH users?id=eq.self {"status":"deactivated"}` → 200 — GATE, §0bd**, reverted 60 s later; `claims_version` unchanged, her token kept working. |
| 8b | Samantha (second SS) / Priya | Samantha removes her **own** SS role now that Priya holds one (0101's positive branch); Priya re-grants it; Samantha signs back in by magic link | allowed, then stale-claims sign-out | Chip enabled on both SS rows once two exist; the removal lands, her next request is bounced to `/login?e=stale-claims` (the claims-version gate); the re-grant lands; magic link → dashboard as `sport_scientist`. | `users.update {claims_version}`, `user_roles.delete {role:sport_scientist}`, `user_roles.changed {removed:[sport_scientist]}`; the re-grant mirrors it | Coach mirror of §0bd: Bethan `PATCH users {status:suspended}` 200, `{status:active}` 200, `{email:someone.else@…}` 200, reverted 200 — a non-admin flips her own status and email. |
| 9 | each staff | Follows their link at 1280; `/dashboard`; at 375 the bar and the More sheet | role-shaped nav | Landed: coach, medic, S&C, nutritionist, second SS. Sidebar: 8 rows for the four, 9 (with Analytics) for the SS — the always-refused rows filed on §0av today, not yet built. Bar: Dashboard · Squad · Schedule · **Flags** · More (SS, coach, medic); **Gym** for S&C; **Nutrition** for the nutritionist; the displaced row joins the sheet; Escape closes the sheet. Sidebar user line reads `Harlow Vale RFC · strength_conditioning` (F-01). | — | — |
| 10 | Priya | Add athlete ×2 (Megan Hollis, adult; Tia Nkemelu, under 18) with invite emails; bulk invite ×10 (paste, preview, send); groups filled from each group's "+ Add" panel; ten positions set from the profile's in-page Edit | 12 on the roster, links, audit | Add athlete: "Megan Hollis is on the squad. Send them this link…" — a link each. Bulk: preview "10 new athlete records will be created · 0 will link · 0 need attention", then "10 sent, 0 failed" with ten links shown once. Squad overview: "12 athletes in the squad"; the bulk ten arrive with no position (the CSV has no column; set afterwards — the profile's position is a free-text field where Add athlete's is a fixed list, F-14). Groups: Forwards 5, Backs 5, Academy 2. **`/squad` at 375 scrolls sideways** — `scrollWidth 438` on a 375 viewport: the roster `table` (404px) in a 341px wrapper with `overflow-x: visible` (F-17; not in the closed sideways item at §0u, which listed subject-access, retention and the leaderboard). Fresh profile at 1280: Athleticism "No data", "no tests defined for this club", S&C history "Adaptation notes are planned but not available yet", Injury "No current restrictions. This is not the same as being cleared.", ACWR "Building baseline · 0 of 21 days", Wellness "0 of 74 days submitted", Flags "0 open · n = 0", Goals "No active programme goal". Availability on every roster row: "Not recorded". | Add athlete: `athletes.insert`, `users.insert`, `users.update {claims_version}`, `user_roles.insert {athlete}`, `athletes.update {changed:[user_id]}` — 5 per athlete. Bulk: the same five per athlete plus one `user.bulk_invite {failed:0, user_ids:[…]}` — 51 rows. Group members: **none** (`group_memberships` is the decided high-volume exclusion). Positions: 10 × `athletes.update`. | — |
| 10b | Megan (9516), Tia (9517) | Follow their links at 375; `/today`; `/me` | landed; the under-18 rule | Both: "Evening, Megan/Tia", the week strip all Rest, **To do · 1 left: Weekly nutrition check-in** (no wellness to-do at 21:20 — checked again in the morning, step 16), "Nothing scheduled — You are not named in a session today. Rest or check with your coach.", then a card **"Not recorded · No restriction recorded. Speak to medical staff."** — a fit new player is told to speak to medical staff because no availability row exists yet (F-15). Me: Megan "Leaderboard · you appear unless you leave · Opted in"; Tia "you choose to appear · **Opted out**" — the under-18 default holds. | — | — |
| 10c | Priya | Add athlete with an email that already has a Fydr account at Ashcombe (`j.barnes@…`, name "Transfer Probe") | the transfer outcome | "Transfer Probe is on your squad. They have no app access at this club, and that is not a failure — … already belongs to a Fydr account, most likely at a previous club. … their old club's account cannot also be yours. The record you have just created is your club's own, and it starts empty…" — the four things, said. **Two consequences:** (a) any sport scientist at any club can learn whether an email is registered on Fydr (cross-club account enumeration; inherent in the transfer copy — a boundary for the security doc, not a gate); (b) **the probe row cannot be removed** — no screen writes `athletes.deleted_at` or `status = 'left_club'` (the enum value exists and is read by Settings' count, `settings/page.tsx:65`; nothing writes it), so a mistaken Add athlete, or an athlete who leaves, stays on the roster forever (F-16). "Transfer Probe" is still on Harlow Vale's roster — left for Isabella's decision, not repaired. | `athletes.insert` only (no user, no invite) | — |

**Day 0 done at 21:25** — 1 org, 1 season, 6 staff (one with TOTP), 13 athlete records (12 real + the probe), 3 groups, 6 thresholds, a logo. Sitting 1 findings are collected under "Findings" below and filed at the end of the sitting.

## Day 1 — structure

| Step | Actor | Action | Expected | Measured | Audit | Gate probes |
|---|---|---|---|---|---|---|
| 11 | Bethan (coach) | Six sessions from the form (one today at 22:00, five over 14–20 Sept with MD offsets), one fixture (v Wexcombe Ladies, Sat 19, 14:00, home); Edit mode: Gym A stepped 07:00 → 07:15 and held; publish offline, then online; "Save this week as a template"; the template applied to 21–27 Sept (needs its own fixture first — v Ridgeway Women, away) | the week; publish model; template round-trip | **Creation is immediate**: the form says "Athletes named in it see this in their athlete app as soon as you create it. There is no separate publish step" and Megan's Today read "Late skills (test) · 22:00 · Main pitch · starts in 33 min" before any publish. **Held edits are not**: after the stepper the banner read "1 change not yet in the athlete app · Athletes still see the schedule as it was", the block "07:15 EDITED", and Megan's row still `06:00Z`; after Publish, `06:15Z` and "The athlete app is up to date". **The stepper's floor is a silent no-op**: two "−" clicks at 07:00 (the floor) left the value at 07:00 but recorded "1 change not yet in the athlete app", the block "EDITED" and "Cancel changes" (F-18, the §0az family — the control should be blocked with its reason at the floor). **§0al's offline publish fails its second half — reopened on the to-do list**: Publish offline navigated the tab to Chrome's ERR_INTERNET_DISCONNECTED page within 1.5 s (the humanised "connection dropped" sentence does not match `isNetworkFailure`'s raw-engine regexes, so `router.refresh()` runs); the held change survived the reload via `sessionStorage`. **"Save this week as a template" saves nothing**: it opens "New week template — Starts at MD-6 to MD, empty except MD-1" and the builder reads "Nothing planned yet · 0 of 6 days planned" (F-19). A template is MD-anchored, so applying it to a week with no fixture creates nothing ("1 template position have no matching day this week" — grammar); with the fixture, "Add alongside" created 1 session, and **the Apply button stayed live afterwards with the plan recomputed to "+1 new"** — a second tap duplicates, and "can't be undone" (F-20). | `sessions.create` ×7 (six + the fixture's match session), `session_participants.add` ×18, `fixtures.insert`; the publish: `sessions.update`, `session_participants.remove` ×2 / `.add` ×2 (a time change re-writes the participants); the template: `week_template.applied`, `sessions.create`, `fixtures.insert` | **Database, own sessions:** `POST sessions` as medic / S&C / nutritionist / athlete → **403 ×4**; `POST thresholds` → 403 ×4; `POST groups` → 403 ×4. `SESSION_EDIT` = SS + coach holds at the database. |
| 12 | Amara (S&C) | Exercise library (empty → six exercises, steps 2.5 / 1.25 / 1 kg); programme "Pre-season strength", block Accumulation 3w, Lower A (back squat 4×5 @ 60, RDL 3×8 @ 50, push-up 3×12–15 load basis none) and Upper A (bench, weighted pull-up, DB row); Publish; assigned to the Forwards group | library, programme, athlete-side visibility | **A new club has no exercise library** — "0 exercises across 0 categories · No exercises yet. Add the first one." Ashcombe's came from the seed; a real club authors every lift by hand before its first programme (F-21). The add form promises "Everything else can be filled in later from the exercise itself", but there is no exercise page — the row chevron is decorative by the component's own comment (audit finding 33, unbuilt) (F-22). "1 exercises across 1 categories" (F-10). Load basis reads "Absolute / Percent 1rm / Percent bw / RPE / None" (F-01 family). Publish is one tap, no confirmation (PATTERN-S5 D1 declined — as decided). After assignment the page reads "Assigned 1 · Forwards", "View as an athlete" lists the five forwards. Megan's Gym tab: "Pre-season strength · Lower A · Upper A" and "Your coach hasn't set targets yet, but meal ideas are ready to browse." **The proposal gate for an injured athlete (§0bb) is not exercised yet — no injury exists until step 18; re-run then.** | `exercises.insert` ×6, `programmes.insert`, `programme_blocks.insert`, `programme_sessions.insert` ×2, `programme_exercises.insert` ×6, `programmes.update` (publish), `programme_assignments.insert` — **every write** | **Screen:** medic's `/programmes/new` reads "This will be a rehab programme."; coach's is redirected to `/programmes`. **Database:** S&C `POST programmes {programme_type: rehab}` → 403; coach `POST programmes {gym}` → 403; medic `POST programmes {gym}` → 403. |
| 13 | Laura (nutritionist) | Plan for Forwards (New plan → Create); protein stepped 1.9 → 2.0 and Assign; a manual target for Maeve (one athlete, any day); a meal saved to the library; Megan's Meal ideas | plan, targets, guidance without a weigh-in | Plan created ("Forwards · 5 athletes"). **Stepper edits are local until Assign, with no unsaved marker**: after "+" the page showed 2.0 and no Save/Discard control; a reload showed 1.9 (F-24). Assign: "Saved · 0 targets recomputed, **5 left as-is (their own personal target)**" — none of the five has a personal target; they were skipped for having no mass (F-25). **"Needs a word · 5 open"** lists five athletes "Weighed in 0/7" on the club's first day — every fresh athlete is an under-logger, and the badge reads the list's cap of five (`nutritionWorkspace.ts:317`), not the thirteen (F-23). Manual target saved (redirect to `/nutrition`, no confirmation line). Meal saved. Megan (no weigh-in): "We don't have a recent weigh-in on file for you, so portions are shown at a **standard reference weight (110 kg)**… Your club hasn't added its own recipes to the library yet — these are the standard starting meal ideas" — §0b L734 holds, but **110 kg is the reference for a women's squad** (F-26, a product decision). Her Today now carries "Working towards · v Wexcombe Ladies · Home · Sat 19 Sept". | **none** — `nutrition_rules` / `nutrition_targets` / `meal_library` carry no audit trigger (audit matrix) | **Database:** `POST nutrition_targets` as medic / S&C / coach / athlete → **403 ×4**; `POST meal_library` as medic / S&C / athlete → 403 ×3 (the coach is in `MEAL_LIBRARY_EDIT`, not probed). |
| 14 | Bethan (coach) | Leaderboard builder: Total distance · Total · Whole squad · This season · **Published to athletes**, "Total distance this season"; a second probe board created and deleted | board exists; renders once three qualify; the under-18 absent | The builder lists eleven eligible metrics and four blocked ones (Body mass, Readiness, Sleep, Soreness) as `BlockedButton`s with their reason on tap (11dc42f). The publish copy: "Under-18 athletes are off by default and only appear if they choose to be. Every athlete can leave at any time." The board: "Not enough results to rank. This board needs at least 3 athletes with a qualifying result. 0 athletes ranked · this season." (GPS arrives at step 15; re-read then.) After Save the button re-enables while the dev server compiles the board route (~6 s) — a second tap would create a duplicate; dev-latency only, noted (F-27, low). Delete = "Delete this board? This cannot be undone. Yes, delete / Never mind" → soft-deleted (`leaderboards.update`). | `leaderboards.insert` ×2, `leaderboards.update` (the delete) | **Database:** `POST leaderboards` as medic / nutritionist / athlete → 403 ×3. |
| 15 | Priya | GPS: the template fetched from `/settings/imports/template` (the header row and an example "Jamie Barnes" row); a three-athlete CSV for today's session imported; the same file re-imported with Megan's distance corrected (5120 → 5250); the batch exported | rows replaced, not doubled; history; export | "✓ hv-gps-1.csv: 3 records imported." — but "Recent imports · No imports yet." stayed on screen until a reload (the list is server-rendered and not refreshed by the client result; F-28, low). After the reload: one batch row "3 accepted · 0 rejected · 22:02 · CSV". Re-import: `gps_records` holds **3 rows, not 6**; Megan's reads 5250; all three carry the second batch's `import_batch_id` (the copy's "the history records which upload last wrote each row" — exact). **`session_id` is null on all three** although "Late skills (test)" was on the schedule for those athletes on that date — the import matches athlete + date only (noted for the training report at step 17). Export: `text/csv` with three caption lines then the rows; audited. | `import_batch.export` only — the **import itself writes no audit row** (`gps_records` / `import_batches` unaudited; the batch table is its own history) | **Database:** `POST gps_records` as coach / medic / athlete → 403 ×3 (`GPS_IMPORT` = SS only). |

**Day 1 done at 23:05** — a week on the schedule (+ a second week from the template), a gym programme assigned to the Forwards, a nutrition plan for the Forwards + one manual target + one meal, a published leaderboard, three GPS rows.
